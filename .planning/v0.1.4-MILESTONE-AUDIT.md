# Milestone v0.1.4 Audit (preliminary — milestone in progress)

**Status:** in_progress (1/9 phases complete, 38/41 REQs outstanding)
**Audited:** 2026-04-25
**Auditor:** integration-checker agent

## Executive Summary

Phase 114 is the only phase shipped; Phases 115-122 have no directories, plans, or commits. Phase 114's three plans (NAV-01..03) are **clean and verified**: all 31 bats tests across `tests/list.bats`, `tests/doctor.bats`, and `tests/commands-surface.bats` pass against the live codebase, and there is no TODO/FIXME/stub residue. The dispatch-precedence pre-flight from the v0.1.3→v0.1.4 audit was correctly resolved by research with a negative regression test, not a runtime guard. The four other plan-phase pre-flights (Phases 116, 117, 119, 120) remain pending — their owning phases haven't been planned. Cross-phase wiring established by Phase 114 (`_arcanon_is_project_dir`, `projectHashDir` export, 12-command iteration list, 8-check `cmdDoctor` matrix) is stable contract surface awaiting consumer-phase plans. Outstanding tech debt is uncommitted wrap-up state and the orthogonal untracked `CLAUDE.md`.

## Phase 114 Spot-Check

| # | Check | Status | Evidence |
|---|-------|--------|----------|
| 1 | `commands/list.md` exists with frontmatter + body | PASS | `plugins/arcanon/commands/list.md` (3363 bytes) |
| 2 | HANDLERS contains `list: cmdList` and `doctor: cmdDoctor` | PASS | `plugins/arcanon/worker/cli/hub.js:1229,1230` |
| 2b | HANDLERS does NOT contain `view: cmdView` | PASS | grep returns 0 hits; `tests/commands-surface.bats` Test 29 enforces |
| 3 | `_arcanon_is_project_dir` returns 0/1 only (no echo) | PASS | `plugins/arcanon/lib/worker-client.sh:86-103`; contract documented at lines 79-81; bats Tests 1-3 enforce empty stdout |
| 4 | `projectHashDir` exported from pool.js | PASS | `plugins/arcanon/worker/db/pool.js:34` |
| 5 | `tests/list.bats`, `tests/doctor.bats`, `commands/view.md` exist | PASS | All present; `commands/doctor.md` (3363 bytes), `commands/view.md` (2109 bytes) |
| 6 | `commands-surface.bats` iteration list = 12 commands | PASS | `tests/commands-surface.bats:18,27` both: `map drift impact sync login status export verify update list view doctor` |
| 7 | CHANGELOG `[Unreleased] / ### Added` has 3 lines for /list, /view, /doctor | PASS | `plugins/arcanon/CHANGELOG.md:11,15,20` |
| 8 | `bats tests/list.bats tests/doctor.bats tests/commands-surface.bats` passes | PASS | 31/31 green |

**Verdict:** Phase 114 implementation matches every claim in the three SUMMARY.md files. Zero drift between plan, summary, and live code.

## Cross-Phase Integration Surface

| Contract | Producer (Phase 114) | Future Consumer | Status |
|----------|----------------------|-----------------|--------|
| `_arcanon_is_project_dir` (bash 0/1 predicate) | `lib/worker-client.sh:86` | Markdown wrappers for `/correct`, `/rescan` (118), `/shadow-scan` (119) | DOCUMENTED + TEST-GUARDED |
| `projectHashDir` (Node export) | `worker/db/pool.js:34` | Phase 119 shadow DB pool, Phase 117 override-application path | EXPORTED + STABLE; consumers must `import from '../db/pool.js'` (NOT database.js — that copy stays private) |
| 12-command iteration list | `tests/commands-surface.bats:18,27` | Phase 116 (`--help` install loop), Phase 122 (`--help` regression grep) | STABLE for now. Risk: 116 and 122 will both mutate the same loop; coordinate to avoid stomping. |
| `cmdDoctor` 8-check + `--json` exit_code shape | `worker/cli/hub.js cmdDoctor` | Phase 122 VER-05 release-gate smoke (`/arcanon:doctor --json` against fresh install) | STABLE; JSON shape documented in 114-03-PLAN.md `<json_output_shape>` and verified by `tests/doctor.bats` Test 9. Phase 122 plan should pin `jq -e '.summary.exit_code == 0'`. |

**Risk callout for Phase 122:** ROADMAP §114 prose says "7 diagnostic checks" but REQUIREMENTS.md NAV-03 enumerates 8 and `cmdDoctor` ships 8. Flagged in 114-03-SUMMARY.md §Discrepancies — Phase 122 plan-phase should reconcile (update prose to "8-check" or accept with footnote).

## REQ Coverage

**Done:** 3 — NAV-01, NAV-02, NAV-03 (all in Phase 114).

**Outstanding:** 38 of 41 v0.1.4 requirements:

| Category | REQs | Owning Phase(s) | Status |
|----------|------|-----------------|--------|
| NAV | NAV-04 (1) | 115 | not started |
| HELP | HELP-01..04 (4) | 116 | not started |
| FRESH | FRESH-01..05 (5) | 116 | not started |
| CORRECT | CORRECT-01..07 (7) | 117 (discuss-phase pending), 118 | not started |
| SHADOW | SHADOW-01..04 (4) | 119 | not started |
| INT | INT-01..10 (10) | 120, 121 | not started |
| VER | VER-01..07 (7) | 122 | not started |
| **Total** | **38** | **8 phases** | |

Reconciles with `.planning/REQUIREMENTS.md:202-241`: NAV-01..03 are `[x]`, others `[ ]`. The traceability table's "Phase" column is still `TBD` for every row including the 3 completed REQs — documentation hygiene gap; backfill `Phase 114` for NAV-01..03 in the wrap-up commit.

## Tech Debt / Dangling State

| Artifact | State | Recommendation |
|----------|-------|----------------|
| `.planning/REQUIREMENTS.md` | uncommitted: NAV-02/03 checkboxes flipped to `[x]` | commit as 114-03 wrap-up |
| `.planning/STATE.md` | uncommitted: stopped_at, completed_phases 0→1, completed_plans 2→3, percent 67→100 | commit as 114-03 wrap-up |
| `114-03-SUMMARY.md` | untracked, on disk, complete | commit as 114-03 wrap-up |
| `CLAUDE.md` (repo root) | untracked, pre-existing GitNexus prompt | leave alone — orthogonal to this audit |
| Phase 114 has no `114-VERIFICATION.md` | gsd-verifier never run | run after wrap-up commit lands |

**Wrap-up ordering:** commit the three planning artifacts first (clean working tree), THEN run gsd-verifier (it sees a stable tree and produces VERIFICATION.md cleanly), THEN commit the verifier output. Running gsd-verifier against a dirty tree would force it to flag the dirtiness as a finding.

## Pre-Flight Risk Register Status

| Phase | Pre-flight | Status |
|-------|-----------|--------|
| 114 | `/arcanon:view` dispatch precedence vs `/arcanon:map view` | **RESOLVED.** 114-RESEARCH.md §2 established Claude Code uses filename-based slash-command resolution; no router exists, no double-dispatch risk. **No runtime guard ships.** Enforced by negative regression test in `tests/commands-surface.bats:29` (`! grep -q 'view: cmdView' worker/cli/hub.js`). 114-02-PLAN.md `<dispatch_precedence_finding>` block (lines 64-82) plus 114-RESEARCH.md §2 ensure future maintainers do not re-litigate. |
| 116 | `/arcanon:status` must wire through new `/api/scan-freshness`, NOT existing `/api/scan-quality` | PENDING — Phase 116 not planned |
| 117 | `manager.js:797-798` has no extension point between `persistFindings` and `endScan`; discuss-phase must pin override-apply hook injection site | PENDING — discuss-phase before plan-phase, neither started |
| 119 | `pool.js getQueryEngine` caches by `projectRoot` only; spec shadow-DB pool-key strategy | PENDING. Note: 114-01 already exported `projectHashDir` from pool.js, so Phase 119 has the path-resolution primitive ready. |
| 120 | `evidence_mode` must gate behind payload version bump (1.2) for hub receiver compat | PENDING — Phase 120 not planned |

1 of 5 resolved (Phase 114's, the only in-progress phase); 4 pending against unstarted phases. Register is doing its intended job.

## Anti-Patterns / Smells

Spot-checked the 3 Phase 114 SUMMARY.md files and shipped code (`hub.js`, `worker-client.sh`, `commands/{list,view,doctor}.md`):

- **TODOs / FIXMEs / XXX / HACK in shipped code:** zero matches.
- **"Deferred to later" / stubs:** only "stub" reference is `114-03-SUMMARY.md:38` describing `mock-hub.js` test fixture as "17-line http stub" — legitimate test infra, not deferred implementation.
- **Test skips:** zero `bats skip` in new bats files. The macOS HOK-06 failure mentioned in 114-02/03 summaries is pre-existing (documented in `STATE.md` Blockers/Concerns), not introduced by Phase 114, and lives outside the Phase 114 test suite.
- **Honest deviations recorded:** 114-03-SUMMARY.md §Deviations documents two auto-fixed bugs during execution (MCP liveness probe contract; `resolveCredentials` field-name typo `api_key` vs actual `apiKey`) with commit references. Correct hygiene.
- **Forward handoff:** 7-vs-8 doctor checks discrepancy logged in 114-03-SUMMARY.md §Discrepancies for Phase 122 doc reconciliation.

No anti-patterns crept in during implementation.

## Recommended Next Actions

1. **Commit the 114-03 wrap-up:** REQUIREMENTS.md NAV-02/03 checkbox flips + STATE.md counter advance + untracked 114-03-SUMMARY.md as one commit (`docs(114): close NAV-02/NAV-03 + advance milestone state to 1/9 phases`).
2. **Run gsd-verifier on Phase 114** against the clean tree; commit `114-VERIFICATION.md`. With 31/31 bats green and zero drift, expect a clean pass.
3. **Schedule Phase 115 plan-phase + Phase 117 discuss-phase as parallel streams.** Phase 115 is unblocked Wave-1 work; Phase 117 is the design-risk hotspot (only phase requiring `/gsd-discuss-phase`) and benefits from early de-risking.
4. **Backfill `Phase 114` into REQUIREMENTS.md traceability table** for NAV-01..03 rows (currently `TBD`).
5. **Surface the 7-vs-8 doctor checks discrepancy** to the Phase 122 planner for ROADMAP §114 prose update.

## Audit Verdict

**status:** in_progress (NOT passed, NOT failed — milestone genuinely not ready to audit; Phase 114 is the only completed unit, internally clean; 8 of 9 phases unstarted, so milestone-level audit is premature)

**Phase 114 sub-verdict:** PASS — 3 NAV REQs delivered, 31/31 bats green, no tech-debt residue, cross-phase contracts documented and test-guarded, dispatch-precedence pre-flight resolved correctly.

**Milestone progress:** 1/9 phases, 3/41 REQs, 1/5 pre-flights. Re-run audit after Phase 122 lands.

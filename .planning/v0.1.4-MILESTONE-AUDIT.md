# Milestone v0.1.4 — Final Audit

**Milestone:** v0.1.4 Operator Surface
**Audit date:** 2026-04-27
**Status:** READY TO SHIP — 9/9 phases (114-122), 21/21 plans, 41/41 REQs

## 1. Per-Phase Verdicts

| Phase | Title | Plans | REQs | Verdict | Rationale |
|---|---|---|---|---|---|
| 114 | Read-only nav (`list`/`view`/`doctor`) | 3/3 | NAV-01..03 | **PASS** | All 3 ship; `/view` pure-markdown (RESEARCH §2 dispatch); doctor 8 checks via 12 doctor.bats; 114-VERIFICATION 31/31 PASS. |
| 115 | Scan-version diff (`/diff`) | 2/2 | NAV-04 | **PASS** | `diffScanVersions(dbA,dbB,scanIdA,scanIdB)` engine pool-agnostic + read-only; 4 input forms; 35 engine + 13 bats green. |
| 116 | `--help` system + `/status` freshness | 2/2 | HELP-01..04, FRESH-01..05 | **PASS** | `lib/help.sh` extractor in all 13 commands; new `GET /api/scan-freshness` with per-repo `git rev-list --count`; existing `/api/scan-quality` byte-untouched. |
| 117 | `scan_overrides` persistence + apply hook | 2/2 | CORRECT-01..03 | **PASS** | Migration 017 idempotent; `applyPendingOverrides(scanVersionId,queryEngine,slog)` injected as hook between `persistFindings` and `endScan`; 25 unit + 2 E2E green. |
| 118 | `/correct`, `/rescan` operator commands | 2/2 | CORRECT-04..07 | **PASS** | `cmdCorrect` stages overrides (`created_by='cli'`); `cmdRescan` uses `scanSingleRepo(options.full=true)`; 11+5 bats green. **Production agent-runner gap → 503** (§4). |
| 119 | Shadow scan + atomic promote | 2/2 | SHADOW-01..04 | **PASS** | `getShadowQueryEngine` (uncached) sidesteps openDb singleton; live byte-identity via sha256 (Test 8); `evictLiveQueryEngine` clears pool Map AND `_db` singleton; WAL sidecars renamed; active scan-lock guard. |
| 120 | Integration data layer | 3/3 | INT-01..05 | **PASS** | Hub payload v1.2 gates `hash-only`/`none`; `full` byte-identical (Test M11); `--offline` Step 0.5 short-circuit; `--spec` repeatable; `known-externals.yaml` 20 entries DATA-ONLY. |
| 121 | Integration consumption + INT-10 | 3/3 | INT-06..10 | **PASS** | Migration 018 adds `actors.label`; per-repo `runActorLabeling` pass; `loadMergedCatalog` single-call seam; user `external_labels` wins; YAML byte-integrity proven. |
| 122 | Verification gate + release pin | 2/2 | VER-01..07 | **PASS** | bats 459/459, node 775/775, --help 17/17, greps 4/4, manifests at 0.1.4, lockfile regen, CHANGELOG pinned, prose drift (7→8) reconciled. |

## 2. REQ Coverage (41/41)

CHANGELOG `[0.1.4]` enumerates per-bullet REQ-IDs; spot-check confirms every REQUIREMENTS.md checkbox is `[x]`:

- **NAV (4):** Phases 114-115. **HELP (4):** Phase 116. **FRESH (5):** Phase 116. **CORRECT (7):** Phases 117-118. **SHADOW (4):** Phase 119. **INT (10):** Phases 120-121. **VER (7):** Phase 122.

= 4+4+5+7+4+10+7 = **41/41 ✅**

## 3. Cross-Phase Integration — All Contracts Held

| Contract | Status | Evidence |
|---|---|---|
| **117 schema vs 118 pre-locked assumptions** | **HELD** | 118-01 SUMMARY "Phase 117 Assumptions" P117-A..G all verified: `qe.upsertOverride({...})→number` at `query-engine.js:1231`; columns `kind`/`target_id`/`action`/`payload`/`created_by` exact shape; bats Test 11 asserts `created_by='cli'`. |
| **119 diff-engine signature vs 115 shipped** | **HELD verbatim** | 115-01 locked the contract. 119-02 SUMMARY: "Shape A landed verbatim, no fallback needed" — `cmdDiffShadow` dynamic-imports + passes both DB handles. Engine pool-agnostic regression (test 18) still green. |
| **121 catalog path vs 120 shipped location** | **HELD with adapter** | 121 plan assumed `entries:` key; 120 shipped `externals:`. Loader `normalizeCatalog` generalized as **single point of adaptation** — accepts both keys + map/list forms + `id`/`name` slug. No 120 file rewrite needed. |
| **119 `applyPendingOverrides` vs 117 actual signature** | **HELD transparently** | 117-02 shipped `(scanVersionId,queryEngine,slog)` invoked between `persistFindings` and `endScan`. Phase 119's shadow QE wraps SHADOW DB; apply-hook reads/writes shadow `scan_overrides` automatically — **zero shadow-specific code in 117-02**. |

## 4. Tech Debt / Deferred Items

`.planning/phases/118-.../deferred-items.md` (created by 118-02) tracks one HIGH item:

**RESCAN-PROD-01: Production agent-runner wiring** (HIGH, target v0.1.5)
- `worker/index.js` never calls `setAgentRunner` → `scanRepos` throws `agentRunner not initialized` at `manager.js:605`.
- Current state: `/api/rescan` and `/scan-shadow` return **503 with operator-friendly bootstrap message**; tests use `ARCANON_TEST_AGENT_RUNNER=1` stub.
- Remediation options: A) worker-owned runner reads `ARCANON_AGENT_BINARY`; B) host-orchestrated like `/arcanon:map`.

**Other deferred (lower priority):** 119-02 live↔shadow lock-key sharing; 120-01 persist `connections.line_start`/`line_end`; 120-01 hub-side v1.2 envelope adoption (separate repo); 121-01 catalog `--reload` flag. **No production blockers.**

## 5. Pre-Flight Risk Register Status (5/5 honored)

| # | Phase | Pre-flight requirement | Honored | Evidence |
|---|---|---|---|---|
| 1 | 114 | `/arcanon:view` dispatch precedence | **YES — by research** | RESEARCH §2: Claude Code resolves slash commands by exact filename match; no router-level double-dispatch. No guard ships. Permanent negative regression `tests/commands-surface.bats` Test 29: `! grep -q 'view: cmdView' worker/cli/hub.js`. |
| 2 | 116 | `/arcanon:status` routes through new `/api/scan-freshness` | **YES** | `cmdStatus` rewired in `worker/cli/hub.js` (commit `77e6d09`) — calls `_fetchScanFreshness` (replaces deleted `_fetchLatestScanLine`); existing `/api/scan-quality` byte-untouched, still consumed by `cmdList`. |
| 3 | 117 | `applyPendingOverrides` is a hook function, not a flag | **YES** | New module `worker/scan/overrides.js` exports `applyPendingOverrides(scanVersionId,queryEngine,slog)`. `manager.js`: 1 import + 1 call between line 797 (`persistFindings`) and 798 (`endScan`), +5 comment lines. |
| 4 | 119 | Shadow DB pool-key strategy | **YES — Option B** | `getShadowQueryEngine` always-fresh, never enters pool cache (RESEARCH §1 Option B). Sidesteps openDb singleton. Live and shadow can never collide. |
| 5 | 120 | `evidence_mode` gated behind payload v1.2 bump | **YES with byte-identity test** | State machine: `(full,*)`→`1.0`/`1.1` (preserved); `(hash-only|none,*)`→`1.2`. **Test M11** asserts `JSON.stringify(buildScanPayload({evidenceMode:"full",...}).payload) === JSON.stringify(buildScanPayload({...}).payload)` for v1.1 — proves zero drift for every existing v1.1 user. |

## 6. Test Counts — Final

| Suite | v0.1.4 Final | v0.1.3 Baseline | Delta |
|---|---|---|---|
| **bats** | **459/459** | ~340 | +~119. HOK-06 macOS p99 caveat **did not trigger** at threshold=200. New v0.1.4 files: list, doctor, diff, help, scan-freshness, correct, rescan, shadow-scan, promote-shadow, diff-shadow, drift-openapi-explicit-spec (INT-04+INT-10), known-externals-yaml, hub-evidence-mode, externals-labels, sync-offline, scan-overrides-apply. |
| **node** | **775/775 / 141 suites** | 630/631 (1 documented fail) | +~145. **The v0.1.3 `manager.test.js:676` mock failure is now resolved** by v0.1.4 work. Cleanest gate to date. |
| **--help smoke** | **17/17** | n/a (new) | +17. Substantive `lib/help.sh` sourcing (substituted for plan's structurally-wrong `bash hub.sh <cmd> --help`). 326B–1233B per command. |
| **regression greps** | **4/4** | 4/4 carry-over | 0. Zero `--help` leaks outside `## Help`; zero refs in README/skills/hooks; `runtime-deps.json` absent; `commands/upload.md` absent. |

## 7. Anti-Pattern Scan

**Production blockers:** None.

**Acknowledged accommodations (not blockers):**
- `ARCANON_TEST_AGENT_RUNNER=1` stub in `worker/index.js` (118-02) — production never sets it; canonical injection mechanism for shadow-scan/rescan tests.
- `/api/rescan` + `/scan-shadow` return 503 in production until agent-runner wired (deferred — §4).
- `_resetDbSingleton()` export in `database.js` (119-02) — first escape hatch from openDb singleton; underscore-prefix marks internal; only `evictLiveQueryEngine` calls it.

122-VERIFICATION.md anti-pattern review: no TODO/FIXME/PLACEHOLDER markers in shipped command handlers; no empty handlers; stub patterns limited to test fixtures.

**Documented deviation (resolved):** 122-01 substituted `bash hub.sh <cmd> --help` smoke with substantive `lib/help.sh` sourcing — same contract HELP-04 bats validates (all green).

## 8. Manifest + Release Pin Sanity

| Artifact | Status |
|---|---|
| 4 manifest files | **6 strings at "0.1.4"** ✅ |
| `package-lock.json` | regenerated via `npm install --package-lock-only` ✅ |
| `runtime-deps.json` | absent (Phase 107 deletion preserved) ✅ |
| `CHANGELOG.md [0.1.4] - 2026-04-27` | pinned with `### Added` + `### Changed`; empty subsections omitted per Keep-a-Changelog ✅ |
| Fresh `[Unreleased]` heading | retained ✅ |
| ROADMAP/REQUIREMENTS prose drift (7→8 doctor checks) | reconciled (commit `473ea96`) ✅ |

## 9. Final Verdict

**PASSED — v0.1.4 Operator Surface READY TO SHIP.**

All 9 phases complete and individually verified. All 41 requirements covered with per-bullet REQ-IDs in CHANGELOG. All 4 cross-phase contracts (117↔118, 115↔119 engine, 120↔121 catalog, 117↔119 apply-hook) held at execution. All 5 pre-flight risk register notes honored (4 by code, 1 by documented research). Test floors exceeded: bats 459/459 (≥340), node 775/775 (≥629), --help 17/17, greps 4/4. Manifests pinned at 0.1.4 across 4 files; lockfile regenerated; CHANGELOG collapsed cleanly.

One HIGH-priority deferred item (`RESCAN-PROD-01`) is documented with two remediation options for v0.1.5; current production behavior is a clean 503 with operator-friendly bootstrap message, not a crash. The v0.1.3 pre-existing `manager.test.js:676` failure is resolved by v0.1.4 work — v0.1.4 ships with **zero known pre-existing test failures**, the cleanest release gate to date.

**Next step:** `/gsd-complete-milestone v0.1.4`.

---
*Audit completed: 2026-04-27 by Claude (gsd-integration-checker)*

---
phase: 43-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/storage/query-engine.test.js
  - tests/storage/query-engine-upsert.test.js
  - tests/storage/api-surface.test.js
  - tests/storage/migration-007.test.js
  - tests/storage/scan-version-bracket.test.js
  - tests/worker/scan-bracket.test.js
  - worker/db/database.test.js
  - worker/db/migrations.test.js
  - worker/db/pool-repo.test.js
  - worker/db/query-engine-enrich.test.js
  - worker/db/query-engine-search.test.js
  - worker/db/snapshot.test.js
  - worker/scan/discovery.test.js
  - worker/scan/manager.test.js
  - worker/server/http.test.js
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "All JS test files use ligamen- prefixed temp directory names — no allclear-test- or allclear-*-test- patterns remain"
    - "All JS tests that reference allclear.config.json by name use ligamen.config.json instead"
    - "All JS tests that reference ~/.allclear/ data directory use ~/.ligamen/"
    - "All error message assertions in JS tests match renamed output strings"
  artifacts:
    - path: "worker/db/database.test.js"
      provides: "DB tests with ligamen- temp dir prefix"
      contains: "ligamen-test-"
    - path: "worker/scan/discovery.test.js"
      provides: "Discovery tests with ligamen.config.json"
      contains: "ligamen.config.json"
    - path: "worker/db/query-engine-search.test.js"
      provides: "Query engine tests with ~/.ligamen/ path"
      contains: ".ligamen"
    - path: "worker/server/http.test.js"
      provides: "HTTP server tests with /ligamen:map command ref"
      contains: "/ligamen:map"
  key_links:
    - from: "worker/scan/discovery.test.js"
      to: "worker/scan/discovery.js"
      via: "ligamen.config.json filename passed to loadFromConfig"
      pattern: "ligamen.config.json"
    - from: "worker/db/query-engine-search.test.js"
      to: "worker/db/query-engine.js"
      via: "~/.ligamen/ home directory path for DB storage"
      pattern: "\\.ligamen"
---

<objective>
Update all 15 JavaScript test files in tests/storage/, tests/worker/, worker/db/, worker/scan/, and worker/server/ to rename every allclear-prefixed temp directory, allclear.config.json reference, ~/.allclear/ path, and AllClear command reference to ligamen equivalents.

Purpose: The JS test suite (`node --test`) must pass against the renamed codebase — temp dirs, config filenames, and assertion strings must match ligamen naming.
Output: 15 JS test files fully renamed.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename temp dir prefixes and config paths in tests/storage/ and tests/worker/ JS tests</name>
  <files>tests/storage/query-engine.test.js, tests/storage/query-engine-upsert.test.js, tests/storage/api-surface.test.js, tests/storage/migration-007.test.js, tests/storage/scan-version-bracket.test.js, tests/worker/scan-bracket.test.js</files>

  <read_first>
    - tests/storage/query-engine.test.js
    - tests/storage/query-engine-upsert.test.js
    - tests/storage/api-surface.test.js
    - tests/storage/migration-007.test.js
    - tests/storage/scan-version-bracket.test.js
    - tests/worker/scan-bracket.test.js
  </read_first>

  <action>
Apply these exact substitutions in each file:

**tests/storage/query-engine.test.js:**
- `"allclear-test-"` → `"ligamen-test-"` (in os.tmpdir() join call)

**tests/storage/query-engine-upsert.test.js:**
- `"allclear-upsert-test-"` → `"ligamen-upsert-test-"` (in os.tmpdir() join call)

**tests/storage/api-surface.test.js:**
- `"allclear-test-"` → `"ligamen-test-"` (both occurrences in os.tmpdir() join calls)

**tests/storage/migration-007.test.js:**
- `"allclear-test-"` → `"ligamen-test-"` (in os.tmpdir() join call)

**tests/storage/scan-version-bracket.test.js:**
- `"allclear-svb-"` → `"ligamen-svb-"` (in os.tmpdir() join call)

**tests/worker/scan-bracket.test.js:**
- `"allclear-bracket-test-"` → `"ligamen-bracket-test-"` (in mkdtempSync call)
  </action>

  <verify>
    <automated>grep -rn "allclear" tests/storage/ tests/worker/ 2>/dev/null | grep -v "^Binary"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "allclear" tests/storage/query-engine.test.js` outputs 0
    - `grep -c "allclear" tests/storage/query-engine-upsert.test.js` outputs 0
    - `grep -c "allclear" tests/storage/api-surface.test.js` outputs 0
    - `grep -c "allclear" tests/storage/migration-007.test.js` outputs 0
    - `grep -c "allclear" tests/storage/scan-version-bracket.test.js` outputs 0
    - `grep -c "allclear" tests/worker/scan-bracket.test.js` outputs 0
    - `grep -c "ligamen-test-" tests/storage/api-surface.test.js` is 2 (two occurrences)
    - `grep -c "ligamen-svb-" tests/storage/scan-version-bracket.test.js` is 1
  </acceptance_criteria>

  <done>All 6 JS test files in tests/storage/ and tests/worker/ contain zero allclear references. All temp directory prefixes use ligamen- naming.</done>
</task>

<task type="auto">
  <name>Task 2: Rename temp dirs, config paths, data dirs, and command refs in worker/**/*.test.js</name>
  <files>worker/db/database.test.js, worker/db/migrations.test.js, worker/db/pool-repo.test.js, worker/db/query-engine-enrich.test.js, worker/db/query-engine-search.test.js, worker/db/snapshot.test.js, worker/scan/discovery.test.js, worker/scan/manager.test.js, worker/server/http.test.js</files>

  <read_first>
    - worker/db/database.test.js
    - worker/db/migrations.test.js
    - worker/db/pool-repo.test.js
    - worker/db/query-engine-enrich.test.js
    - worker/db/query-engine-search.test.js
    - worker/db/snapshot.test.js
    - worker/scan/discovery.test.js
    - worker/scan/manager.test.js
    - worker/server/http.test.js
  </read_first>

  <action>
Apply these exact substitutions:

**worker/db/database.test.js:**
- `"allclear-test-"` → `"ligamen-test-"` (in os.tmpdir() join for testRoot)
- `".allclear"` → `".ligamen"` (the literal string ".allclear" used as a subdirectory name in DB path construction — line ~51)

**worker/db/migrations.test.js:**
- `"allclear-schema-test-"` → `"ligamen-schema-test-"` (in os.tmpdir() join for testRoot)

**worker/db/pool-repo.test.js:**
- `"allclear-test-"` → `"ligamen-test-"` (in all 4 mkdtempSync calls)

**worker/db/query-engine-enrich.test.js:**
- `"allclear-enrich-test-"` → `"ligamen-enrich-test-"` (in mkdtempSync call)
- `// Write an allclear.config.json in tmpDir` → `// Write a ligamen.config.json in tmpDir` (comment)
- `"allclear.config.json"` → `"ligamen.config.json"` (in the join(tmpDir, "allclear.config.json") call — the config file name written during test setup)

**worker/db/query-engine-search.test.js:**
- `"allclear-search-test-"` → `"ligamen-search-test-"` (in mkdtempSync call)
- `".allclear"` → `".ligamen"` (in the homedir() path join for dbDir — this is the data directory path the query engine resolves)

**worker/db/snapshot.test.js:**
- `"allclear-snap-test-"` → `"ligamen-snap-test-"` (in os.tmpdir() join for testRoot)
- `"allclear-snap-test2-"` → `"ligamen-snap-test2-"` (in os.tmpdir() join for testRoot2)
- `"allclear-retention-test-"` → `"ligamen-retention-test-"` (in os.tmpdir() join for testRoot3)

**worker/scan/discovery.test.js:**
- `"allclear-test-"` → `"ligamen-test-"` (in mkdtempSync helper)
- `"allclear.config.json"` → `"ligamen.config.json"` (ALL occurrences — path.join calls, readFileSync calls, test description strings, and comments including: "loadFromConfig returns [] when allclear.config.json is missing" → "loadFromConfig returns [] when ligamen.config.json is missing", "saveConfirmed creates allclear.config.json when absent" → "saveConfirmed creates ligamen.config.json when absent")

**worker/scan/manager.test.js:**
- `"allclear-test-"` → `"ligamen-test-"` (in mkdtempSync call)
- `"allclear-nogit-"` → `"ligamen-nogit-"` (in mkdtempSync call for noGitDir)

**worker/server/http.test.js:**
- `"allclear-test-"` → `"ligamen-test-"` (in all 3 mkdtempSync calls)
- `"No map data yet. Pass ?project=/path/to/repo or run /allclear:map first."` → `"No map data yet. Pass ?project=/path/to/repo or run /ligamen:map first."` (error message assertion in assert.equal call)
  </action>

  <verify>
    <automated>grep -rn "allclear\|AllClear" worker/db/*.test.js worker/scan/discovery.test.js worker/scan/manager.test.js worker/server/http.test.js 2>/dev/null | grep -v "^Binary"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "allclear" worker/db/database.test.js` outputs 0
    - `grep -c "allclear" worker/db/migrations.test.js` outputs 0
    - `grep -c "allclear" worker/db/pool-repo.test.js` outputs 0
    - `grep -c "allclear" worker/db/query-engine-enrich.test.js` outputs 0
    - `grep -c "allclear" worker/db/query-engine-search.test.js` outputs 0
    - `grep -c "allclear" worker/db/snapshot.test.js` outputs 0
    - `grep -c "allclear" worker/scan/discovery.test.js` outputs 0
    - `grep -c "allclear" worker/scan/manager.test.js` outputs 0
    - `grep -c "allclear" worker/server/http.test.js` outputs 0
    - `grep -c "ligamen.config.json" worker/scan/discovery.test.js` is greater than 3 (multiple path joins and descriptions)
    - `grep -c ".ligamen" worker/db/query-engine-search.test.js` is greater than 0
    - `grep "ligamen:map" worker/server/http.test.js` contains the error message assertion
  </acceptance_criteria>

  <done>All 9 worker JS test files contain zero allclear references. Temp dir prefixes, config filenames, data dir paths, and command reference strings all use ligamen naming.</done>
</task>

</tasks>

<verification>
Run after both tasks complete:

```bash
# No allclear references in any JS test under tests/ or worker/
grep -rn "allclear" tests/storage/ tests/worker/ worker/db/*.test.js worker/scan/discovery.test.js worker/scan/manager.test.js worker/server/http.test.js 2>/dev/null | wc -l
# Expected: 0

# Verify ligamen temp dir prefixes present
grep -rn "ligamen-test-\|ligamen-svb-\|ligamen-search-\|ligamen-enrich-\|ligamen-snap-\|ligamen-bracket-" worker/db/ worker/scan/ worker/server/ tests/storage/ tests/worker/ 2>/dev/null | wc -l
# Expected: > 0
```
</verification>

<success_criteria>
All 15 JS test files contain zero occurrences of "allclear" or "AllClear" in any string, path, or comment. Every temp directory prefix, config filename reference, data directory path, and command assertion string uses ligamen naming.
</success_criteria>

<output>
After completion, create `.planning/phases/43-tests/43-02-SUMMARY.md` with:
- Files modified
- Substitutions made per file
- Verification command output
</output>

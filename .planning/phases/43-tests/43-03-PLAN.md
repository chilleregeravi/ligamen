---
phase: 43-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/config/ligamen.config.json
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "The test fixture config file is named ligamen.config.json — allclear.config.json does not exist under tests/fixtures/config/"
    - "The fixture content contains no allclear references"
  artifacts:
    - path: "tests/fixtures/config/ligamen.config.json"
      provides: "Test fixture config with linked-repos for bats tests"
      contains: "linked-repos"
  key_links:
    - from: "tests/config.bats"
      to: "tests/fixtures/config/ligamen.config.json"
      via: "LIGAMEN_CONFIG_FILE env var pointing to FIXTURE_DIR/ligamen.config.json"
      pattern: "ligamen.config.json"
---

<objective>
Rename the test fixture config file from allclear.config.json to ligamen.config.json and remove the old file.

Purpose: tests/config.bats references FIXTURE_DIR/ligamen.config.json after the env var rename in plan 01 — the fixture file must exist under the new name and the old name must be gone.
Output: tests/fixtures/config/ligamen.config.json exists, tests/fixtures/config/allclear.config.json deleted.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename fixture config file and verify old file removed</name>
  <files>tests/fixtures/config/ligamen.config.json</files>

  <read_first>
    - tests/fixtures/config/allclear.config.json
  </read_first>

  <action>
1. Read the contents of tests/fixtures/config/allclear.config.json. It currently contains:
   ```json
   {
     "linked-repos": ["../api", "../ui", "/opt/repos/sdk"]
   }
   ```

2. Create tests/fixtures/config/ligamen.config.json with the exact same content — the linked-repos values are real sibling paths, not brand names, so they stay unchanged:
   ```json
   {
     "linked-repos": ["../api", "../ui", "/opt/repos/sdk"]
   }
   ```

3. Delete tests/fixtures/config/allclear.config.json using the Bash tool:
   ```bash
   rm /Users/ravichillerega/sources/allclear/tests/fixtures/config/allclear.config.json
   ```
  </action>

  <verify>
    <automated>ls /Users/ravichillerega/sources/allclear/tests/fixtures/config/ && cat /Users/ravichillerega/sources/allclear/tests/fixtures/config/ligamen.config.json</automated>
  </verify>

  <acceptance_criteria>
    - `ls tests/fixtures/config/` does NOT list allclear.config.json
    - `ls tests/fixtures/config/` DOES list ligamen.config.json
    - `cat tests/fixtures/config/ligamen.config.json` contains `"linked-repos"` with the three repo paths
    - `grep -r "allclear" tests/fixtures/config/` outputs nothing
  </acceptance_criteria>

  <done>tests/fixtures/config/ligamen.config.json exists with the linked-repos content. tests/fixtures/config/allclear.config.json has been deleted. No allclear string appears anywhere in the fixtures/config/ directory.</done>
</task>

</tasks>

<verification>
Run after task completes:

```bash
# Old file gone
ls tests/fixtures/config/allclear.config.json 2>/dev/null && echo "ERROR: old file still exists" || echo "OK: old file removed"

# New file present and valid JSON
node -e "const f=require('./tests/fixtures/config/ligamen.config.json'); console.log(f['linked-repos'].length, 'repos')"
# Expected: 3 repos

# No allclear in fixtures dir
grep -r "allclear" tests/fixtures/ 2>/dev/null | wc -l
# Expected: 0
```
</verification>

<success_criteria>
tests/fixtures/config/ contains exactly ligamen.config.json with the three linked-repos paths. allclear.config.json is absent. The fixture directory contains zero allclear references.
</success_criteria>

<output>
After completion, create `.planning/phases/43-tests/43-03-SUMMARY.md` with:
- File renamed (old → new)
- Content of ligamen.config.json
- Verification command output confirming old file absent
</output>

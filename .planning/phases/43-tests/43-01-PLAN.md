---
phase: 43-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/config.bats
  - tests/session-start.bats
  - tests/worker-lifecycle.bats
  - tests/worker-index.bats
  - tests/file-guard.bats
  - tests/mcp-server.bats
  - tests/lint.bats
  - tests/siblings.bats
  - tests/drift-versions.bats
  - tests/integration/impact-flow.bats
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "All bats test files reference LIGAMEN_* env vars exclusively — no ALLCLEAR_* env var names remain in test code"
    - "All bats temp paths use /tmp/ligamen_* — no /tmp/allclear_* paths remain"
    - "All bats config file references use ligamen.config.json — allclear.config.json not referenced in tests"
    - "All bats assertion strings match renamed output (Ligamen active., /ligamen:*, AllClear replaced with Ligamen)"
  artifacts:
    - path: "tests/config.bats"
      provides: "Config layer tests with LIGAMEN_* env vars"
      contains: "LIGAMEN_CONFIG_FILE"
    - path: "tests/session-start.bats"
      provides: "Session start hook tests with ligamen naming"
      contains: "Ligamen active."
    - path: "tests/worker-lifecycle.bats"
      provides: "Worker lifecycle tests with LIGAMEN_DATA_DIR"
      contains: "LIGAMEN_DATA_DIR"
    - path: "tests/worker-index.bats"
      provides: "Worker index tests with LIGAMEN_* vars"
      contains: "LIGAMEN_WORKER_PORT"
  key_links:
    - from: "tests/config.bats"
      to: "scripts/config.sh"
      via: "env var LIGAMEN_CONFIG_FILE used in test setup"
      pattern: "LIGAMEN_CONFIG_FILE"
    - from: "tests/session-start.bats"
      to: "scripts/session-start.sh"
      via: "assertion strings must match renamed script output"
      pattern: "Ligamen active"
---

<objective>
Update all 10 bats test files to rename every ALLCLEAR_* env var, /tmp/allclear_* temp path, allclear.config.json reference, and AllClear assertion string to their ligamen equivalents.

Purpose: The bats test suite must pass against the renamed codebase — every test assertion, env var, and temp path must use ligamen naming.
Output: 10 bats files fully renamed, ready to run against phase-42 renamed scripts.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename env vars, temp paths, and config refs in core bats tests</name>
  <files>tests/config.bats, tests/worker-lifecycle.bats, tests/worker-index.bats, tests/mcp-server.bats, tests/lint.bats, tests/drift-versions.bats</files>

  <read_first>
    - tests/config.bats
    - tests/worker-lifecycle.bats
    - tests/worker-index.bats
    - tests/mcp-server.bats
    - tests/lint.bats
    - tests/drift-versions.bats
  </read_first>

  <action>
Apply these exact substitutions to each file listed:

**Env var renames (all occurrences, in test setup/teardown/assertions/inline):**
- `ALLCLEAR_CONFIG_FILE` → `LIGAMEN_CONFIG_FILE`
- `ALLCLEAR_CONFIG_LINKED_REPOS` → `LIGAMEN_CONFIG_LINKED_REPOS`
- `_ALLCLEAR_CONFIG_LOADED` → `_LIGAMEN_CONFIG_LOADED`
- `ALLCLEAR_DISABLE_FORMAT` → `LIGAMEN_DISABLE_FORMAT`
- `ALLCLEAR_DISABLE_LINT` → `LIGAMEN_DISABLE_LINT`
- `ALLCLEAR_DISABLE_GUARD` → `LIGAMEN_DISABLE_GUARD`
- `ALLCLEAR_LINT_THROTTLE` → `LIGAMEN_LINT_THROTTLE`
- `ALLCLEAR_EXTRA_BLOCKED` → `LIGAMEN_EXTRA_BLOCKED`
- `ALLCLEAR_DATA_DIR` → `LIGAMEN_DATA_DIR`
- `ALLCLEAR_WORKER_PORT` → `LIGAMEN_WORKER_PORT`
- `ALLCLEAR_LOG_LEVEL` → `LIGAMEN_LOG_LEVEL`
- `ALLCLEAR_DB_PATH` → `LIGAMEN_DB_PATH`

**Temp path renames:**
- `/tmp/allclear_clippy_` → `/tmp/ligamen_clippy_`

**Config file renames in allclear.config.json path refs:**
- `allclear.config.json` → `ligamen.config.json`

**Data dir path renames:**
- `.allclear/nonexistent-test.db` → `.ligamen/nonexistent-test.db`

**Comment header renames:**
- `# AllClear` prefix in file header comment → `# Ligamen`
- `# tests/config.bats — AllClear configuration` → `# tests/config.bats — Ligamen configuration`

Do NOT rename fixture file paths that are under tests/fixtures/ — those are handled in Task 3.
  </action>

  <verify>
    <automated>grep -rn "ALLCLEAR_\|/tmp/allclear_\|allclear\.config\.json\|\.allclear/" tests/config.bats tests/worker-lifecycle.bats tests/worker-index.bats tests/mcp-server.bats tests/lint.bats tests/drift-versions.bats 2>/dev/null | grep -v "^Binary" | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "ALLCLEAR_" tests/config.bats` outputs 0
    - `grep -c "ALLCLEAR_" tests/worker-lifecycle.bats` outputs 0
    - `grep -c "ALLCLEAR_" tests/worker-index.bats` outputs 0
    - `grep -c "ALLCLEAR_DB_PATH" tests/mcp-server.bats` outputs 0
    - `grep -c "/tmp/allclear_clippy_" tests/lint.bats` outputs 0
    - `grep -c "LIGAMEN_CONFIG_FILE" tests/config.bats` is greater than 0
    - `grep -c "LIGAMEN_DATA_DIR" tests/worker-lifecycle.bats` is greater than 0
    - `grep -c "LIGAMEN_WORKER_PORT" tests/worker-index.bats` is greater than 0
    - `grep -c "LIGAMEN_DB_PATH" tests/mcp-server.bats` is greater than 0
  </acceptance_criteria>

  <done>All six bats files contain zero ALLCLEAR_* strings and all env var, path, and config references use LIGAMEN_* naming.</done>
</task>

<task type="auto">
  <name>Task 2: Rename assertion strings and command refs in session-start, file-guard, siblings, and integration bats</name>
  <files>tests/session-start.bats, tests/file-guard.bats, tests/siblings.bats, tests/integration/impact-flow.bats</files>

  <read_first>
    - tests/session-start.bats
    - tests/file-guard.bats
    - tests/siblings.bats
    - tests/integration/impact-flow.bats
  </read_first>

  <action>
Apply these exact substitutions:

**In tests/session-start.bats:**
- `/tmp/allclear_session_` → `/tmp/ligamen_session_` (all occurrences of temp sentinel files)
- `/tmp/allclear_test_worker_started_` → `/tmp/ligamen_test_worker_started_` (integration sentinel paths)
- `allclear.config.json` → `ligamen.config.json` (all inline heredoc config file creation)
- `'AllClear active.'` → `'Ligamen active.'` (Python assertion string in embedded script)
- `'expected AllClear active. in context: '` → `'expected Ligamen active. in context: '`
- `'AllClear active'` → `'Ligamen active'` (second occurrence without period)
- `'expected AllClear active in context: '` → `'expected Ligamen active in context: '`
- `/allclear:quality-gate` → `/ligamen:quality-gate` (command reference in assertion strings)
- `'expected /allclear:quality-gate in context: '` → `'expected /ligamen:quality-gate in context: '`
- `'expected commands in context: '` — leave unchanged (generic)
- `AllClear worker: running` → `Ligamen worker: running` (mock function return strings)
- `ALLCLEAR_DISABLE_SESSION_START` → `LIGAMEN_DISABLE_SESSION_START`
- `ALLCLEAR_CHROMA_MODE` (in INTG-E2E-06 test) — leave as-is if in integration bats referencing SKILL.md content (it checks that SKILL.md still contains the var name)
- Header comment `# AllClear` → `# Ligamen` if present

**In tests/file-guard.bats:**
- `# AllClear — file-guard.bats` → `# Ligamen — file-guard.bats` (header)
- `# Human-readable denial message must carry the AllClear prefix.` → `# Human-readable denial message must carry the Ligamen prefix.`
- `@test "guard hook - block message contains AllClear prefix"` → `@test "guard hook - block message contains Ligamen prefix"`
- `assert_output --partial "AllClear"` → `assert_output --partial "Ligamen"` (both occurrences)
- Any `ALLCLEAR_DISABLE_GUARD` → `LIGAMEN_DISABLE_GUARD`

**In tests/siblings.bats:**
- `# AllClear — siblings.bats` → `# Ligamen — siblings.bats` (header)
- `allclear.config.json` → `ligamen.config.json` (config file name in test setup)
- `"siblings.sh - uses allclear.config.json linked-repos list when present"` → `"siblings.sh - uses ligamen.config.json linked-repos list when present"`
- `# allclear.config.json override` comment → `# ligamen.config.json override`

**In tests/integration/impact-flow.bats:**
- `/tmp/allclear_test_intg_worker_started` → `/tmp/ligamen_test_intg_worker_started`
- `/tmp/allclear_test_intg_worker_no_impact_map` → `/tmp/ligamen_test_intg_worker_no_impact_map`
- `allclear.config.json` → `ligamen.config.json` (all inline JSON heredoc blocks)
- `/tmp/allclear_session_intg-` → `/tmp/ligamen_session_intg-`
- `allclear-intg-e2e-05-` → `ligamen-intg-e2e-05-` (JS temp dir prefix in embedded Node script)
- `/allclear:quality-gate` → `/ligamen:quality-gate` (assertion string in embedded Python/JS)
- `'expected commands in context: '` — leave unchanged
- `ALLCLEAR_CHROMA_MODE` in INTG-E2E-06: this test checks that SKILL.md still contains "ALLCLEAR_CHROMA_MODE" — update ONLY IF phase-41 has renamed this var to LIGAMEN_CHROMA_MODE in SKILL.md. Since tests depend on phase-42 source changes, check what the skill file will say after renaming. For now, update to `LIGAMEN_CHROMA_MODE` to match the renamed env var from phase-40.
- `test@allclear` (git config email in test setup) — leave unchanged (it is a git user email, not a product reference)
  </action>

  <verify>
    <automated>grep -n "allclear\|ALLCLEAR\|AllClear" tests/session-start.bats tests/file-guard.bats tests/siblings.bats tests/integration/impact-flow.bats 2>/dev/null | grep -v "test@allclear" | grep -v "^Binary"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "AllClear active" tests/session-start.bats` outputs 0
    - `grep -c "/allclear:quality-gate" tests/session-start.bats` outputs 0
    - `grep -c "Ligamen active" tests/session-start.bats` is greater than 0
    - `grep -c "/ligamen:quality-gate" tests/session-start.bats` is greater than 0
    - `grep -c "AllClear" tests/file-guard.bats` outputs 0 (excluding header if already renamed)
    - `grep -c "Ligamen" tests/file-guard.bats` is greater than 0
    - `grep -c "allclear.config.json" tests/siblings.bats` outputs 0
    - `grep -c "ligamen.config.json" tests/siblings.bats` is greater than 0
    - `grep -c "/tmp/allclear_" tests/integration/impact-flow.bats` outputs 0
    - `grep -c "allclear.config.json" tests/integration/impact-flow.bats` outputs 0
  </acceptance_criteria>

  <done>All assertion strings, command references, temp paths, and config file names in session-start, file-guard, siblings, and integration bats use ligamen naming. Only test@allclear git email unchanged.</done>
</task>

</tasks>

<verification>
Run after both tasks complete:

```bash
# No ALLCLEAR_ env vars remain in any bats file
grep -rn "ALLCLEAR_" tests/*.bats tests/integration/*.bats 2>/dev/null | wc -l
# Expected: 0

# No AllClear assertion strings remain
grep -rn "AllClear" tests/*.bats tests/integration/*.bats 2>/dev/null | grep -v "test@allclear" | wc -l
# Expected: 0

# No /tmp/allclear_ paths remain
grep -rn "/tmp/allclear_" tests/*.bats tests/integration/*.bats 2>/dev/null | wc -l
# Expected: 0

# No allclear.config.json references remain
grep -rn "allclear\.config\.json" tests/*.bats tests/integration/*.bats 2>/dev/null | wc -l
# Expected: 0
```
</verification>

<success_criteria>
All 10 bats test files contain zero occurrences of ALLCLEAR_*, /tmp/allclear_*, "allclear.config.json", or "AllClear" (brand string) — except the git email test@allclear which is identity-irrelevant. All ligamen equivalents are present.
</success_criteria>

<output>
After completion, create `.planning/phases/43-tests/43-01-SUMMARY.md` with:
- Files modified
- Substitutions made per file
- Verification command output
</output>

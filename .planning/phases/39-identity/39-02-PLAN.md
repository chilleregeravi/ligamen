---
phase: 39-identity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - allclear.config.json.example
  - lib/config.sh
autonomous: true
requirements:
  - IDENT-03
  - IDENT-04
must_haves:
  truths:
    - "Makefile uses PLUGIN_NAME := ligamen and all targets reference ligamen"
    - "Example config file is named ligamen.config.json.example"
    - "lib/config.sh loads ligamen.config.json by default and uses LIGAMEN_CONFIG_* variable names"
  artifacts:
    - path: "Makefile"
      provides: "Build and install automation"
      contains: "PLUGIN_NAME := ligamen"
    - path: "ligamen.config.json.example"
      provides: "Config file template for users"
    - path: "lib/config.sh"
      provides: "Config loading library used by all scripts"
      contains: "LIGAMEN_CONFIG_FILE"
  key_links:
    - from: "lib/config.sh"
      to: "ligamen.config.json"
      via: "LIGAMEN_CONFIG_FILE default"
      pattern: "LIGAMEN_CONFIG_FILE.*ligamen\\.config\\.json"
    - from: "Makefile install target"
      to: "plugins/ligamen symlink"
      via: "PLUGIN_NAME variable"
      pattern: "PLUGIN_NAME := ligamen"
---

<objective>
Rename the Makefile plugin variable and the config filename from "allclear" to "ligamen".

Purpose: The Makefile is the primary dev workflow entrypoint (make install, make test). The config filename is the user-facing configuration artifact — it must be "ligamen.config.json" for users to override settings. lib/config.sh is the only source file in scope for this phase because it directly embeds the config filename as a default value.
Output: Updated Makefile, renamed allclear.config.json.example → ligamen.config.json.example, updated lib/config.sh.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Makefile PLUGIN_NAME to ligamen</name>
  <files>Makefile</files>
  <read_first>
    - Makefile — read the entire file before making any edits
  </read_first>
  <action>
    Apply these exact string replacements in Makefile:

    1. `PLUGIN_NAME := allclear` → `PLUGIN_NAME := ligamen`

    That single variable change cascades to all targets: `install`, `plugins/$(PLUGIN_NAME)`, and `uninstall` will all automatically reference `ligamen` because they use `$(PLUGIN_NAME)`. No other lines need changing.

    Note: The `plugins/allclear` symlink that currently exists in the working tree was created by a prior `make install` run. It is NOT managed by the Makefile source — only the new `plugins/ligamen` symlink will be created on the next `make install`. Optionally remove the stale symlink in this task:

    ```bash
    rm -f /Users/ravichillerega/sources/allclear/plugins/allclear
    ```
  </action>
  <verify>
    <automated>grep 'PLUGIN_NAME := ligamen' /Users/ravichillerega/sources/allclear/Makefile && grep -v 'PLUGIN_NAME := allclear' /Users/ravichillerega/sources/allclear/Makefile | grep -c 'allclear' | xargs -I{} sh -c 'if [ {} -eq 0 ]; then echo "Makefile OK"; else echo "FAIL: allclear still present in Makefile"; exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'PLUGIN_NAME := ligamen' Makefile` returns a match
    - `grep 'PLUGIN_NAME := allclear' Makefile` returns NO match
    - `grep 'allclear' Makefile` returns NO match (the variable drives all references)
  </acceptance_criteria>
  <done>Makefile has PLUGIN_NAME := ligamen. All install/uninstall targets reference ligamen via the variable. No allclear strings remain in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Rename config example file and update lib/config.sh defaults</name>
  <files>allclear.config.json.example, lib/config.sh</files>
  <read_first>
    - allclear.config.json.example — read current file content
    - lib/config.sh — read entire file before editing
  </read_first>
  <action>
    Step 1 — Rename the example config file:
    Rename `allclear.config.json.example` to `ligamen.config.json.example`.
    The file contents are `{ "linked-repos": ["../api", "../ui", "../sdk"] }` — contents do not change.
    Deletion of the old name and creation of the new name achieves the rename (git mv equivalent).

    Step 2 — Update lib/config.sh with these exact string replacements:

    1. Comment on line 2: `# lib/config.sh — Sourceable library: loads allclear.config.json if present.` → `# lib/config.sh — Sourceable library: loads ligamen.config.json if present.`
    2. Comment on line 8: `#   ALLCLEAR_CONFIG_LINKED_REPOS  — bash array of linked repo paths from config` → `#   LIGAMEN_CONFIG_LINKED_REPOS  — bash array of linked repo paths from config`
    3. Comment lines 10-11:
       `#   ALLCLEAR_CONFIG_FILE  — override path to config file` → `#   LIGAMEN_CONFIG_FILE  — override path to config file`
       `#                           (default: allclear.config.json in current directory)` → `#                           (default: ligamen.config.json in current directory)`
    4. Guard variable line 14: `if [[ -n "${_ALLCLEAR_CONFIG_LOADED:-}" ]]; then` → `if [[ -n "${_LIGAMEN_CONFIG_LOADED:-}" ]]; then`
    5. Guard set line 17: `_ALLCLEAR_CONFIG_LOADED=1` → `_LIGAMEN_CONFIG_LOADED=1`
    6. Env var default line 20: `ALLCLEAR_CONFIG_FILE="${ALLCLEAR_CONFIG_FILE:-allclear.config.json}"` → `LIGAMEN_CONFIG_FILE="${LIGAMEN_CONFIG_FILE:-ligamen.config.json}"`
    7. Array init line 23: `ALLCLEAR_CONFIG_LINKED_REPOS=()` → `LIGAMEN_CONFIG_LINKED_REPOS=()`
    8. File check line 25: `if [[ -f "$ALLCLEAR_CONFIG_FILE" ]]; then` → `if [[ -f "$LIGAMEN_CONFIG_FILE" ]]; then`
    9. Warning echo line 28: `echo "allclear: warning: allclear.config.json is malformed, using defaults" >&2` → `echo "ligamen: warning: ligamen.config.json is malformed, using defaults" >&2`
    10. jq read line 33: `done < <(jq -r '.["linked-repos"][]? // empty' "$ALLCLEAR_CONFIG_FILE" 2>/dev/null)` → `done < <(jq -r '.["linked-repos"][]? // empty' "$LIGAMEN_CONFIG_FILE" 2>/dev/null)`
    11. Array append line 32: `ALLCLEAR_CONFIG_LINKED_REPOS+=("$_linked_repo_path")` → `LIGAMEN_CONFIG_LINKED_REPOS+=("$_linked_repo_path")`
    12. Footer comment line 37: `# Note: Do NOT export ALLCLEAR_CONFIG_LINKED_REPOS — bash cannot export arrays` → `# Note: Do NOT export LIGAMEN_CONFIG_LINKED_REPOS — bash cannot export arrays`

    Note: lib/config.sh references to ALLCLEAR_CONFIG_* will be fully renamed in Phase 40 (Environment & Paths) for all OTHER scripts. This task renames only the config filename default value and the config-specific variable names internal to lib/config.sh. Do NOT rename any ALLCLEAR_ variables that are not directly related to the config file loading logic in lib/config.sh.

    Wait — re-check scope: IDENT-04 says "config filename renamed across all references". Phase 40 handles ALLCLEAR_* env var renames. The config-specific env override (ALLCLEAR_CONFIG_FILE) is the mechanism to point at the config file — this IS a config filename reference and must be renamed to LIGAMEN_CONFIG_FILE here. Other ALLCLEAR_* env vars (ALLCLEAR_DATA_DIR, etc.) are Phase 40 scope. Rename only what is in lib/config.sh as listed above.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/ravichillerega/sources/allclear && source lib/config.sh && echo "LIGAMEN_CONFIG_FILE=${LIGAMEN_CONFIG_FILE}" && [[ "${LIGAMEN_CONFIG_FILE}" == "ligamen.config.json" ]] && echo "config.sh OK"'</automated>
  </verify>
  <acceptance_criteria>
    - `ls ligamen.config.json.example` returns the file (exists)
    - `ls allclear.config.json.example` returns NOT FOUND (removed)
    - `grep 'allclear' lib/config.sh` returns NO match
    - `grep 'LIGAMEN_CONFIG_FILE' lib/config.sh` returns matches (the variable is defined)
    - `grep 'ligamen.config.json' lib/config.sh` returns a match (default filename)
    - `source lib/config.sh && echo $LIGAMEN_CONFIG_FILE` outputs `ligamen.config.json`
  </acceptance_criteria>
  <done>allclear.config.json.example is deleted and ligamen.config.json.example exists with identical content. lib/config.sh loads ligamen.config.json by default, uses LIGAMEN_CONFIG_FILE and LIGAMEN_CONFIG_LINKED_REPOS as variable names, and contains no allclear strings.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# Makefile has no allclear references
grep 'allclear' /Users/ravichillerega/sources/allclear/Makefile && echo FAIL || echo "Makefile clean"

# Old config example is gone, new one exists
ls /Users/ravichillerega/sources/allclear/ligamen.config.json.example
ls /Users/ravichillerega/sources/allclear/allclear.config.json.example 2>/dev/null && echo "FAIL: old file still exists" || echo "old file removed"

# config.sh has no allclear strings
grep 'allclear' /Users/ravichillerega/sources/allclear/lib/config.sh && echo FAIL || echo "config.sh clean"

# config.sh sources cleanly and sets correct default
bash -c 'source /Users/ravichillerega/sources/allclear/lib/config.sh && echo "default: $LIGAMEN_CONFIG_FILE"'
# Expected: default: ligamen.config.json
```
</verification>

<success_criteria>
- Makefile: PLUGIN_NAME := ligamen, zero allclear strings
- ligamen.config.json.example exists with content {"linked-repos": ["../api", "../ui", "../sdk"]}
- allclear.config.json.example does NOT exist
- lib/config.sh: default config file is ligamen.config.json, all internal variables use LIGAMEN_ prefix, zero allclear strings
</success_criteria>

<output>
After completion, create `.planning/phases/39-identity/39-02-SUMMARY.md` with what was changed.
</output>

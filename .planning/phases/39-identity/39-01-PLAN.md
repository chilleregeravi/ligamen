---
phase: 39-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
autonomous: true
requirements:
  - IDENT-01
  - IDENT-02
must_haves:
  truths:
    - "package.json name is @ligamen/cli with binary ligamen-init and updated description"
    - "plugin.json name, author, and repository reference ligamen"
    - "marketplace.json name, plugin entry name, and source path reference ligamen"
  artifacts:
    - path: "package.json"
      provides: "npm package identity"
      contains: "@ligamen/cli"
    - path: ".claude-plugin/plugin.json"
      provides: "Claude Code plugin manifest"
      contains: "\"name\": \"ligamen\""
    - path: ".claude-plugin/marketplace.json"
      provides: "Claude Code marketplace listing"
      contains: "\"name\": \"ligamen\""
  key_links:
    - from: "package.json"
      to: "npm registry"
      via: "name field"
      pattern: "@ligamen/cli"
    - from: ".claude-plugin/plugin.json"
      to: "Claude Code plugin loader"
      via: "name field"
      pattern: "\"name\": \"ligamen\""
---

<objective>
Rename the npm package and Claude Code plugin manifests from "allclear" to "ligamen".

Purpose: The npm package and plugin manifests are the primary identity artifacts. After this plan npm install produces @ligamen/cli with a ligamen-init binary, and Claude Code marketplace discovery shows the plugin as "ligamen".
Output: Updated package.json, .claude-plugin/plugin.json, and .claude-plugin/marketplace.json.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename npm package identity in package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json — read the entire current file before making any edits
  </read_first>
  <action>
    Apply the following exact string replacements in package.json:

    1. `"name": "@allclear/cli"` → `"name": "@ligamen/cli"`
    2. `"description": "AllClear — auto-format, auto-lint, and cross-repo quality gates for Claude Code"` → `"description": "Ligamen — auto-format, auto-lint, and cross-repo quality gates for Claude Code"`
    3. `"url": "https://github.com/AetherHQ/allclear"` → `"url": "https://github.com/AetherHQ/ligamen"`
    4. `"allclear-init": "./bin/allclear-init.js"` → `"ligamen-init": "./bin/ligamen-init.js"` (inside the "bin" object)

    No other fields change. The version, license, engines, dependencies, scripts remain untouched.
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('/Users/ravichillerega/sources/allclear/package.json','utf8')); console.assert(p.name==='@ligamen/cli','name wrong'); console.assert(p.bin['ligamen-init']==='./bin/ligamen-init.js','bin wrong'); console.assert(!p.bin['allclear-init'],'old bin still present'); console.assert(!p.description.includes('AllClear'),'old desc'); console.log('package.json OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"@ligamen/cli"' package.json` returns a match
    - `grep 'allclear' package.json` returns NO match
    - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0 (valid JSON)
  </acceptance_criteria>
  <done>package.json has @ligamen/cli as name, ligamen-init as the binary key, AetherHQ/ligamen as repo URL, and no remaining allclear strings.</done>
</task>

<task type="auto">
  <name>Task 2: Rename plugin manifests in plugin.json and marketplace.json</name>
  <files>.claude-plugin/plugin.json, .claude-plugin/marketplace.json</files>
  <read_first>
    - .claude-plugin/plugin.json — read entire file before editing
    - .claude-plugin/marketplace.json — read entire file before editing
  </read_first>
  <action>
    In .claude-plugin/plugin.json apply these exact string replacements:

    1. `"name": "allclear"` → `"name": "ligamen"`
    2. `"author": { "name": "AllClear Contributors" }` → `"author": { "name": "Ligamen Contributors" }`
    3. `"repository": "https://github.com/AetherHQ/allclear"` → `"repository": "https://github.com/AetherHQ/ligamen"`
    4. `"description": "Auto-format, auto-lint, and cross-repo quality gates for Claude Code"` — leave unchanged (no brand name embedded)

    In .claude-plugin/marketplace.json apply these exact string replacements:

    1. Top-level `"name": "allclear"` → `"name": "ligamen"`
    2. Inside the plugins array: `"name": "allclear"` → `"name": "ligamen"`
    3. `"source": "./plugins/allclear"` → `"source": "./plugins/ligamen"`
  </action>
  <verify>
    <automated>node -e "
const fs=require('fs');
const p=JSON.parse(fs.readFileSync('/Users/ravichillerega/sources/allclear/.claude-plugin/plugin.json','utf8'));
console.assert(p.name==='ligamen','plugin name wrong');
console.assert(p.author.name==='Ligamen Contributors','author wrong');
const m=JSON.parse(fs.readFileSync('/Users/ravichillerega/sources/allclear/.claude-plugin/marketplace.json','utf8'));
console.assert(m.name==='ligamen','marketplace top-level name wrong');
console.assert(m.plugins[0].name==='ligamen','marketplace plugin entry name wrong');
console.assert(m.plugins[0].source==='./plugins/ligamen','marketplace source wrong');
console.log('manifests OK');
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"ligamen"' .claude-plugin/plugin.json` returns matches for the name field
    - `grep 'allclear' .claude-plugin/plugin.json` returns NO match
    - `grep '"ligamen"' .claude-plugin/marketplace.json` returns matches
    - `grep 'allclear' .claude-plugin/marketplace.json` returns NO match
    - Both files remain valid JSON (jq empty .claude-plugin/plugin.json && jq empty .claude-plugin/marketplace.json exits 0)
  </acceptance_criteria>
  <done>plugin.json has name "ligamen" and Ligamen Contributors as author. marketplace.json has "ligamen" in all three locations (top-level name, plugin entry name, and source path). No allclear strings remain in either file.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# No allclear strings remain in the three identity files
grep -n 'allclear' package.json .claude-plugin/plugin.json .claude-plugin/marketplace.json

# All three files are valid JSON
node -e "['package.json','.claude-plugin/plugin.json','.claude-plugin/marketplace.json'].forEach(f=>{ JSON.parse(require('fs').readFileSync('/Users/ravichillerega/sources/allclear/'+f,'utf8')); console.log(f+' valid'); })"

# Package name is correct
node -e "console.log(JSON.parse(require('fs').readFileSync('/Users/ravichillerega/sources/allclear/package.json','utf8')).name)"
# Expected output: @ligamen/cli
```
</verification>

<success_criteria>
- package.json: name=@ligamen/cli, bin key=ligamen-init, repo=AetherHQ/ligamen, description says Ligamen, zero allclear strings
- plugin.json: name=ligamen, author=Ligamen Contributors, repo=AetherHQ/ligamen, zero allclear strings
- marketplace.json: top name=ligamen, plugin entry name=ligamen, source=./plugins/ligamen, zero allclear strings
- All three files are valid JSON
</success_criteria>

<output>
After completion, create `.planning/phases/39-identity/39-01-SUMMARY.md` with what was changed.
</output>

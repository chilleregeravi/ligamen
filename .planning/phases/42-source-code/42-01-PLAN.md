---
phase: 42-source-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pulse-check.sh
  - scripts/worker-stop.sh
  - scripts/impact.sh
  - scripts/lint.sh
  - scripts/mcp-wrapper.sh
  - scripts/file-guard.sh
  - scripts/session-start.sh
  - scripts/worker-start.sh
  - scripts/drift-versions.sh
  - scripts/format.sh
  - lib/worker-client.sh
autonomous: true
requirements:
  - CODE-01
  - CODE-03

must_haves:
  truths:
    - "No shell script comment header contains the string 'AllClear'"
    - "No shell script stdout/stderr message contains the string 'AllClear'"
    - "session-start.sh emits 'Ligamen active.' — not 'AllClear active.'"
  artifacts:
    - path: "scripts/session-start.sh"
      provides: "Session start hook with Ligamen branding"
      contains: "Ligamen active."
    - path: "scripts/lint.sh"
      provides: "Lint hook with Ligamen output prefix"
      contains: "Ligamen lint"
    - path: "scripts/file-guard.sh"
      provides: "File guard with Ligamen prefix in denial messages"
      contains: "Ligamen:"
    - path: "lib/worker-client.sh"
      provides: "Worker client with Ligamen branding in status output"
      contains: "Ligamen worker:"
  key_links:
    - from: "scripts/session-start.sh"
      to: "terminal output"
      via: "CONTEXT variable"
      pattern: "CONTEXT=\"Ligamen active"
---

<objective>
Rename all "AllClear" branding strings in shell scripts to "Ligamen".

Purpose: Shell scripts are the user-visible entry points for hooks and skills. Anywhere a script prints to stdout/stderr or carries a comment header identifying the product, it must say "Ligamen".

Output: 11 shell script files with all "AllClear" comment headers and output messages replaced by "Ligamen" equivalents. The session start hook's CONTEXT variable emits "Ligamen active." so Claude Code sessions see the new name.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename AllClear → Ligamen in scripts/ comment headers and output messages</name>
  <files>
    scripts/pulse-check.sh
    scripts/worker-stop.sh
    scripts/impact.sh
    scripts/lint.sh
    scripts/mcp-wrapper.sh
    scripts/file-guard.sh
    scripts/worker-start.sh
    scripts/drift-versions.sh
    scripts/format.sh
  </files>
  <read_first>
    - scripts/pulse-check.sh
    - scripts/worker-stop.sh
    - scripts/impact.sh
    - scripts/lint.sh
    - scripts/mcp-wrapper.sh
    - scripts/file-guard.sh
    - scripts/worker-start.sh
    - scripts/drift-versions.sh
    - scripts/format.sh
  </read_first>
  <action>
In each file below, replace exactly the strings listed. Read each file first, make targeted edits — do not reformat or alter logic.

**scripts/pulse-check.sh** (line 2):
- `# AllClear Pulse Skill — service health check helper`
  → `# Ligamen Pulse Skill — service health check helper`

**scripts/worker-stop.sh** (lines 2-3):
- `# AllClear — worker-stop.sh`
  → `# Ligamen — worker-stop.sh`
- `# Gracefully stops the AllClear background worker process.`
  → `# Gracefully stops the Ligamen background worker process.`

**scripts/impact.sh** (line 2):
- `# scripts/impact.sh — Cross-repo reference scanner for AllClear`
  → `# scripts/impact.sh — Cross-repo reference scanner for Ligamen`

**scripts/lint.sh** (line 157):
- `MSG="AllClear lint [${LINTER_NAME}]: ${FILE}`
  → `MSG="Ligamen lint [${LINTER_NAME}]: ${FILE}`

**scripts/mcp-wrapper.sh** (line 2):
- `# Wrapper to launch the AllClear MCP server.`
  → `# Wrapper to launch the Ligamen MCP server.`

**scripts/file-guard.sh** (lines 53, 56, 63):
- `printf 'AllClear: blocked write to %s -- %s\n'`
  → `printf 'Ligamen: blocked write to %s -- %s\n'`
- `"permissionDecisionReason":"AllClear: blocked write to %s -- %s"`
  → `"permissionDecisionReason":"Ligamen: blocked write to %s -- %s"`
- `printf '{"systemMessage": "AllClear: %s -- %s"}\n'`
  → `printf '{"systemMessage": "Ligamen: %s -- %s"}\n'`

**scripts/worker-start.sh** (lines 2-3):
- `# AllClear — worker-start.sh`
  → `# Ligamen — worker-start.sh`
- `# Starts the AllClear background worker process as a daemon.`
  → `# Starts the Ligamen background worker process as a daemon.`

**scripts/drift-versions.sh** (line 3):
- `# Part of the AllClear drift skill (Phase 10, DRFT-01, DRFT-05, DRFT-06)`
  → `# Part of the Ligamen drift skill (Phase 10, DRFT-01, DRFT-05, DRFT-06)`

**scripts/format.sh** (line 2):
- `# AllClear: Auto-format hook`
  → `# Ligamen: Auto-format hook`

IMPORTANT: Do NOT change /tmp/allclear_* paths, ALLCLEAR_* env var names, or allclear.config.json references in these files — those are covered by Phase 40 (env/paths) and Phase 39 (config filename). Only change human-readable "AllClear" branding strings in comments and output messages.
  </action>
  <verify>
    <automated>grep -rn "AllClear" /Users/ravichillerega/sources/allclear/scripts/ --include="*.sh" | grep -v "ALLCLEAR_\|allclear\.config\|/tmp/allclear\|\.allclear\|/allclear:"</automated>
  </verify>
  <acceptance_criteria>
    - The grep command above returns zero lines (no "AllClear" branding strings remain in scripts/)
    - grep -n "Ligamen" scripts/lint.sh returns a line matching MSG="Ligamen lint
    - grep -n "Ligamen:" scripts/file-guard.sh returns 3 lines (stderr printf, JSON permissionDecisionReason, JSON systemMessage)
    - grep -n "Ligamen" scripts/format.sh returns line 2: "# Ligamen: Auto-format hook"
  </acceptance_criteria>
  <done>All 9 scripts/ shell files have "AllClear" replaced with "Ligamen" in comment headers and output messages only.</done>
</task>

<task type="auto">
  <name>Task 2: Rename AllClear → Ligamen in session-start.sh CONTEXT and lib/worker-client.sh output</name>
  <files>
    scripts/session-start.sh
    lib/worker-client.sh
  </files>
  <read_first>
    - scripts/session-start.sh
    - lib/worker-client.sh
  </read_first>
  <action>
**scripts/session-start.sh** — three changes:

Line 2 (comment header):
- `# AllClear — session-start.sh`
  → `# Ligamen — session-start.sh`

Line 4 (comment):
- `# Injects project type and available allclear commands into session context exactly once.`
  → `# Injects project type and available ligamen commands into session context exactly once.`

Lines 88, 90 (CONTEXT variable — CODE-03):
- `CONTEXT="AllClear active."`
  → `CONTEXT="Ligamen active."`
- `CONTEXT="AllClear active. Detected: ${PROJECT_TYPES}."`
  → `CONTEXT="Ligamen active. Detected: ${PROJECT_TYPES}."`

IMPORTANT: Do NOT change /tmp/allclear_session_* on line 32, the ALLCLEAR_DISABLE_SESSION_START env var reference on line 12, or the allclear.config.json reference on line 53 — those are covered by Phase 40 and Phase 39.

**lib/worker-client.sh** — three changes:

Line 2 (comment header):
- `# lib/worker-client.sh — AllClear worker HTTP client helpers`
  → `# lib/worker-client.sh — Ligamen worker HTTP client helpers`

Lines 72, 74 (echo output messages):
- `echo "AllClear worker: running (port ${port})"`
  → `echo "Ligamen worker: running (port ${port})"`
- `echo "AllClear worker: running"`
  → `echo "Ligamen worker: running"`

IMPORTANT: Do NOT change ALLCLEAR_DATA_DIR references or ~/.allclear paths in lib/worker-client.sh — those are Phase 40.
  </action>
  <verify>
    <automated>grep -n "AllClear" /Users/ravichillerega/sources/allclear/scripts/session-start.sh /Users/ravichillerega/sources/allclear/lib/worker-client.sh | grep -v "ALLCLEAR_\|allclear\.config\|/tmp/allclear\|\.allclear\|/allclear:"</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "CONTEXT=" scripts/session-start.sh | grep "AllClear" returns zero lines
    - grep -n 'CONTEXT="Ligamen active' scripts/session-start.sh returns exactly 2 lines (lines 88 and 90)
    - grep -n "Ligamen worker:" lib/worker-client.sh returns 2 lines
    - The grep command in verify returns zero lines
  </acceptance_criteria>
  <done>session-start.sh emits "Ligamen active." and lib/worker-client.sh prints "Ligamen worker: running". No "AllClear" branding strings remain in either file.</done>
</task>

</tasks>

<verification>
Run the combined check across all shell files touched by this plan:

```bash
grep -rn "AllClear" \
  /Users/ravichillerega/sources/allclear/scripts/ \
  /Users/ravichillerega/sources/allclear/lib/worker-client.sh \
  | grep -v "ALLCLEAR_\|allclear\.config\|/tmp/allclear\|\.allclear\|/allclear:"
```

Expected: zero output.

Spot-check session-start CONTEXT:
```bash
grep -n "CONTEXT=" /Users/ravichillerega/sources/allclear/scripts/session-start.sh
```
Expected: both lines contain "Ligamen active."
</verification>

<success_criteria>
1. Shell script comment headers say "Ligamen" — no "AllClear" comment headers remain in scripts/ or lib/
2. All stdout/stderr output messages in shell scripts say "Ligamen" — grep for bare "AllClear" in sh files returns zero branding hits
3. session-start.sh CONTEXT variable is "Ligamen active." and "Ligamen active. Detected: ${PROJECT_TYPES}."
4. lib/worker-client.sh status output says "Ligamen worker: running"
</success_criteria>

<output>
After completion, create `.planning/phases/42-source-code/42-01-SUMMARY.md` using the summary template.
</output>

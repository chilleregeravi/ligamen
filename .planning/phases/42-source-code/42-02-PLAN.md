---
phase: 42-source-code
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - worker/ui/graph.js
  - worker/server/chroma.js
  - worker/mcp/server.js
  - worker/scan/findings.js
  - worker/scan/discovery.js
  - worker/scan/manager.js
  - worker/scan/confirmation.js
  - worker/db/database.js
  - worker/db/query-engine.js
  - worker/scan/agent-prompt-common.md
  - worker/scan/agent-prompt-deep.md
  - worker/scan/agent-prompt-discovery.md
  - worker/scan/agent-prompt-infra.md
  - worker/scan/agent-prompt-library.md
  - worker/scan/agent-prompt-service.md
autonomous: true
requirements:
  - CODE-02

must_haves:
  truths:
    - "No JavaScript source file header contains the string 'AllClear'"
    - "No internal JS log message or tool description string contains 'AllClear'"
    - "No agent prompt file heading contains 'AllClear'"
  artifacts:
    - path: "worker/mcp/server.js"
      provides: "MCP server with Ligamen branding in tool description"
      contains: "AllClear HTTP worker"
    - path: "worker/scan/agent-prompt-common.md"
      provides: "Agent prompt with Ligamen heading"
      contains: "# Ligamen Scan"
    - path: "worker/db/database.js"
      provides: "DB module with Ligamen file header"
      contains: "Ligamen v2.0"
  key_links:
    - from: "worker/mcp/server.js line 678"
      to: "MCP tool description"
      via: "string literal"
      pattern: "Ligamen HTTP worker"
---

<objective>
Rename all "AllClear" branding strings in JavaScript source files and agent prompt markdown files to "Ligamen".

Purpose: JS file headers identify the module and its origin product. The MCP tool description is user-visible in Claude's tool list. Agent prompt headings are read by sub-agents and should reflect the new product name.

Output: 9 JavaScript source files with "AllClear" replaced in file headers and internal strings, plus 6 agent prompt .md files with headings updated to "Ligamen".
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename AllClear → Ligamen in JS source file headers and internal strings</name>
  <files>
    worker/ui/graph.js
    worker/server/chroma.js
    worker/mcp/server.js
    worker/scan/findings.js
    worker/scan/discovery.js
    worker/scan/manager.js
    worker/scan/confirmation.js
    worker/db/database.js
    worker/db/query-engine.js
  </files>
  <read_first>
    - worker/ui/graph.js (line 2)
    - worker/server/chroma.js (line 2)
    - worker/mcp/server.js (lines 1-5, line 678)
    - worker/scan/findings.js (lines 2, 80)
    - worker/scan/discovery.js (line 2)
    - worker/scan/manager.js (line 2)
    - worker/scan/confirmation.js (line 2)
    - worker/db/database.js (line 2)
    - worker/db/query-engine.js (line 2)
  </read_first>
  <action>
In each file, replace exactly the strings listed. Read the file first, make targeted edits — do not reformat or alter logic.

**worker/ui/graph.js** (line 2):
- ` * graph.js — Entry point for AllClear service dependency graph UI.`
  → ` * graph.js — Entry point for Ligamen service dependency graph UI.`

**worker/server/chroma.js** (line 2):
- ` * worker/chroma-sync.js — ChromaDB async sync module for AllClear v2.0`
  → ` * worker/chroma-sync.js — ChromaDB async sync module for Ligamen v2.0`

**worker/mcp/server.js** (line 678):
- `"Trigger a dependency scan via the AllClear HTTP worker. Returns unavailable when the worker is not running.",`
  → `"Trigger a dependency scan via the Ligamen HTTP worker. Returns unavailable when the worker is not running.",`

**worker/scan/findings.js** (lines 2 and 80):
- ` * worker/findings-schema.js — Findings schema validator for AllClear v2.0`
  → ` * worker/findings-schema.js — Findings schema validator for Ligamen v2.0`
- ` * Validates an agent findings object against the AllClear findings schema.`
  → ` * Validates an agent findings object against the Ligamen findings schema.`

**worker/scan/discovery.js** (line 2):
- ` * worker/repo-discovery.js — Repo discovery module for AllClear v2.0`
  → ` * worker/repo-discovery.js — Repo discovery module for Ligamen v2.0`

**worker/scan/manager.js** (line 2):
- ` * worker/scan-manager.js — Scan orchestration for AllClear v2.0 agent scanning.`
  → ` * worker/scan-manager.js — Scan orchestration for Ligamen v2.0 agent scanning.`

**worker/scan/confirmation.js** (line 2):
- ` * worker/confirmation-flow.js — User confirmation flow module for AllClear v2.0`
  → ` * worker/confirmation-flow.js — User confirmation flow module for Ligamen v2.0`

**worker/db/database.js** (line 2):
- ` * worker/db.js — Database lifecycle module for AllClear v2.0`
  → ` * worker/db.js — Database lifecycle module for Ligamen v2.0`

**worker/db/query-engine.js** (line 2):
- ` * worker/query-engine.js — Read/write query layer over the AllClear SQLite schema.`
  → ` * worker/query-engine.js — Read/write query layer over the Ligamen SQLite schema.`

IMPORTANT: Do NOT change ALLCLEAR_DATA_DIR, allclear.config.json, ~/.allclear, allclear-impact collection name, /allclear: command references, or any functional code — only comment headers and the one user-visible tool description string in mcp/server.js. Those other strings are covered by earlier phases (39, 40, 41).
  </action>
  <verify>
    <automated>grep -rn "AllClear" /Users/ravichillerega/sources/allclear/worker/ --include="*.js" | grep -v "\.test\.js" | grep -v "node_modules" | grep -v "ALLCLEAR_DATA_DIR\|allclear\.config\|\.allclear\|allclear-impact\|/allclear:"</automated>
  </verify>
  <acceptance_criteria>
    - The grep command above returns zero lines
    - grep -n "Ligamen" worker/mcp/server.js | grep "HTTP worker" returns exactly 1 line containing "Ligamen HTTP worker"
    - grep -n "Ligamen" worker/scan/findings.js returns 2 lines (header + JSDoc comment)
    - grep -n "Ligamen" worker/db/database.js returns a line on line 2 with "Database lifecycle module for Ligamen"
    - grep -n "Ligamen" worker/db/query-engine.js returns a line on line 2 with "Ligamen SQLite schema"
  </acceptance_criteria>
  <done>All 9 JS source files have "AllClear" replaced with "Ligamen" in file headers and internal description strings. Functional code and env/path references are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rename AllClear → Ligamen in agent prompt headings</name>
  <files>
    worker/scan/agent-prompt-common.md
    worker/scan/agent-prompt-deep.md
    worker/scan/agent-prompt-discovery.md
    worker/scan/agent-prompt-infra.md
    worker/scan/agent-prompt-library.md
    worker/scan/agent-prompt-service.md
  </files>
  <read_first>
    - worker/scan/agent-prompt-common.md
    - worker/scan/agent-prompt-deep.md
    - worker/scan/agent-prompt-discovery.md
    - worker/scan/agent-prompt-infra.md
    - worker/scan/agent-prompt-library.md
    - worker/scan/agent-prompt-service.md
  </read_first>
  <action>
Each agent prompt file has exactly one "AllClear" occurrence in the H1 heading on line 1. Replace each heading:

**worker/scan/agent-prompt-common.md** (line 1):
- `# AllClear Scan — Common Rules`
  → `# Ligamen Scan — Common Rules`

**worker/scan/agent-prompt-deep.md** (line 1):
- `# AllClear Deep Scan Agent — Phase 2`
  → `# Ligamen Deep Scan Agent — Phase 2`

**worker/scan/agent-prompt-discovery.md** (line 1):
- `# AllClear Discovery Agent — Phase 1`
  → `# Ligamen Discovery Agent — Phase 1`

**worker/scan/agent-prompt-infra.md** (line 1):
- `# AllClear Scan — Infrastructure Repository`
  → `# Ligamen Scan — Infrastructure Repository`

**worker/scan/agent-prompt-library.md** (line 1):
- `# AllClear Scan — Library / SDK Repository`
  → `# Ligamen Scan — Library / SDK Repository`

**worker/scan/agent-prompt-service.md** (line 1):
- `# AllClear Scan — Service Repository`
  → `# Ligamen Scan — Service Repository`

Read each file fully before editing — confirm there are no additional "AllClear" occurrences beyond line 1 before saving.
  </action>
  <verify>
    <automated>grep -rn "AllClear" /Users/ravichillerega/sources/allclear/worker/scan/ --include="*.md"</automated>
  </verify>
  <acceptance_criteria>
    - The grep command above returns zero lines
    - grep -n "^# Ligamen" worker/scan/agent-prompt-common.md returns line 1: "# Ligamen Scan — Common Rules"
    - grep -n "^# Ligamen" worker/scan/agent-prompt-deep.md returns line 1: "# Ligamen Deep Scan Agent — Phase 2"
    - All 6 agent-prompt-*.md files have their H1 heading starting with "# Ligamen"
  </acceptance_criteria>
  <done>All 6 agent prompt files have H1 headings updated from "AllClear" to "Ligamen". Sub-agents reading these prompts see the new product name.</done>
</task>

</tasks>

<verification>
Run the combined check across all JS and agent prompt files touched by this plan:

```bash
grep -rn "AllClear" \
  /Users/ravichillerega/sources/allclear/worker/ \
  --include="*.js" --include="*.md" \
  | grep -v "\.test\.js" \
  | grep -v "node_modules" \
  | grep -v "ALLCLEAR_DATA_DIR\|allclear\.config\|\.allclear\|allclear-impact\|/allclear:"
```

Expected: zero output.
</verification>

<success_criteria>
1. All 9 JS source file comment headers say "Ligamen v2.0" — no "AllClear" in JSDoc headers
2. The MCP server tool description says "Ligamen HTTP worker" — confirmed grep hit on line 678
3. All 6 agent prompt H1 headings say "# Ligamen ..." — no "AllClear" headings remain
4. Functional code, env vars, path strings, and collection names are unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/42-source-code/42-02-SUMMARY.md` using the summary template.
</output>

---
phase: 47-test-doc-cleanup
plan: 01
type: execute
wave: 1
depends_on: ["46-02"]
files_modified:
  - tests/structure.bats
  - docs/architecture.md
  - docs/commands.md
  - scripts/session-start.sh
  - README.md
autonomous: true
requirements:
  - CLN-01
  - CLN-02

must_haves:
  truths:
    - "The bats test suite runs with zero failures after the pulse and deploy-verify commands are gone"
    - "A full-text search for 'pulse' and 'deploy-verify' across the repo (excluding git history) returns zero results"
  artifacts:
    - path: "tests/structure.bats"
      provides: "Structural validation tests referencing only the three remaining commands"
      contains: "quality-gate cross-impact drift"
    - path: "docs/commands.md"
      provides: "Commands reference doc listing only the remaining commands"
    - path: "docs/architecture.md"
      provides: "Architecture doc with an accurate project-structure listing"
    - path: "scripts/session-start.sh"
      provides: "Session hook injecting only active command names into context"
    - path: "README.md"
      provides: "README capability list with no Kubernetes-specific command entries"
  key_links:
    - from: "tests/structure.bats"
      to: "commands/ directory"
      via: "file-existence assertions for each command"
      pattern: "for cmd in quality-gate cross-impact drift"
---

<objective>
Remove every remaining reference to the pulse and deploy-verify commands from tests and documentation so the repository contains no trace of those commands after Phase 46 deletes the command files.

Purpose: After Phase 46 deletes the command files and primary docs, any leftover mention in tests, architecture docs, the commands reference, session-start, or the README will either cause test failures or mislead users. This phase eliminates those references.
Output: Clean test suite (no failures from missing command files), accurate docs, and a repo-wide search for "pulse" / "deploy-verify" that returns zero hits.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix structure.bats — remove pulse and deploy-verify from command lists</name>
  <files>tests/structure.bats</files>
  <read_first>
    Read tests/structure.bats lines 60-80 to see the two test cases that iterate over command names.
  </read_first>
  <action>
    Edit tests/structure.bats:

    Line 64: Change
      `for cmd in quality-gate cross-impact drift pulse deploy-verify; do`
    to
      `for cmd in quality-gate cross-impact drift; do`

    Line 71: Change
      `for cmd in quality-gate cross-impact drift pulse deploy-verify; do`
    to
      `for cmd in quality-gate cross-impact drift; do`

    Also update the test name on line 64 from "all five command files exist" to
    "all command files exist" (the count is now three, not five; drop the number).

    No other changes to this file.
  </action>
  <verify>
    <automated>grep -n "pulse\|deploy.verify" /Users/ravichillerega/sources/ligamen/tests/structure.bats; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pulse\|deploy-verify" tests/structure.bats` returns no output (zero matches)
    - The two for-loop lines each read: `for cmd in quality-gate cross-impact drift; do`
    - The test name no longer says "five"
  </acceptance_criteria>
  <done>tests/structure.bats contains no references to pulse or deploy-verify</done>
</task>

<task type="auto">
  <name>Task 2: Sweep all remaining doc and script references</name>
  <files>
    docs/architecture.md
    docs/commands.md
    scripts/session-start.sh
    README.md
  </files>
  <read_first>
    Read the following files in full before editing:
    - docs/architecture.md (project structure listing — lines 1-68)
    - docs/commands.md (command reference — lines 49-66 contain the pulse and deploy-verify sections)
    - scripts/session-start.sh line 92 (CONTEXT string)
    - README.md lines 43-48 (capability bullets)
  </read_first>
  <action>
    **docs/architecture.md** — Remove lines 13 and 14 from the project-structure code block:
      `    pulse.md`
      `    deploy-verify.md`
    The commands/ block should list only: quality-gate.md, cross-impact.md, drift.md, map.md

    **docs/commands.md** — Delete the two complete command sections:
      - Delete from `## \`/ligamen:pulse\` — Service Health` through the closing line of that section (lines 49-57, including the blank line before the next heading)
      - Delete from `## \`/ligamen:deploy-verify\` — Deploy Verification` through end of file (lines 58-66)

    **scripts/session-start.sh** — On the CONTEXT line (currently):
      `CONTEXT="${CONTEXT} Commands: /ligamen:quality-gate, /ligamen:cross-impact, /ligamen:drift, /ligamen:pulse, /ligamen:deploy-verify."`
    Change to:
      `CONTEXT="${CONTEXT} Commands: /ligamen:quality-gate, /ligamen:cross-impact, /ligamen:drift."`

    **README.md** — Remove the two bullet lines:
      `- \`/ligamen:pulse\` — Kubernetes service health check`
      `- \`/ligamen:deploy-verify\` — compare expected vs actual cluster state`
    Leave surrounding bullets intact.
  </action>
  <verify>
    <automated>grep -rn "pulse\|deploy.verify\|deploy_verify" /Users/ravichillerega/sources/ligamen/docs/ /Users/ravichillerega/sources/ligamen/scripts/session-start.sh /Users/ravichillerega/sources/ligamen/README.md 2>/dev/null; echo "grep exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "pulse\|deploy-verify" docs/ scripts/session-start.sh README.md` returns zero matches
    - docs/architecture.md commands/ block lists only: quality-gate.md, cross-impact.md, drift.md, map.md
    - docs/commands.md ends after the `/ligamen:drift` section with no pulse or deploy-verify sections
    - scripts/session-start.sh CONTEXT line lists: /ligamen:quality-gate, /ligamen:cross-impact, /ligamen:drift
    - README.md no longer has bullet points for pulse or deploy-verify
  </acceptance_criteria>
  <done>All four files contain zero references to pulse or deploy-verify</done>
</task>

</tasks>

<verification>
Run after both tasks complete:

1. Confirm zero repo-wide matches (excluding git history and .planning/):
   ```
   grep -rn "pulse\|deploy.verify\|deploy_verify" \
     /Users/ravichillerega/sources/ligamen/ \
     --include="*.md" --include="*.bats" --include="*.bash" \
     --include="*.sh" --include="*.json" --include="*.yaml" \
     2>/dev/null | grep -v "\.planning/\|\.git/"
   ```
   Expected: zero lines of output

2. Confirm bats structural tests pass (Phase 46 must be complete first, but the test logic itself must be correct):
   ```
   bats /Users/ravichillerega/sources/ligamen/tests/structure.bats
   ```
   Expected: all tests pass (or fail only because Phase 46 hasn't run yet — the test logic must be valid)
</verification>

<success_criteria>
- Zero occurrences of "pulse" or "deploy-verify" in any tracked file outside of git history
- tests/structure.bats command lists contain only quality-gate, cross-impact, drift
- docs/commands.md has no pulse or deploy-verify sections
- docs/architecture.md project-structure listing has no pulse.md or deploy-verify.md entries
- scripts/session-start.sh CONTEXT string lists only the three remaining commands
- README.md has no Kubernetes-specific command bullet points
</success_criteria>

<output>
After completion, create `.planning/phases/47-test-doc-cleanup/47-01-SUMMARY.md` following the summary template.
</output>

---
phase: 49-directory-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/ligamen/commands/cross-impact.md
  - plugins/ligamen/commands/drift.md
  - plugins/ligamen/commands/map.md
  - plugins/ligamen/commands/quality-gate.md
  - plugins/ligamen/hooks/hooks.json
  - plugins/ligamen/hooks/lint.json
  - plugins/ligamen/scripts/drift-common.sh
  - plugins/ligamen/scripts/drift-openapi.sh
  - plugins/ligamen/scripts/drift-types.sh
  - plugins/ligamen/scripts/drift-versions.sh
  - plugins/ligamen/scripts/file-guard.sh
  - plugins/ligamen/scripts/format.sh
  - plugins/ligamen/scripts/impact.sh
  - plugins/ligamen/scripts/lint.sh
  - plugins/ligamen/scripts/mcp-wrapper.sh
  - plugins/ligamen/scripts/session-start.sh
  - plugins/ligamen/scripts/worker-start.sh
  - plugins/ligamen/scripts/worker-stop.sh
  - plugins/ligamen/lib/config.sh
  - plugins/ligamen/lib/detect.sh
  - plugins/ligamen/lib/linked-repos.sh
  - plugins/ligamen/lib/worker-client.sh
  - plugins/ligamen/worker/index.js
  - plugins/ligamen/skills/impact/
  - plugins/ligamen/skills/quality-gate/
  - plugins/ligamen/.claude-plugin/marketplace.json
  - plugins/ligamen/.claude-plugin/plugin.json
  - plugins/ligamen/package.json
  - plugins/ligamen/package-lock.json
  - plugins/ligamen/ligamen.config.json.example
autonomous: false
requirements:
  - STR-01
  - STR-02

must_haves:
  truths:
    - "plugins/ligamen/ exists and contains all plugin source directories (commands, hooks, scripts, worker, lib, skills, .claude-plugin)"
    - "plugins/ligamen/ contains package.json, package-lock.json, and ligamen.config.json.example"
    - "Root of repo contains no plugin source directories — commands/, hooks/, scripts/, worker/, lib/, skills/, .claude-plugin/ are gone from root"
    - "Git history is preserved — moved files show as 'renamed' not 'deleted + added' in git log"
  artifacts:
    - path: "plugins/ligamen/commands/cross-impact.md"
      provides: "Commands moved to plugins/ligamen/"
    - path: "plugins/ligamen/hooks/hooks.json"
      provides: "Hooks moved to plugins/ligamen/"
    - path: "plugins/ligamen/.claude-plugin/plugin.json"
      provides: ".claude-plugin moved to plugins/ligamen/"
    - path: "plugins/ligamen/package.json"
      provides: "package.json moved to plugins/ligamen/"
    - path: "plugins/ligamen/scripts/format.sh"
      provides: "scripts moved to plugins/ligamen/"
    - path: "plugins/ligamen/lib/config.sh"
      provides: "lib moved to plugins/ligamen/"
    - path: "plugins/ligamen/worker/index.js"
      provides: "worker moved to plugins/ligamen/"
    - path: "plugins/ligamen/skills/impact"
      provides: "skills moved to plugins/ligamen/"
  key_links:
    - from: "root"
      to: "plugins/ligamen/"
      via: "git mv (history-preserving rename)"
      pattern: "renamed.*plugins/ligamen"
---

<objective>
Move all plugin source files into `plugins/ligamen/` using `git mv` to preserve history, leaving only repo-level files at the repository root.

Purpose: Restructure the repo for Claude Code marketplace distribution. The marketplace install model (`claude plugin marketplace add`) requires plugin source to live in a subdirectory, not at the repo root.

Output: `plugins/ligamen/` containing all plugin files; root containing only README.md, LICENSE, Makefile, docs/, tests/, .planning/, .mcp.json, and dotfiles.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugins/ligamen/ and move all plugin source via git mv</name>
  <files>
    plugins/ligamen/commands/,
    plugins/ligamen/hooks/,
    plugins/ligamen/scripts/,
    plugins/ligamen/worker/,
    plugins/ligamen/lib/,
    plugins/ligamen/skills/,
    plugins/ligamen/.claude-plugin/,
    plugins/ligamen/package.json,
    plugins/ligamen/package-lock.json,
    plugins/ligamen/ligamen.config.json.example
  </files>
  <read_first>
    - /Users/ravichillerega/sources/ligamen/.gitignore — confirm `plugins/` is listed (it is: "plugins/"); you must REMOVE this line before running git mv, otherwise git will ignore the destination and the move will fail
    - /Users/ravichillerega/sources/ligamen/Makefile — understand the current symlink-based install approach that will be replaced in Phase 50
  </read_first>
  <action>
CRITICAL: The `.gitignore` currently contains `plugins/` which would cause git to ignore the destination directory. Before running any git mv commands, remove the `plugins/` line from `.gitignore` and commit or stage that change.

Step 1 — Remove `plugins/` from `.gitignore`:
Edit `/Users/ravichillerega/sources/ligamen/.gitignore` to remove the line `plugins/`.

Step 2 — Create the destination directory structure:
```
mkdir -p /Users/ravichillerega/sources/ligamen/plugins/ligamen
```

Step 3 — Run all git mv commands from the repo root (`/Users/ravichillerega/sources/ligamen`):
```
git mv commands    plugins/ligamen/commands
git mv hooks       plugins/ligamen/hooks
git mv scripts     plugins/ligamen/scripts
git mv worker      plugins/ligamen/worker
git mv lib         plugins/ligamen/lib
git mv skills      plugins/ligamen/skills
git mv .claude-plugin plugins/ligamen/.claude-plugin
git mv package.json        plugins/ligamen/package.json
git mv package-lock.json   plugins/ligamen/package-lock.json
git mv ligamen.config.json.example plugins/ligamen/ligamen.config.json.example
```

Note: `node_modules/` is already gitignored and must NOT be moved with git mv — it will remain at root and can be deleted manually if desired (it is irrelevant to git history).

Step 4 — Stage the `.gitignore` change:
```
git add .gitignore
```

Step 5 — Commit all staged changes:
```
git commit -m "feat(49): move plugin source into plugins/ligamen/ (history-preserving)"
```
  </action>
  <verify>
    <automated>
# All plugin directories must be gone from root
test ! -d /Users/ravichillerega/sources/ligamen/commands && \
test ! -d /Users/ravichillerega/sources/ligamen/hooks && \
test ! -d /Users/ravichillerega/sources/ligamen/scripts && \
test ! -d /Users/ravichillerega/sources/ligamen/worker && \
test ! -d /Users/ravichillerega/sources/ligamen/lib && \
test ! -d /Users/ravichillerega/sources/ligamen/skills && \
test ! -d /Users/ravichillerega/sources/ligamen/.claude-plugin && \
test ! -f /Users/ravichillerega/sources/ligamen/package.json && \
test ! -f /Users/ravichillerega/sources/ligamen/package-lock.json && \
test ! -f /Users/ravichillerega/sources/ligamen/ligamen.config.json.example && \
echo "ROOT CLEAN: pass" || echo "ROOT CLEAN: FAIL"

# All plugin directories must exist in plugins/ligamen/
test -d /Users/ravichillerega/sources/ligamen/plugins/ligamen/commands && \
test -d /Users/ravichillerega/sources/ligamen/plugins/ligamen/hooks && \
test -d /Users/ravichillerega/sources/ligamen/plugins/ligamen/scripts && \
test -d /Users/ravichillerega/sources/ligamen/plugins/ligamen/worker && \
test -d /Users/ravichillerega/sources/ligamen/plugins/ligamen/lib && \
test -d /Users/ravichillerega/sources/ligamen/plugins/ligamen/skills && \
test -d /Users/ravichillerega/sources/ligamen/plugins/ligamen/.claude-plugin && \
test -f /Users/ravichillerega/sources/ligamen/plugins/ligamen/package.json && \
test -f /Users/ravichillerega/sources/ligamen/plugins/ligamen/package-lock.json && \
test -f /Users/ravichillerega/sources/ligamen/plugins/ligamen/ligamen.config.json.example && \
echo "DEST EXISTS: pass" || echo "DEST EXISTS: FAIL"

# Git history: hooks.json should show as renamed (not deleted+added)
cd /Users/ravichillerega/sources/ligamen && git log --diff-filter=R --summary --oneline -1 | grep -q "plugins/ligamen" && echo "HISTORY PRESERVED: pass" || echo "HISTORY PRESERVED: check manually with: git log --follow plugins/ligamen/hooks/hooks.json"
    </automated>
  </verify>
  <done>
    - Root no longer contains commands/, hooks/, scripts/, worker/, lib/, skills/, .claude-plugin/, package.json, package-lock.json, or ligamen.config.json.example
    - plugins/ligamen/ contains all of the above
    - `git log --follow plugins/ligamen/hooks/hooks.json` shows history going back before this commit
    - Single commit on main with all renames staged
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify directory structure and git history are correct</name>
  <read_first>None — verify what Task 1 produced.</read_first>
  <what-built>
    All plugin source directories moved from repo root into plugins/ligamen/ via git mv. Root now contains only repo-level files. The .gitignore `plugins/` exclusion was removed so git tracks the new directory.
  </what-built>
  <how-to-verify>
    Run these commands from `/Users/ravichillerega/sources/ligamen` and confirm outputs:

    1. Confirm root is clean — should show ONLY: .git, .gitattributes, .gitignore, .gitmodules, .mcp.json, .planning, LICENSE, Makefile, README.md, docs, plugins, tests (and possibly node_modules):
       ```
       ls /Users/ravichillerega/sources/ligamen/
       ```

    2. Confirm plugins/ligamen/ has all plugin source:
       ```
       ls /Users/ravichillerega/sources/ligamen/plugins/ligamen/
       ```
       Expected: commands  hooks  lib  scripts  skills  worker  .claude-plugin  package.json  package-lock.json  ligamen.config.json.example

    3. Confirm git sees renames (not delete+add):
       ```
       cd /Users/ravichillerega/sources/ligamen && git show --stat HEAD | head -20
       ```
       Expected: lines like `commands/cross-impact.md => plugins/ligamen/commands/cross-impact.md`

    4. Confirm history follows a moved file:
       ```
       cd /Users/ravichillerega/sources/ligamen && git log --oneline --follow plugins/ligamen/hooks/hooks.json | head -5
       ```
       Expected: shows commits predating this move.
  </how-to-verify>
  <acceptance_criteria>
    - `ls /` at repo root shows no commands/, hooks/, scripts/, worker/, lib/, skills/, or .claude-plugin/ directories
    - `ls plugins/ligamen/` shows all 10 expected items
    - `git show --stat HEAD` contains `=>` rename notation (not separate delete + create entries)
    - `git log --follow plugins/ligamen/hooks/hooks.json` returns more than 1 commit
  </acceptance_criteria>
  <resume-signal>Type "approved" if structure and history look correct, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
After Task 1 completes, run the automated verification block in its `<verify>` section. All three checks must print "pass". After Task 2, human confirms the git log follows history across the rename.
</verification>

<success_criteria>
- plugins/ligamen/ exists and contains: commands/, hooks/, scripts/, worker/, lib/, skills/, .claude-plugin/, package.json, package-lock.json, ligamen.config.json.example
- Root contains only repo-level files: README.md, LICENSE, Makefile, docs/, tests/, .planning/, .mcp.json, dotfiles
- Git history is preserved — `git log --follow` on any moved file shows commits predating this restructure
- .gitignore no longer excludes `plugins/` so the new directory is tracked
- Single clean commit on main branch
</success_criteria>

<output>
After completion, create `.planning/phases/49-directory-restructure/49-01-SUMMARY.md` with:
- What was moved (full list of git mv commands executed)
- Final directory structure (output of `ls plugins/ligamen/`)
- Git commit hash
- Any issues encountered and how they were resolved
</output>

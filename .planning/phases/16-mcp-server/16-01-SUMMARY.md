---
phase: 16-mcp-server
plan: 01
subsystem: infra
tags: [mcp, sqlite, better-sqlite3, modelcontextprotocol, stdio, node, fastify, zod]

# Dependency graph
requires:
  - phase: 14-storage-foundation
    provides: SQLite impact-map.db schema and query engine consumed by openDb() helper
provides:
  - stdio MCP server entry point (worker/mcp-server.js) with McpServer + StdioServerTransport wired up
  - openDb() helper function for read-only SQLite access used by Plan 02 tools
  - .mcp.json Claude Code auto-discovery registration for allclear-impact server
  - Full Node.js dependency set (better-sqlite3, MCP SDK, zod, fastify, @fastify/static, @fastify/cors, chromadb)
affects:
  - 16-02 (adds MCP tools importing openDb from mcp-server.js)
  - 16-03 (CI lint check and bats test suite)
  - 17-http-server-web-ui (fastify/static/cors deps already installed)
  - 15-worker-lifecycle (package.json now has type:module and engines set)

# Tech tracking
tech-stack:
  added:
    - "@modelcontextprotocol/sdk ^1.27.1 — McpServer + StdioServerTransport"
    - "better-sqlite3 ^12.8.0 — read-only SQLite with WAL + FTS5"
    - "zod ^3.25.0 — MCP SDK peer dependency"
    - "fastify ^5.8.2 — HTTP server (Phase 17)"
    - "@fastify/static ^8.0.0 — D3 UI assets"
    - "@fastify/cors ^10.0.0 — CORS headers"
    - "chromadb ^3.3.3 — optional vector DB client"
  patterns:
    - "All MCP server logging via console.error — stdout belongs exclusively to MCP stdio protocol"
    - "DB path resolved from ALLCLEAR_DB_PATH env with fallback to .allclear/impact-map.db relative to CWD"
    - "openDb() returns null (not throws) when DB file absent — tools must handle null gracefully"
    - "SIGTERM handler calls process.exit(0) for clean shutdown"

key-files:
  created:
    - worker/mcp-server.js
    - .mcp.json
    - worker/.gitkeep
    - package-lock.json
  modified:
    - package.json

key-decisions:
  - "type:module added to package.json — all worker files use ESM import syntax"
  - "engines node>=20.0.0 enforced — required for better-sqlite3 12.x and fastify 5.x"
  - "Fastify and related deps added in this plan to avoid a second npm install in Phase 17"
  - "openDb() exported as named export so Plan 02 tools can import it without re-opening the DB"
  - ".mcp.json placed at plugin root (next to package.json) not inside .claude-plugin/"

patterns-established:
  - "Zero console.log in mcp-server.js — enforced rule for all future MCP server code"
  - "ALLCLEAR_DB_PATH env var is intentionally relative in .mcp.json — resolves to user project CWD"

requirements-completed: [MCPS-01, MCPS-07, MCPS-08]

# Metrics
duration: 2min
completed: 2026-03-15
---

# Phase 16 Plan 01: MCP Server Summary

**stdio MCP server skeleton using McpServer + StdioServerTransport with openDb() helper, .mcp.json Claude Code registration, and all worker Node.js dependencies installed**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-15T16:06:01Z
- **Completed:** 2026-03-15T16:08:00Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Created worker/mcp-server.js: imports MCP SDK, resolves DB path from env, exports openDb() helper, zero console.log calls, SIGTERM handler
- Created .mcp.json at plugin root registering allclear-impact as stdio MCP server for Claude Code auto-discovery
- Updated package.json with type:module, engines node>=20, and all 7 worker dependencies; npm install generated package-lock.json

## Task Commits

Each task was committed atomically:

1. **Task 1: Add worker dependencies to package.json** - `19df43e` (chore)
2. **Task 2: Create worker/mcp-server.js skeleton and .mcp.json registration** - `0995446` (feat)

**Plan metadata:** (docs commit — see below)

## Files Created/Modified
- `worker/mcp-server.js` - stdio MCP server entry point: McpServer, StdioServerTransport, openDb() helper
- `.mcp.json` - Claude Code plugin registration for allclear-impact MCP server
- `package.json` - Added type:module, engines, and all worker dependencies
- `package-lock.json` - Generated by npm install
- `worker/.gitkeep` - Placeholder to keep worker/ directory in git

## Decisions Made
- Placed .mcp.json at plugin root (next to package.json), not inside .claude-plugin/, matching Claude Code convention
- Added fastify and related HTTP deps now to prevent a second npm install when Phase 17 executes
- openDb() returns null instead of throwing when the DB file is absent — aligns with the "empty results not errors" requirement in Phase 16 success criteria

## Deviations from Plan

None - plan executed exactly as written.

npm auto-added `@chroma-core/default-embed` as an optionalDependency when installing chromadb — this is expected npm behavior for peer dependencies and does not affect the plan.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plan 02 (16-02) can now import openDb from worker/mcp-server.js and register the five MCP tools
- All npm dependencies are installed and available
- .mcp.json is in place — Claude Code will discover the server once tools are registered in Plan 02

---
*Phase: 16-mcp-server*
*Completed: 2026-03-15*

## Self-Check: PASSED

- worker/mcp-server.js: FOUND
- .mcp.json: FOUND
- package.json: FOUND
- 16-01-SUMMARY.md: FOUND
- Commit 19df43e (chore - package.json deps): FOUND
- Commit 0995446 (feat - mcp-server.js + .mcp.json): FOUND

---
phase: 111-quality-score-and-audit-trail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/arcanon/worker/db/migrations/014_scan_versions_quality_score.js
  - plugins/arcanon/worker/db/migrations/015_enrichment_log.js
  - plugins/arcanon/worker/db/migration-014.test.js
  - plugins/arcanon/worker/db/migration-015.test.js
autonomous: true
requirements:
  - TRUST-05
  - TRUST-06
  - TRUST-14

must_haves:
  truths:
    - "Migration 014 adds nullable `scan_versions.quality_score REAL` column"
    - "Migration 015 creates `enrichment_log` table with FK to scan_versions"
    - "Both migrations are idempotent — re-running on an applied DB is a no-op"
    - "Migration 015 creates `idx_enrichment_log_scan_version_id` and `idx_enrichment_log_enricher` indexes"
    - "FK constraint `enrichment_log.scan_version_id REFERENCES scan_versions(id) ON DELETE CASCADE` is enforced"
  artifacts:
    - path: "plugins/arcanon/worker/db/migrations/014_scan_versions_quality_score.js"
      provides: "Migration 014 module — exports `version = 14` and `up(db)`"
      contains: "ALTER TABLE scan_versions ADD COLUMN quality_score REAL"
    - path: "plugins/arcanon/worker/db/migrations/015_enrichment_log.js"
      provides: "Migration 015 module — exports `version = 15` and `up(db)`"
      contains: "CREATE TABLE IF NOT EXISTS enrichment_log"
    - path: "plugins/arcanon/worker/db/migration-014.test.js"
      provides: "Idempotence + schema verification for migration 014"
    - path: "plugins/arcanon/worker/db/migration-015.test.js"
      provides: "Idempotence + schema verification for migration 015"
  key_links:
    - from: "migration 015"
      to: "scan_versions(id)"
      via: "FOREIGN KEY ... ON DELETE CASCADE"
      pattern: "REFERENCES scan_versions\\(id\\) ON DELETE CASCADE"
    - from: "migration 014"
      to: "PRAGMA table_info(scan_versions) idempotence guard"
      via: "hasCol() check before ALTER TABLE"
      pattern: "PRAGMA table_info"
---

<objective>
Land migrations 014 and 015 — schema only, no application wiring. After this plan,
`scan_versions.quality_score REAL` (nullable) exists and `enrichment_log` table
exists. Both migrations are idempotent and tested.

Purpose: Schema must be in place before Plan 111-02 (quality score wiring) and
Plan 111-03 (enrichment log wiring) can run their try/prepare code paths against
a real column / table.

Output: Two new migration files + two test files. No production code changes
outside the migrations directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-quality-score-and-audit-trail/111-CONTEXT.md

# Existing migration templates (read in full before writing)
@plugins/arcanon/worker/db/migrations/010_service_dependencies.js
@plugins/arcanon/worker/db/migrations/011_services_boundary_entry.js

<interfaces>
<!-- Migration module contract — every migration in this directory exports the same shape -->
<!-- Pattern extracted from migrations 010, 011 (read above) -->

```
// migration module contract
export const version = N;          // integer, sequential
export function up(db) {           // db = better-sqlite3 Database instance
  // idempotent schema mutation
}
```

<!-- Idempotence patterns — choose based on what the migration does -->

<!-- Pattern A: ALTER TABLE ADD COLUMN (use for migration 014) -->
```
const hasCol = (table, col) =>
  db.prepare("PRAGMA table_info(" + table + ")").all().some((c) => c.name === col);

if (!hasCol("scan_versions", "quality_score")) {
  db.exec("ALTER TABLE scan_versions ADD COLUMN quality_score REAL;");
}
```

<!-- Pattern B: CREATE TABLE / CREATE INDEX (use for migration 015) -->
<!-- IF NOT EXISTS makes these natively idempotent — no PRAGMA guard needed -->
```
db.exec(`
  CREATE TABLE IF NOT EXISTS enrichment_log (...);
  CREATE INDEX IF NOT EXISTS idx_... ON enrichment_log(...);
`);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 014 — scan_versions.quality_score column + test</name>
  <files>
    plugins/arcanon/worker/db/migrations/014_scan_versions_quality_score.js,
    plugins/arcanon/worker/db/migration-014.test.js
  </files>
  <behavior>
    - Test 1 (idempotence): Open in-memory DB, apply migrations 001..013 then 014. Run 014 a second time — must not throw, must not duplicate the column. Verify via PRAGMA table_info that exactly one `quality_score REAL` column exists on `scan_versions`.
    - Test 2 (column shape): After applying 014, the `quality_score` column has type REAL and is nullable (no NOT NULL constraint). Existing rows have `quality_score = NULL`.
    - Test 3 (preserves existing columns): Migration 014 does NOT modify or drop any existing columns on `scan_versions` (id, repo_id, started_at, completed_at remain intact).
    - Test 4 (sequential `version` export): The module exports `version === 14`.
  </behavior>
  <action>
    Create `plugins/arcanon/worker/db/migrations/014_scan_versions_quality_score.js` modeled directly on `011_services_boundary_entry.js`. The module exports `version = 14` and `function up(db)`. The `up` function uses the `hasCol` PRAGMA check pattern (idempotence pattern A above) before issuing the ALTER TABLE statement that adds `quality_score REAL` to `scan_versions`. Add a JSDoc header explaining: "Migration 014 — TRUST-05: adds `scan_versions.quality_score REAL` (nullable). Existing rows pick up NULL; new scans populate via `endScan()` in Plan 111-02. Idempotent via PRAGMA table_info check."

    Create `plugins/arcanon/worker/db/migration-014.test.js` using the existing node test runner (`import { test } from 'node:test'; import assert from 'node:assert'`). Construct an in-memory `better-sqlite3` DB (`new Database(':memory:')`), apply all migrations 001–013 in order, then apply 014, run the four tests above. To apply prior migrations, use the existing migrations runner — locate it (likely at `plugins/arcanon/worker/db/migrations.js` or `worker/db/run-migrations.js`) during execution. If no runner exists, dynamically import each migration file and call `up(db)` in numeric order.

    Verify column with: `db.prepare("PRAGMA table_info(scan_versions)").all().filter(c => c.name === 'quality_score')` — must have length 1, type 'REAL', notnull 0.
  </action>
  <verify>
    <automated>cd plugins/arcanon && node --test worker/db/migration-014.test.js</automated>
  </verify>
  <done>
    Migration 014 file exists, exports `version = 14` and `up(db)`. Test file passes 4 cases (idempotence, column shape, preservation, version export). Re-running migration on a DB that already has the column is a no-op.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 015 — enrichment_log table + test</name>
  <files>
    plugins/arcanon/worker/db/migrations/015_enrichment_log.js,
    plugins/arcanon/worker/db/migration-015.test.js
  </files>
  <behavior>
    - Test 1 (idempotence): Apply migrations 001..014 then 015. Run 015 a second time — must not throw. PRAGMA table_info still returns one `enrichment_log` table (not duplicated).
    - Test 2 (column schema): After applying 015, `PRAGMA table_info(enrichment_log)` returns rows for: id (INTEGER, PK), scan_version_id (INTEGER, NOT NULL), enricher (TEXT, NOT NULL), target_kind (TEXT, NOT NULL), target_id (INTEGER, NOT NULL), field (TEXT, NOT NULL), from_value (TEXT, nullable), to_value (TEXT, nullable), reason (TEXT, nullable), created_at (TEXT, NOT NULL with default).
    - Test 3 (CHECK constraint on target_kind): Insert with `target_kind = 'service'` succeeds; insert with `target_kind = 'connection'` succeeds; insert with `target_kind = 'invalid'` throws SqliteError.
    - Test 4 (FK to scan_versions with CASCADE): Enable foreign key enforcement (`PRAGMA foreign_keys = ON`). Insert a scan_versions row, insert an enrichment_log row referencing it. DELETE the scan_versions row — the enrichment_log row is also deleted (CASCADE).
    - Test 5 (indexes exist): `SELECT name FROM sqlite_master WHERE type = 'index' AND tbl_name = 'enrichment_log'` returns at least `idx_enrichment_log_scan_version_id` and `idx_enrichment_log_enricher`.
    - Test 6 (default created_at): INSERT without specifying created_at populates it with current datetime.
  </behavior>
  <action>
    Create `plugins/arcanon/worker/db/migrations/015_enrichment_log.js` modeled on `010_service_dependencies.js` (pattern B — `CREATE TABLE IF NOT EXISTS` + `CREATE INDEX IF NOT EXISTS`). The module exports `version = 15` and `function up(db)`. Use a single `db.exec` call (the better-sqlite3 method, not shell) with the table + index DDL.

    Schema (per orchestrator instruction):
    ```
    CREATE TABLE IF NOT EXISTS enrichment_log (
      id              INTEGER PRIMARY KEY AUTOINCREMENT,
      scan_version_id INTEGER NOT NULL REFERENCES scan_versions(id) ON DELETE CASCADE,
      enricher        TEXT    NOT NULL,
      target_kind     TEXT    NOT NULL CHECK(target_kind IN ('service', 'connection')),
      target_id       INTEGER NOT NULL,
      field           TEXT    NOT NULL,
      from_value      TEXT,
      to_value        TEXT,
      reason          TEXT,
      created_at      TEXT    NOT NULL DEFAULT (datetime('now'))
    );

    CREATE INDEX IF NOT EXISTS idx_enrichment_log_scan_version_id
      ON enrichment_log(scan_version_id);

    CREATE INDEX IF NOT EXISTS idx_enrichment_log_enricher
      ON enrichment_log(enricher);
    ```

    Add JSDoc header: "Migration 015 — TRUST-06: creates `enrichment_log` table for post-scan reconciliation audit trail. Each row records a field change (e.g., crossing reclassified external → cross-service). Indexed on scan_version_id (for the `impact_audit_log` MCP tool) and enricher (for filterable queries). FK with ON DELETE CASCADE — when a scan_versions row is deleted, its audit rows go with it. Idempotent via IF NOT EXISTS."

    Create `plugins/arcanon/worker/db/migration-015.test.js`. Same in-memory DB pattern as Task 1, applying migrations 001–014 first, then 015. Run the six tests. Note: SQLite's `PRAGMA foreign_keys` defaults OFF in better-sqlite3 — explicitly enable it before Test 4. The CHECK constraint test (Test 3) must use `assert.throws` matching a SqliteError with message containing 'CHECK' or 'constraint'.

    For Test 4 (FK CASCADE): after enabling FKs, INSERT a `scan_versions` row using the existing prepared SQL pattern (need a repos row first — INSERT INTO repos with a unique path/name, then INSERT INTO scan_versions with that repo_id and an ISO timestamp). Insert an enrichment_log row with `scan_version_id = <new id>`. DELETE the scan_versions row. Assert `SELECT COUNT(*) FROM enrichment_log` returns 0.
  </action>
  <verify>
    <automated>cd plugins/arcanon && node --test worker/db/migration-015.test.js</automated>
  </verify>
  <done>
    Migration 015 file exists, exports `version = 15` and `up(db)`. Test file passes all 6 cases. Re-running migration is a no-op. CHECK constraint enforced. FK CASCADE works. Indexes present.
  </done>
</task>

</tasks>

<verification>
Both migrations land cleanly:

```
cd plugins/arcanon
node --test worker/db/migration-014.test.js worker/db/migration-015.test.js
```

Sanity check: a fresh DB run through all migrations 001–015 produces a schema where:
- `scan_versions.quality_score REAL` is present and nullable
- `enrichment_log` table exists with the 10 columns, 2 indexes, FK to scan_versions, CHECK on target_kind

No other code paths in the repo reference `quality_score` or `enrichment_log` yet — Plans 111-02 and 111-03 wire them in.
</verification>

<success_criteria>
- [ ] `worker/db/migrations/014_scan_versions_quality_score.js` exists, idempotent ALTER pattern
- [ ] `worker/db/migrations/015_enrichment_log.js` exists, CREATE IF NOT EXISTS pattern
- [ ] `migration-014.test.js` passes all 4 cases
- [ ] `migration-015.test.js` passes all 6 cases (incl. CHECK + FK CASCADE)
- [ ] Both migrations re-runnable without error (idempotent)
- [ ] PRAGMA table_info confirms exact column shapes
- [ ] No changes outside `worker/db/migrations/` and the two test files
</success_criteria>

<output>
After completion, create `.planning/phases/111-quality-score-and-audit-trail/111-01-SUMMARY.md` with:
- Files created (4)
- PRAGMA dumps for `scan_versions` and `enrichment_log` after migration applied
- Confirmation that 001–015 migrate cleanly in sequence
- Any deviation from the schema in this plan (none expected)
</output>

---
phase: 40-environment-paths
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - worker/index.js
  - worker/db/pool.js
  - worker/db/database.js
  - worker/mcp/server.js
  - worker/server/chroma.js
autonomous: true
requirements:
  - ENV-01
  - ENV-02

must_haves:
  truths:
    - "Setting process.env.LIGAMEN_DATA_DIR controls the worker data directory; ALLCLEAR_DATA_DIR has no effect"
    - "Setting process.env.LIGAMEN_WORKER_PORT controls the worker port; ALLCLEAR_WORKER_PORT has no effect"
    - "Setting process.env.LIGAMEN_CHROMA_MODE enables ChromaDB; ALLCLEAR_CHROMA_MODE has no effect"
    - "The default DB directory path resolved by pool.js and database.js is ~/.ligamen/projects/<hash>/; no ~/.allclear/ path is constructed"
    - "The MCP server reads LIGAMEN_DATA_DIR and falls back to ~/.ligamen; no ALLCLEAR_ variable is read"
    - "Settings keys read from settings.json use LIGAMEN_* names (LIGAMEN_LOG_LEVEL, LIGAMEN_WORKER_PORT, LIGAMEN_CHROMA_*)"
  artifacts:
    - path: "worker/index.js"
      provides: "LIGAMEN_DATA_DIR env var, LIGAMEN_LOG_LEVEL from settings, LIGAMEN_WORKER_PORT from settings, LIGAMEN_CHROMA_MODE check"
    - path: "worker/db/pool.js"
      provides: "LIGAMEN_DATA_DIR env var, ~/.ligamen default"
    - path: "worker/db/database.js"
      provides: "~/.ligamen/projects/<hash>/ path in projectHashDir()"
    - path: "worker/mcp/server.js"
      provides: "LIGAMEN_DATA_DIR env var, LIGAMEN_LOG_LEVEL from settings, LIGAMEN_DB_PATH, LIGAMEN_PROJECT_ROOT"
    - path: "worker/server/chroma.js"
      provides: "LIGAMEN_CHROMA_MODE, LIGAMEN_CHROMA_HOST, LIGAMEN_CHROMA_PORT, LIGAMEN_CHROMA_SSL, LIGAMEN_CHROMA_API_KEY, LIGAMEN_CHROMA_TENANT, LIGAMEN_CHROMA_DATABASE"
  key_links:
    - from: "worker/index.js"
      to: "~/.ligamen"
      via: "process.env.LIGAMEN_DATA_DIR || path.join(os.homedir(), '.ligamen')"
      pattern: "LIGAMEN_DATA_DIR"
    - from: "worker/db/pool.js"
      to: "~/.ligamen"
      via: "process.env.LIGAMEN_DATA_DIR || path.join(os.homedir(), '.ligamen')"
      pattern: "LIGAMEN_DATA_DIR"
    - from: "worker/db/database.js"
      to: "~/.ligamen/projects/<hash>"
      via: "path.join(os.homedir(), '.ligamen', 'projects', hash)"
      pattern: "\\.ligamen"
    - from: "worker/server/chroma.js"
      to: "ChromaDB client init"
      via: "settings.LIGAMEN_CHROMA_MODE gate"
      pattern: "LIGAMEN_CHROMA_MODE"
---

<objective>
Rename all ALLCLEAR_* environment variable references and ~/.allclear path strings in the five production JavaScript worker files to their LIGAMEN_* equivalents.

Purpose: After this plan, no JavaScript worker file reads process.env.ALLCLEAR_* or constructs ~/.allclear paths. All runtime behavior is controlled via LIGAMEN_* variables and ~/.ligamen paths.
Output: Five updated JS files with complete ALLCLEAR_ → LIGAMEN_ rename and ".allclear" → ".ligamen" path updates.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename ALLCLEAR_* vars and paths in worker/index.js and worker/db/</name>
  <files>worker/index.js, worker/db/pool.js, worker/db/database.js</files>
  <read_first>
    - worker/index.js (read every line before editing)
    - worker/db/pool.js (read every line before editing)
    - worker/db/database.js (read every line before editing)
  </read_first>
  <action>
Apply exact renames to worker/index.js:

1. `process.env.ALLCLEAR_DATA_DIR || path.join(os.homedir(), ".allclear")` → `process.env.LIGAMEN_DATA_DIR || path.join(os.homedir(), ".ligamen")`
2. Comment `// 2. Read settings.json for ALLCLEAR_LOG_LEVEL and port override` → `// 2. Read settings.json for LIGAMEN_LOG_LEVEL and port override`
3. `if (allSettings.ALLCLEAR_LOG_LEVEL) logLevel = allSettings.ALLCLEAR_LOG_LEVEL;` → `if (allSettings.LIGAMEN_LOG_LEVEL) logLevel = allSettings.LIGAMEN_LOG_LEVEL;`
4. `if (allSettings.ALLCLEAR_WORKER_PORT)` → `if (allSettings.LIGAMEN_WORKER_PORT)`
5. `port = parseInt(allSettings.ALLCLEAR_WORKER_PORT, 10);` → `port = parseInt(allSettings.LIGAMEN_WORKER_PORT, 10);`
6. `if (allSettings.ALLCLEAR_CHROMA_MODE) {` → `if (allSettings.LIGAMEN_CHROMA_MODE) {`

Apply exact renames to worker/db/pool.js:

1. `process.env.ALLCLEAR_DATA_DIR || path.join(os.homedir(), ".allclear")` → `process.env.LIGAMEN_DATA_DIR || path.join(os.homedir(), ".ligamen")`
2. Comment `* Scans ~/.allclear/projects/ for impact-map.db files.` → `* Scans ~/.ligamen/projects/ for impact-map.db files.`

Apply exact renames to worker/db/database.js:

1. Comment `* DB path: ~/.allclear/projects/<sha256(projectRoot).slice(0,12)>/impact-map.db` → `* DB path: ~/.ligamen/projects/<sha256(projectRoot).slice(0,12)>/impact-map.db`
2. `return path.join(os.homedir(), ".allclear", "projects", hash);` → `return path.join(os.homedir(), ".ligamen", "projects", hash);`

NOTE: Do NOT rename variables in *.test.js files — that is Phase 43 (Tests).
NOTE: Do NOT rename comment/header branding strings like "AllClear v2.0" in database.js header — that is Phase 42 (Source Code).
  </action>
  <verify>
    <automated>grep -n "ALLCLEAR_\|\.allclear" /Users/ravichillerega/sources/allclear/worker/index.js /Users/ravichillerega/sources/allclear/worker/db/pool.js /Users/ravichillerega/sources/allclear/worker/db/database.js; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
- `grep "ALLCLEAR_\|\.allclear" worker/index.js` returns zero matches
- `grep "ALLCLEAR_\|\.allclear" worker/db/pool.js` returns zero matches
- `grep "ALLCLEAR_\|\.allclear" worker/db/database.js` returns zero matches (excluding comment in file header about "AllClear v2.0" which is branding, not env vars)
- `grep "LIGAMEN_DATA_DIR" worker/index.js` returns 1 match
- `grep "LIGAMEN_DATA_DIR" worker/db/pool.js` returns 1 match
- `grep '\.ligamen' worker/db/database.js` returns at least 2 matches (comment + code)
- `grep "LIGAMEN_LOG_LEVEL\|LIGAMEN_WORKER_PORT\|LIGAMEN_CHROMA_MODE" worker/index.js` returns at least 3 matches
  </acceptance_criteria>
  <done>worker/index.js, worker/db/pool.js, and worker/db/database.js contain no ALLCLEAR_ env var references and no .allclear path strings in production code.</done>
</task>

<task type="auto">
  <name>Task 2: Rename ALLCLEAR_* vars in worker/mcp/server.js and worker/server/chroma.js</name>
  <files>worker/mcp/server.js, worker/server/chroma.js</files>
  <read_first>
    - worker/mcp/server.js (read every line before editing)
    - worker/server/chroma.js (read every line before editing)
  </read_first>
  <action>
Apply exact renames to worker/mcp/server.js:

1. `process.env.ALLCLEAR_DATA_DIR || path.join(os.homedir(), ".allclear")` → `process.env.LIGAMEN_DATA_DIR || path.join(os.homedir(), ".ligamen")`
2. `if (_settings.ALLCLEAR_LOG_LEVEL) _mcpLogLevel = _settings.ALLCLEAR_LOG_LEVEL;` → `if (_settings.LIGAMEN_LOG_LEVEL) _mcpLogLevel = _settings.LIGAMEN_LOG_LEVEL;`
3. Comment `* Resolve the per-project DB path: ~/.allclear/projects/<hash>/impact-map.db` → `* Resolve the per-project DB path: ~/.ligamen/projects/<hash>/impact-map.db`
4. `process.env.ALLCLEAR_DB_PATH ||` → `process.env.LIGAMEN_DB_PATH ||`
5. `resolveDbPath(process.env.ALLCLEAR_PROJECT_ROOT || process.cwd());` → `resolveDbPath(process.env.LIGAMEN_PROJECT_ROOT || process.cwd());`
6. `const root = process.env.ALLCLEAR_PROJECT_ROOT || process.cwd();` → `const root = process.env.LIGAMEN_PROJECT_ROOT || process.cwd();`
7. All tool description strings referencing `ALLCLEAR_PROJECT_ROOT` → `LIGAMEN_PROJECT_ROOT` (there are 4 occurrences in tool input schema description strings, around lines 551, 587, 623, 657)

Apply exact renames to worker/server/chroma.js:

1. JSDoc comment `* If ALLCLEAR_CHROMA_MODE is empty/falsy` → `* If LIGAMEN_CHROMA_MODE is empty/falsy`
2. JSDoc `* @param {string} [settings.ALLCLEAR_CHROMA_MODE]` → `* @param {string} [settings.LIGAMEN_CHROMA_MODE]`
3. JSDoc `* @param {string} [settings.ALLCLEAR_CHROMA_HOST]` → `* @param {string} [settings.LIGAMEN_CHROMA_HOST]`
4. JSDoc `* @param {string} [settings.ALLCLEAR_CHROMA_PORT]` → `* @param {string} [settings.LIGAMEN_CHROMA_PORT]`
5. JSDoc `* @param {string} [settings.ALLCLEAR_CHROMA_SSL]` → `* @param {string} [settings.LIGAMEN_CHROMA_SSL]`
6. `if (!settings.ALLCLEAR_CHROMA_MODE) {` → `if (!settings.LIGAMEN_CHROMA_MODE) {`
7. `const host = settings.ALLCLEAR_CHROMA_HOST || "localhost";` → `const host = settings.LIGAMEN_CHROMA_HOST || "localhost";`
8. `const port = parseInt(settings.ALLCLEAR_CHROMA_PORT || "8000", 10);` → `const port = parseInt(settings.LIGAMEN_CHROMA_PORT || "8000", 10);`
9. `const ssl = settings.ALLCLEAR_CHROMA_SSL === "true";` → `const ssl = settings.LIGAMEN_CHROMA_SSL === "true";`
10. `const apiKey = settings.ALLCLEAR_CHROMA_API_KEY || "";` → `const apiKey = settings.LIGAMEN_CHROMA_API_KEY || "";`
11. `const tenant = settings.ALLCLEAR_CHROMA_TENANT || "default_tenant";` → `const tenant = settings.LIGAMEN_CHROMA_TENANT || "default_tenant";`
12. `const database = settings.ALLCLEAR_CHROMA_DATABASE || "default_database";` → `const database = settings.LIGAMEN_CHROMA_DATABASE || "default_database";`

NOTE: Do NOT rename ALLCLEAR_ references inside *.test.js files — that is Phase 43 (Tests).
NOTE: Do NOT rename comment/header branding strings like "AllClear" in file/module headers — that is Phase 42 (Source Code).
  </action>
  <verify>
    <automated>grep -n "ALLCLEAR_\|\.allclear" /Users/ravichillerega/sources/allclear/worker/mcp/server.js /Users/ravichillerega/sources/allclear/worker/server/chroma.js; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
- `grep "ALLCLEAR_\|\.allclear" worker/mcp/server.js` returns zero matches
- `grep "ALLCLEAR_\|\.allclear" worker/server/chroma.js` returns zero matches
- `grep "LIGAMEN_DATA_DIR\|\.ligamen" worker/mcp/server.js` returns at least 2 matches
- `grep "LIGAMEN_DB_PATH\|LIGAMEN_PROJECT_ROOT" worker/mcp/server.js` returns at least 3 matches
- `grep "LIGAMEN_CHROMA_MODE\|LIGAMEN_CHROMA_HOST\|LIGAMEN_CHROMA_PORT" worker/server/chroma.js` returns at least 4 matches
  </acceptance_criteria>
  <done>worker/mcp/server.js and worker/server/chroma.js contain no ALLCLEAR_ env var references and no .allclear path strings in production code.</done>
</task>

</tasks>

<verification>
After both tasks:

```bash
# Should print nothing (zero ALLCLEAR_ references remaining in production JS files)
grep -rn "ALLCLEAR_\|\.allclear" \
  /Users/ravichillerega/sources/allclear/worker/index.js \
  /Users/ravichillerega/sources/allclear/worker/db/pool.js \
  /Users/ravichillerega/sources/allclear/worker/db/database.js \
  /Users/ravichillerega/sources/allclear/worker/mcp/server.js \
  /Users/ravichillerega/sources/allclear/worker/server/chroma.js
```
</verification>

<success_criteria>
Zero grep matches for `ALLCLEAR_` or `\.allclear` in all five production JavaScript files. LIGAMEN_* variable names and ~/.ligamen paths are present and correct throughout.
</success_criteria>

<output>
After completion, create `.planning/phases/40-environment-paths/40-02-SUMMARY.md`
</output>

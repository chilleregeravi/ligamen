---
phase: 40-environment-paths
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/config.sh
  - lib/worker-client.sh
  - scripts/worker-start.sh
  - scripts/worker-stop.sh
  - scripts/format.sh
  - scripts/lint.sh
  - scripts/file-guard.sh
  - scripts/session-start.sh
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-03

must_haves:
  truths:
    - "Exporting LIGAMEN_DATA_DIR controls the worker data directory in shell scripts; ALLCLEAR_DATA_DIR has no effect"
    - "Exporting LIGAMEN_WORKER_PORT controls the worker port in shell scripts; ALLCLEAR_WORKER_PORT has no effect"
    - "Exporting LIGAMEN_DISABLE_FORMAT=1 disables auto-format; ALLCLEAR_DISABLE_FORMAT has no effect"
    - "Exporting LIGAMEN_DISABLE_LINT=1 disables auto-lint; ALLCLEAR_DISABLE_LINT has no effect"
    - "Exporting LIGAMEN_DISABLE_GUARD=1 disables file-guard; ALLCLEAR_DISABLE_GUARD has no effect"
    - "Exporting LIGAMEN_DISABLE_SESSION_START=1 disables session-start hook; ALLCLEAR_DISABLE_SESSION_START has no effect"
    - "Temp files created by lint.sh use /tmp/ligamen_clippy_* pattern; no /tmp/allclear_clippy_* files are created"
    - "Temp files created by session-start.sh use /tmp/ligamen_session_* pattern; no /tmp/allclear_session_* files are created"
    - "The default data directory resolved in all shell scripts is ~/.ligamen; ~/.allclear is never referenced"
  artifacts:
    - path: "lib/config.sh"
      provides: "LIGAMEN_CONFIG_FILE, LIGAMEN_CONFIG_LINKED_REPOS, _LIGAMEN_CONFIG_LOADED guard"
    - path: "lib/worker-client.sh"
      provides: "LIGAMEN_DATA_DIR default ~/.ligamen in all 4 functions"
    - path: "scripts/worker-start.sh"
      provides: "LIGAMEN_DATA_DIR, LIGAMEN_WORKER_PORT, ~/.ligamen default, ligamen.config.json port key"
    - path: "scripts/worker-stop.sh"
      provides: "LIGAMEN_DATA_DIR, ~/.ligamen default"
    - path: "scripts/format.sh"
      provides: "LIGAMEN_DISABLE_FORMAT check"
    - path: "scripts/lint.sh"
      provides: "LIGAMEN_DISABLE_LINT, LIGAMEN_LINT_THROTTLE, /tmp/ligamen_clippy_* throttle file"
    - path: "scripts/file-guard.sh"
      provides: "LIGAMEN_DISABLE_GUARD, LIGAMEN_EXTRA_BLOCKED"
    - path: "scripts/session-start.sh"
      provides: "LIGAMEN_DISABLE_SESSION_START, /tmp/ligamen_session_* flag file"
  key_links:
    - from: "scripts/worker-start.sh"
      to: "$HOME/.ligamen"
      via: "LIGAMEN_DATA_DIR:-$HOME/.ligamen default"
      pattern: "LIGAMEN_DATA_DIR"
    - from: "lib/worker-client.sh"
      to: "$HOME/.ligamen"
      via: "LIGAMEN_DATA_DIR:-$HOME/.ligamen in worker_running, worker_call, wait_for_worker, worker_status_line"
      pattern: "LIGAMEN_DATA_DIR"
---

<objective>
Rename all ALLCLEAR_* environment variables and allclear path references in the eight shell script files to their LIGAMEN_* equivalents.

Purpose: After this plan, no shell script reads or sets any ALLCLEAR_* variable or references ~/.allclear or /tmp/allclear_* paths. All runtime behavior is controlled via LIGAMEN_* variables and ~/.ligamen paths.
Output: Eight updated shell script files with complete ALLCLEAR_ → LIGAMEN_ rename and ~/.allclear → ~/.ligamen, /tmp/allclear_ → /tmp/ligamen_ path updates.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename ALLCLEAR_* vars and paths in lib/ shell scripts</name>
  <files>lib/config.sh, lib/worker-client.sh</files>
  <read_first>
    - lib/config.sh (read every line before editing)
    - lib/worker-client.sh (read every line before editing)
  </read_first>
  <action>
Apply the following exact renames to lib/config.sh:

1. Line comment "ALLCLEAR_CONFIG_LINKED_REPOS" → "LIGAMEN_CONFIG_LINKED_REPOS"
2. Line comment "ALLCLEAR_CONFIG_FILE" → "LIGAMEN_CONFIG_FILE"
3. `_ALLCLEAR_CONFIG_LOADED` → `_LIGAMEN_CONFIG_LOADED` (both occurrences: the guard check `${_ALLCLEAR_CONFIG_LOADED:-}` and the assignment `_ALLCLEAR_CONFIG_LOADED=1`)
4. `ALLCLEAR_CONFIG_FILE` → `LIGAMEN_CONFIG_FILE` (all occurrences including the variable definition line `ALLCLEAR_CONFIG_FILE="${ALLCLEAR_CONFIG_FILE:-allclear.config.json}"` → `LIGAMEN_CONFIG_FILE="${LIGAMEN_CONFIG_FILE:-ligamen.config.json}"`)
5. `ALLCLEAR_CONFIG_LINKED_REPOS` → `LIGAMEN_CONFIG_LINKED_REPOS` (all occurrences)
6. Comment at bottom: "Do NOT export ALLCLEAR_CONFIG_LINKED_REPOS" → "Do NOT export LIGAMEN_CONFIG_LINKED_REPOS"
7. Update the header comment block: "ALLCLEAR_CONFIG_LINKED_REPOS" → "LIGAMEN_CONFIG_LINKED_REPOS" and "ALLCLEAR_CONFIG_FILE" → "LIGAMEN_CONFIG_FILE"
8. Warning message "allclear: warning: allclear.config.json is malformed" → "ligamen: warning: ligamen.config.json is malformed"
9. The default config filename fallback `allclear.config.json` → `ligamen.config.json` (inside the variable definition)

Apply the following exact renames to lib/worker-client.sh:

1. `ALLCLEAR_DATA_DIR` → `LIGAMEN_DATA_DIR` in all 4 functions:
   - worker_running(): `local data_dir="${ALLCLEAR_DATA_DIR:-$HOME/.allclear}"` → `local data_dir="${LIGAMEN_DATA_DIR:-$HOME/.ligamen}"`
   - worker_call(): `local data_dir="${ALLCLEAR_DATA_DIR:-$HOME/.allclear}"` → `local data_dir="${LIGAMEN_DATA_DIR:-$HOME/.ligamen}"`
   - wait_for_worker(): `local data_dir="${ALLCLEAR_DATA_DIR:-$HOME/.allclear}"` → `local data_dir="${LIGAMEN_DATA_DIR:-$HOME/.ligamen}"`
   - worker_status_line(): `local data_dir="${ALLCLEAR_DATA_DIR:-$HOME/.allclear}"` → `local data_dir="${LIGAMEN_DATA_DIR:-$HOME/.ligamen}"`

NOTE: The header comments in worker-client.sh say "AllClear worker" and "AllClear" — do NOT change those comment strings in this phase. Phase 42 handles comment/header branding. This plan only renames env vars and paths.
  </action>
  <verify>
    <automated>grep -n "ALLCLEAR_\|\.allclear\|allclear_" /Users/ravichillerega/sources/allclear/lib/config.sh /Users/ravichillerega/sources/allclear/lib/worker-client.sh; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
- `grep "ALLCLEAR_\|\.allclear\|allclear_" lib/config.sh` returns zero matches
- `grep "ALLCLEAR_\|\.allclear\|allclear_" lib/worker-client.sh` returns zero matches
- `grep "LIGAMEN_CONFIG_FILE\|LIGAMEN_CONFIG_LINKED_REPOS\|_LIGAMEN_CONFIG_LOADED" lib/config.sh` returns at least 6 matches
- `grep "LIGAMEN_DATA_DIR" lib/worker-client.sh` returns exactly 4 matches (one per function)
- `grep "ligamen.config.json" lib/config.sh` returns 1 match (the default fallback line)
  </acceptance_criteria>
  <done>lib/config.sh and lib/worker-client.sh contain no ALLCLEAR_ variables, no .allclear paths, no allclear_ prefixes. All variables renamed to LIGAMEN_ equivalents with ~/.ligamen defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Rename ALLCLEAR_* vars and paths in scripts/ shell scripts</name>
  <files>scripts/worker-start.sh, scripts/worker-stop.sh, scripts/format.sh, scripts/lint.sh, scripts/file-guard.sh, scripts/session-start.sh</files>
  <read_first>
    - scripts/worker-start.sh (read every line before editing)
    - scripts/worker-stop.sh (read every line before editing)
    - scripts/format.sh (read every line before editing)
    - scripts/lint.sh (read every line before editing)
    - scripts/file-guard.sh (read every line before editing)
    - scripts/session-start.sh (read every line before editing)
  </read_first>
  <action>
Apply exact renames to scripts/worker-start.sh:
1. `DATA_DIR="${ALLCLEAR_DATA_DIR:-$HOME/.allclear}"` → `DATA_DIR="${LIGAMEN_DATA_DIR:-$HOME/.ligamen}"`
2. `if [[ -n "${ALLCLEAR_WORKER_PORT:-}" ]]; then` → `if [[ -n "${LIGAMEN_WORKER_PORT:-}" ]]; then`
3. `PORT="${ALLCLEAR_WORKER_PORT}"` → `PORT="${LIGAMEN_WORKER_PORT}"`
4. Comment `# 2. ~/.allclear/settings.json key "ALLCLEAR_WORKER_PORT"` → `# 2. ~/.ligamen/settings.json key "LIGAMEN_WORKER_PORT"`
5. `_port=$(jq -r '.ALLCLEAR_WORKER_PORT // empty' "${DATA_DIR}/settings.json"` → `_port=$(jq -r '.LIGAMEN_WORKER_PORT // empty' "${DATA_DIR}/settings.json"`
6. Comment `# 3. allclear.config.json in CWD key ."impact-map".port` → `# 3. ligamen.config.json in CWD key ."impact-map".port`
7. `[[ -f "${PWD}/allclear.config.json" ]]` → `[[ -f "${PWD}/ligamen.config.json" ]]`
8. `_port=$(jq -r '.["impact-map"].port // empty' "${PWD}/allclear.config.json"` → `_port=$(jq -r '.["impact-map"].port // empty' "${PWD}/ligamen.config.json"`
9. Inline comment `# Determine data directory (machine-wide: ~/.allclear or override)` → `# Determine data directory (machine-wide: ~/.ligamen or override)`

Apply exact renames to scripts/worker-stop.sh:
1. `DATA_DIR="${ALLCLEAR_DATA_DIR:-$HOME/.allclear}"` → `DATA_DIR="${LIGAMEN_DATA_DIR:-$HOME/.ligamen}"`
2. Inline comment `# Determine data directory (machine-wide: ~/.allclear or override)` → `# Determine data directory (machine-wide: ~/.ligamen or override)`

Apply exact renames to scripts/format.sh:
1. `[[ "${ALLCLEAR_DISABLE_FORMAT:-}" == "1" ]] && exit 0` → `[[ "${LIGAMEN_DISABLE_FORMAT:-}" == "1" ]] && exit 0`

Apply exact renames to scripts/lint.sh:
1. `[[ -n "${ALLCLEAR_DISABLE_LINT:-}" ]] && exit 0` → `[[ -n "${LIGAMEN_DISABLE_LINT:-}" ]] && exit 0`
2. `THROTTLE_FILE="/tmp/allclear_clippy_${THROTTLE_KEY}"` → `THROTTLE_FILE="/tmp/ligamen_clippy_${THROTTLE_KEY}"`
3. `THROTTLE_SECS="${ALLCLEAR_LINT_THROTTLE:-30}"` → `THROTTLE_SECS="${LIGAMEN_LINT_THROTTLE:-30}"`

Apply exact renames to scripts/file-guard.sh:
1. Header comment `#   ALLCLEAR_DISABLE_GUARD=1` → `#   LIGAMEN_DISABLE_GUARD=1`
2. Header comment `#   ALLCLEAR_EXTRA_BLOCKED` → `#   LIGAMEN_EXTRA_BLOCKED`
3. `if [[ "${ALLCLEAR_DISABLE_GUARD:-0}" == "1" ]]; then` → `if [[ "${LIGAMEN_DISABLE_GUARD:-0}" == "1" ]]; then`
4. Comment `# ALLCLEAR_EXTRA_BLOCKED -- user-defined colon-separated patterns (CONF-04)` → `# LIGAMEN_EXTRA_BLOCKED -- user-defined colon-separated patterns (CONF-04)`
5. `if [[ -n "${ALLCLEAR_EXTRA_BLOCKED:-}" ]]; then` → `if [[ -n "${LIGAMEN_EXTRA_BLOCKED:-}" ]]; then`
6. `IFS=':' read -ra _extra_patterns <<< "$ALLCLEAR_EXTRA_BLOCKED"` → `IFS=':' read -ra _extra_patterns <<< "$LIGAMEN_EXTRA_BLOCKED"`
7. Error message `"matches custom block pattern '$_pat' in ALLCLEAR_EXTRA_BLOCKED"` → `"matches custom block pattern '$_pat' in LIGAMEN_EXTRA_BLOCKED"`

Apply exact renames to scripts/session-start.sh:
1. `[[ -n "${ALLCLEAR_DISABLE_SESSION_START:-}" ]] && exit 0` → `[[ -n "${LIGAMEN_DISABLE_SESSION_START:-}" ]] && exit 0`
2. `FLAG_FILE="/tmp/allclear_session_${SESSION_ID}.initialized"` → `FLAG_FILE="/tmp/ligamen_session_${SESSION_ID}.initialized"`
  </action>
  <verify>
    <automated>grep -n "ALLCLEAR_\|\.allclear\|/tmp/allclear" /Users/ravichillerega/sources/allclear/scripts/worker-start.sh /Users/ravichillerega/sources/allclear/scripts/worker-stop.sh /Users/ravichillerega/sources/allclear/scripts/format.sh /Users/ravichillerega/sources/allclear/scripts/lint.sh /Users/ravichillerega/sources/allclear/scripts/file-guard.sh /Users/ravichillerega/sources/allclear/scripts/session-start.sh; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
- `grep "ALLCLEAR_\|\.allclear\|/tmp/allclear" scripts/worker-start.sh` returns zero matches
- `grep "ALLCLEAR_\|\.allclear\|/tmp/allclear" scripts/worker-stop.sh` returns zero matches
- `grep "ALLCLEAR_\|/tmp/allclear" scripts/format.sh` returns zero matches
- `grep "ALLCLEAR_\|/tmp/allclear" scripts/lint.sh` returns zero matches
- `grep "ALLCLEAR_\|/tmp/allclear" scripts/file-guard.sh` returns zero matches
- `grep "ALLCLEAR_\|/tmp/allclear" scripts/session-start.sh` returns zero matches
- `grep "LIGAMEN_DATA_DIR\|\.ligamen" scripts/worker-start.sh` returns at least 3 matches
- `grep "LIGAMEN_WORKER_PORT" scripts/worker-start.sh` returns at least 3 matches
- `grep "ligamen.config.json" scripts/worker-start.sh` returns 2 matches
- `grep "/tmp/ligamen_clippy_" scripts/lint.sh` returns 1 match
- `grep "/tmp/ligamen_session_" scripts/session-start.sh` returns 1 match
  </acceptance_criteria>
  <done>All six scripts/ files contain no ALLCLEAR_ variables, no .allclear paths, no /tmp/allclear_ prefixes. All renamed to LIGAMEN_ equivalents with ~/.ligamen and /tmp/ligamen_ paths.</done>
</task>

</tasks>

<verification>
After both tasks:

```bash
# Should print nothing (zero ALLCLEAR_ references remaining in production shell files)
grep -rn "ALLCLEAR_\|\.allclear\|/tmp/allclear" \
  /Users/ravichillerega/sources/allclear/lib/config.sh \
  /Users/ravichillerega/sources/allclear/lib/worker-client.sh \
  /Users/ravichillerega/sources/allclear/scripts/worker-start.sh \
  /Users/ravichillerega/sources/allclear/scripts/worker-stop.sh \
  /Users/ravichillerega/sources/allclear/scripts/format.sh \
  /Users/ravichillerega/sources/allclear/scripts/lint.sh \
  /Users/ravichillerega/sources/allclear/scripts/file-guard.sh \
  /Users/ravichillerega/sources/allclear/scripts/session-start.sh
```
</verification>

<success_criteria>
Zero grep matches for `ALLCLEAR_`, `\.allclear`, or `/tmp/allclear` in all eight shell files. LIGAMEN_* variables and ~/.ligamen paths are present and correct.
</success_criteria>

<output>
After completion, create `.planning/phases/40-environment-paths/40-01-SUMMARY.md`
</output>

---
phase: 114-read-only-navigability-commands-list-view-doctor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/arcanon/commands/view.md
  - tests/commands-surface.bats
  - plugins/arcanon/CHANGELOG.md
autonomous: true
requirements: [NAV-02]
must_haves:
  truths:
    - "Typing /arcanon:view in a Claude Code session resolves to commands/view.md (not commands/map.md)"
    - "view.md auto-starts the worker if it is not running, then opens the graph UI in the user's default browser"
    - "view.md does NOT add a `view: cmdView` entry to worker/cli/hub.js HANDLERS — there is no Node-side handler"
    - "/arcanon:map view continues to work exactly as it did before (the existing `If view flag` block in map.md is untouched)"
  artifacts:
    - path: plugins/arcanon/commands/view.md
      provides: "Top-level slash-command alias for the graph UI launcher"
      contains: "worker_running || bash"
  key_links:
    - from: plugins/arcanon/commands/view.md
      to: plugins/arcanon/scripts/worker-start.sh
      via: "worker_running || bash worker-start.sh (cloned from map.md:23-31)"
      pattern: "worker-start\\.sh"
    - from: plugins/arcanon/commands/view.md
      to: "browser via xdg-open|open"
      via: "cross-platform open of http://localhost:${PORT}"
      pattern: "xdg-open|open .*localhost"
---

<objective>
Ship `/arcanon:view` — a top-level slash-command alias for the existing graph-UI launcher currently hidden under `/arcanon:map view`. Pure markdown command; no Node handler; no router collision.

Purpose: NAV-02. New users discover the graph UI without needing to know the `view` subcommand. The existing `/arcanon:map view` keystroke remains supported for muscle memory.

Output: One new markdown file (`commands/view.md`), one regression-test addition to `tests/commands-surface.bats` (REPO ROOT — bats convention), one CHANGELOG entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-read-only-navigability-commands-list-view-doctor/114-RESEARCH.md

@plugins/arcanon/commands/map.md
@plugins/arcanon/commands/status.md
@plugins/arcanon/lib/worker-client.sh
@tests/commands-surface.bats

<test_path_convention>
**Bats test files live at the REPO ROOT under `tests/`, NOT under `plugins/arcanon/tests/`.**

`tests/commands-surface.bats` is at the repo root, alongside the other 16+ bats files. Run from repo root: `bats tests/commands-surface.bats`. Only **fixtures** live under `plugins/arcanon/tests/fixtures/` (this plan adds no fixtures).
</test_path_convention>

<dispatch_precedence_finding>
**Pre-flight resolution from RESEARCH §2 — quoted verbatim so a future reader does NOT re-litigate this:**

> Claude Code resolves slash commands by exact filename match against `commands/<name>.md`.
> The "subcommand" `/arcanon:map view` is not a separate dispatched route — it is inline narrative
> inside `map.md` that Claude interprets via `$ARGUMENTS`.
>
> Evidence (RESEARCH §2):
> 1. `commands/map.md:21` reads `## If \`view\` flag: ...` — markdown heading, not a dispatcher.
> 2. There is NO grep hit for `case.*\$ARGUMENTS|switch.*subcommand|\$1.*view` against `map.md`.
> 3. `scripts/hub.sh` does not know about `view`. `worker/cli/hub.js HANDLERS` does not have a `view` key.
> 4. Today, typing `/arcanon:view <anything>` returns "command not found" — there is no `commands/view.md`.
>
> When `commands/view.md` is added, Claude Code resolves `/arcanon:view` to that file directly.
> `commands/map.md` is never consulted for `/arcanon:view`. The user's existing keystroke
> `/arcanon:map view` continues to work (still resolves to `map.md`).

**Implication for this plan: NO double-dispatch guard ships.** There is no router to double-fire — Claude reads exactly one markdown file per slash command invocation. The v0.1.3 audit pre-flight raised the question; the research has resolved it as a non-concern.
</dispatch_precedence_finding>

<background>
This plan is intentionally the smallest of the three Phase 114 plans. The graph-UI launcher block already exists at `commands/map.md:22-32`; cloning it into `commands/view.md` is the entire body of work. No HTTP changes, no hub.js changes, no `worker-client.sh` changes (Plan 114-01 is adding `_arcanon_is_project_dir()` independently — this plan does not depend on it because the graph UI should still launch in non-Arcanon dirs after a fresh `/arcanon:map`).

Per RESEARCH §7 Q4: match `map.md:24-25` exactly — `worker_running || bash worker-start.sh`. The 2-3s cold start is acceptable for a UI-launching command and is the existing UX.
</background>

<reference_block>
<!-- The exact block to clone from map.md:21-31 -->

```markdown
## If `view` flag: Open Graph UI and Exit

```bash
source ${CLAUDE_PLUGIN_ROOT}/lib/worker-client.sh
worker_running || bash ${CLAUDE_PLUGIN_ROOT}/scripts/worker-start.sh
PORT=$(cat ~/.arcanon/worker.port)
# Cross-platform open
if command -v xdg-open &>/dev/null; then xdg-open "http://localhost:${PORT}"
elif command -v open &>/dev/null; then open "http://localhost:${PORT}"
else echo "Open http://localhost:${PORT} in your browser"; fi
```

Print "Graph UI opened" and stop. Do not proceed to scanning.
```
</reference_block>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/view.md by cloning map.md:22-32 verbatim, register in tests/commands-surface.bats, add CHANGELOG entry</name>
  <files>plugins/arcanon/commands/view.md, tests/commands-surface.bats, plugins/arcanon/CHANGELOG.md</files>
  <action>
    Create `plugins/arcanon/commands/view.md`:

    ```markdown
    ---
    description: Open the Arcanon graph UI in your default browser. Top-level alias for /arcanon:map view.
    argument-hint: ""
    allowed-tools: Bash
    ---

    # Arcanon View — Graph UI Launcher

    Open the service dependency graph in your browser. Auto-starts the worker if it is not running.

    ```bash
    source ${CLAUDE_PLUGIN_ROOT}/lib/worker-client.sh
    worker_running || bash ${CLAUDE_PLUGIN_ROOT}/scripts/worker-start.sh
    PORT=$(cat ~/.arcanon/worker.port)
    # Cross-platform open
    if command -v xdg-open &>/dev/null; then xdg-open "http://localhost:${PORT}"
    elif command -v open &>/dev/null; then open "http://localhost:${PORT}"
    else echo "Open http://localhost:${PORT} in your browser"; fi
    ```

    Print "Graph UI opened" and stop.

    ## See also

    - `/arcanon:map` — re-scan repos and rebuild the graph
    - `/arcanon:list` — concise project overview (no UI)
    ```

    Update `tests/commands-surface.bats` (at REPO ROOT):

    1. Per NIT 8: extend the existing iteration list to cover **all current commands** including this plan's `view` plus the pre-existing missing `verify`/`update`. Final iteration list: `map drift impact sync login status export verify update view` (Plan 114-01 will additionally add `list`; Plan 114-03 will additionally add `doctor`. The plans land in the same wave with no merge conflict because each appends a different command name to the same list — coordinate by alphabetizing the final list at integration time.)
    2. Add a regression test (new `@test`) that asserts:
       - `commands/view.md` exists.
       - Frontmatter has `description:` and `allowed-tools: Bash`.
       - Body contains the literal substring `worker-start.sh` (proves the auto-start block is present).
       - Body does NOT contain `bash hub.sh view` (proves no Node-side handler is invoked — there is no `cmdView`).
    3. Add a separate regression test asserting `commands/map.md` STILL contains the literal substring `If \`view\` flag` — this protects the existing `/arcanon:map view` route from accidental removal during this plan or future cleanups.
    4. Add a defensive negative test asserting `worker/cli/hub.js` does NOT contain the literal substring `view: cmdView` — guards against a future contributor adding a Node handler that would create the dispatch ambiguity the v0.1.3 audit warned about.

    Update `plugins/arcanon/CHANGELOG.md`:
    - Under `## [Unreleased]` `### Added`, append: `- **`/arcanon:view` command** (NAV-02). Top-level alias for `/arcanon:map view` — opens the graph UI in your default browser. Auto-starts the worker. The existing `/arcanon:map view` route is preserved for back-compat.`
  </action>
  <verify>
    <automated>bats tests/commands-surface.bats</automated>
  </verify>
  <done>
    - `commands/view.md` exists with cloned worker-start + browser-open block.
    - `tests/commands-surface.bats` (at REPO ROOT) has the four new assertions (file exists, frontmatter shape, no-cmdView negative, map.md `If view flag` preserved) and they all pass.
    - The iteration list includes `view` (and its sibling commands per NIT 8).
    - `worker/cli/hub.js` is unchanged — `git diff plugins/arcanon/worker/cli/hub.js` is empty.
    - `lib/worker-client.sh` is unchanged by this plan — `git diff plugins/arcanon/lib/worker-client.sh` is empty (Plan 114-01 owns that file).
    - CHANGELOG entry is appended.
    - Manual smoke: typing `/arcanon:view` in a Claude Code session triggers the new view.md (not map.md), worker starts if needed, browser opens to `http://localhost:${PORT}`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI -> markdown wrapper | trusted local user |
| markdown wrapper -> worker-start.sh | local sourced script with no user input |
| markdown wrapper -> browser open | URL is `http://localhost:${PORT}` where PORT is read from `~/.arcanon/worker.port` (worker-managed file) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-02-01 | T | view.md PORT read | accept | `~/.arcanon/worker.port` is created and managed by the worker; tampering requires local FS write access (already trusted) |
| T-114-02-02 | E | xdg-open / open | accept | Existing pattern from map.md, no new exposure; URL is hardcoded to localhost |
| T-114-02-03 | I | dispatch precedence | mitigate | Negative regression test asserts `worker/cli/hub.js` has no `view: cmdView` entry — prevents a future contributor from creating a dual-dispatch ambiguity |

**Read-only contract:** No DB writes. No migrations. No new HTTP routes. No new auth surface.
</threat_model>

<verification>
- bats: `bats tests/commands-surface.bats` (run from repo root) — all green, including the four new assertions for view.md.
- Regression: existing 315 bats tests still green (`bats tests/` from repo root).
- Manual smoke: `/arcanon:view` opens the graph UI; `/arcanon:map view` still works.
- `git diff --stat` — only the three files in `files_modified` should appear.
</verification>

<success_criteria>
1. `commands/view.md` exists with frontmatter (`description`, `allowed-tools: Bash`) + the cloned worker-start + browser-open bash block from `map.md:22-32`.
2. `tests/commands-surface.bats` (REPO ROOT) has new tests: view.md exists, frontmatter shape valid, no-cmdView negative, `If view flag` preserved in map.md. The iteration list also includes `view` (and pre-existing missing `verify`/`update` per NIT 8).
3. `worker/cli/hub.js` is untouched (no `cmdView`, no `view:` HANDLERS entry).
4. `/arcanon:map view` continues to work (no regression in map.md's `view` branch).
5. CHANGELOG `[Unreleased] / ### Added` contains the new `/arcanon:view` line.
</success_criteria>

<output>
After completion, create `.planning/phases/114-read-only-navigability-commands-list-view-doctor/114-02-SUMMARY.md` with sections: Goal, Truths Validated, Artifacts Created, Files Modified, Tests Added, Decisions (note: "no double-dispatch guard ships per RESEARCH §2 finding"), Open Items.
</output>

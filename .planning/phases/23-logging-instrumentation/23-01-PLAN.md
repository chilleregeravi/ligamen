---
phase: 23-logging-instrumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - worker/lib/logger.js
autonomous: true
requirements:
  - LOG-INFRA-01
  - LOG-INFRA-02

must_haves:
  truths:
    - "A shared logger module exists that any worker module can import"
    - "Every log line produced by the logger includes a `component` field"
    - "The logger writes structured JSON to ~/.allclear/logs/worker.log and stderr"
  artifacts:
    - path: "worker/lib/logger.js"
      provides: "createLogger factory — returns logger bound to a component tag"
      exports: ["createLogger"]
  key_links:
    - from: "worker/lib/logger.js"
      to: "~/.allclear/logs/worker.log"
      via: "fs.appendFileSync in log()"
      pattern: "appendFileSync.*worker\\.log"
---

<objective>
Extract the structured log() function from worker/index.js into a reusable logger factory module at worker/lib/logger.js. The module exports a single createLogger({ dataDir, port, logLevel, component }) function. Every log line it produces includes a `component` field, enabling Phase 24's log filtering by subsystem.

Purpose: All worker modules share one consistent log format instead of rolling their own console calls. This is the prerequisite for log filtering (Phase 24).
Output: worker/lib/logger.js with exported createLogger factory.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current log() function in worker/index.js (lines 52-67) — this is what we're extracting: -->

```javascript
const LEVELS = { DEBUG: 0, INFO: 1, WARN: 2, ERROR: 3 };

function log(level, msg, extra = {}) {
  if (LEVELS[level] < LEVELS[logLevel]) return;
  const line = JSON.stringify({
    ts: new Date().toISOString(),
    level,
    msg,
    pid: process.pid,
    port,
    ...extra,
  });
  const logFile = path.join(dataDir, "logs", "worker.log");
  fs.appendFileSync(logFile, line + "\n");
  process.stderr.write(line + "\n");
}
```

<!-- Callers in worker/index.js: -->
log("INFO", "ChromaDB connected");
log("INFO", "worker started", { port });
log("INFO", `received ${signal}, shutting down`);
log("INFO", "worker stopped");
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create worker/lib/logger.js with createLogger factory</name>
  <files>worker/lib/logger.js</files>
  <behavior>
    - createLogger({ dataDir, port, logLevel, component }) returns a logger object
    - logger.log("INFO", "msg") writes JSON line: { ts, level, msg, pid, port, component }
    - logger.log("DEBUG", "msg") is suppressed when logLevel is "INFO"
    - logger.log("INFO", "msg", { extra: 1 }) merges extra fields into the JSON line
    - logger.info("msg") is a convenience shorthand for logger.log("INFO", "msg")
    - logger.error("msg") is a convenience shorthand for logger.log("ERROR", "msg")
    - logger.warn("msg") is a convenience shorthand for logger.log("WARN", "msg")
    - logger.debug("msg") is a convenience shorthand for logger.log("DEBUG", "msg")
    - port field is omitted from the JSON line when port is undefined or null
    - JSON line is appended to {dataDir}/logs/worker.log via fs.appendFileSync
    - JSON line is also written to process.stderr
    - LEVELS order: DEBUG=0, INFO=1, WARN=2, ERROR=3
  </behavior>
  <action>
    Create worker/lib/ directory and worker/lib/logger.js.

    The module uses node:fs and node:path (ESM imports). It exports a single named export: createLogger.

    createLogger signature: createLogger({ dataDir, port, logLevel = "INFO", component }) => logger

    The returned logger object has:
      - log(level, msg, extra = {})
      - info(msg, extra = {})
      - warn(msg, extra = {})
      - error(msg, extra = {})
      - debug(msg, extra = {})

    Internal log(level, msg, extra) implementation:
      1. Check LEVELS[level] >= LEVELS[logLevel] — return early if suppressed
      2. Build line object: { ts: new Date().toISOString(), level, msg, pid: process.pid, component, ...extra }
      3. If port is defined and not null, include port in line object (insert before component for readable field ordering)
      4. const line = JSON.stringify(lineObject)
      5. fs.appendFileSync(path.join(dataDir, "logs", "worker.log"), line + "\n")
      6. process.stderr.write(line + "\n")

    Convenience methods simply delegate: info(msg, extra) { this.log("INFO", msg, extra) } etc.

    Do NOT use a class — use a plain object literal returned from createLogger. This keeps it simple and avoids `this` binding issues when destructured.

    No default export. Named export only: export function createLogger(...).
  </action>
  <verify>
    <automated>node --input-type=module &lt;&lt;'EOF'
import { createLogger } from './worker/lib/logger.js';
import os from 'os';
import path from 'path';
import fs from 'fs';
const tmp = fs.mkdtempSync(path.join(os.tmpdir(), 'actest-'));
fs.mkdirSync(path.join(tmp, 'logs'));
const log = createLogger({ dataDir: tmp, port: 37888, logLevel: 'INFO', component: 'test' });
log.info('hello');
log.debug('suppressed');
const lines = fs.readFileSync(path.join(tmp, 'logs', 'worker.log'), 'utf8').trim().split('\n');
if (lines.length !== 1) throw new Error('Expected 1 line, got ' + lines.length);
const parsed = JSON.parse(lines[0]);
if (parsed.component !== 'test') throw new Error('Missing component');
if (parsed.level !== 'INFO') throw new Error('Wrong level');
if (parsed.port !== 37888) throw new Error('Missing port');
if (!parsed.ts) throw new Error('Missing ts');
fs.rmSync(tmp, { recursive: true });
console.log('PASS');
EOF</automated>
  </verify>
  <done>worker/lib/logger.js exists, exports createLogger, every produced log line includes component field, level filtering works, convenience methods present</done>
</task>

</tasks>

<verification>
Run the inline node verification in Task 1's verify block. Confirm:
- worker/lib/logger.js exists
- createLogger is the named export
- JSON lines include ts, level, msg, pid, port, component fields
- DEBUG suppressed when logLevel=INFO
</verification>

<success_criteria>
worker/lib/logger.js exists and passes the automated verification. Any downstream module can call createLogger({ dataDir, port, logLevel, component }) and get a structured logger that writes to worker.log with a component tag on every line.
</success_criteria>

<output>
After completion, create `.planning/phases/23-logging-instrumentation/23-01-SUMMARY.md`
</output>

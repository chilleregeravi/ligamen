---
phase: 23-logging-instrumentation
plan: 02
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified:
  - worker/index.js
  - worker/server/http.js
  - worker/server/chroma.js
autonomous: true
requirements:
  - LOG-INFRA-01
  - LOG-INFRA-02
  - LOG-INFRA-03

must_haves:
  truths:
    - "worker/index.js uses the shared logger instead of its inline log() function"
    - "worker/server/http.js emits component=http log lines on server startup"
    - "worker/server/chroma.js uses the shared logger instead of process.stderr.write"
    - "No inline log() function definition remains in worker/index.js"
  artifacts:
    - path: "worker/index.js"
      provides: "Worker entry point using imported createLogger"
      contains: "createLogger"
    - path: "worker/server/http.js"
      provides: "HTTP server with component=http structured logging"
      contains: "component"
    - path: "worker/server/chroma.js"
      provides: "ChromaDB sync module using shared logger"
      contains: "createLogger"
  key_links:
    - from: "worker/index.js"
      to: "worker/lib/logger.js"
      via: "import { createLogger } from '../lib/logger.js'"
      pattern: "createLogger"
    - from: "worker/server/http.js"
      to: "worker/lib/logger.js"
      via: "import { createLogger } from '../lib/logger.js'"
      pattern: "createLogger"
    - from: "worker/server/chroma.js"
      to: "worker/lib/logger.js"
      via: "import { createLogger } from '../lib/logger.js'"
      pattern: "createLogger"
---

<objective>
Wire the shared logger from Plan 01 into worker/index.js (replacing the inline log() definition), worker/server/http.js (adding structured request/startup logging with component=http), and worker/server/chroma.js (replacing process.stderr.write calls with structured log calls using component=worker).

Purpose: Remove the inline log() from the entry point; ensure HTTP server events and ChromaDB lifecycle events are captured with consistent component tags.
Output: Three modified files, all using createLogger from worker/lib/logger.js.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-logging-instrumentation/23-01-SUMMARY.md

<interfaces>
<!-- From worker/lib/logger.js (created in Plan 01): -->
```javascript
export function createLogger({ dataDir, port, logLevel, component })
// Returns: { log(level, msg, extra), info(msg, extra), warn(msg, extra), error(msg, extra), debug(msg, extra) }
```

<!-- worker/index.js current inline log() (lines 52-67) — REMOVE this entire block: -->
```javascript
const LEVELS = { DEBUG: 0, INFO: 1, WARN: 2, ERROR: 3 };
function log(level, msg, extra = {}) { ... }
```

<!-- worker/server/http.js createHttpServer signature: -->
```javascript
async function createHttpServer(queryEngine, options = {})
// options: { port, resolveQueryEngine }
// Returns: FastifyInstance
```

<!-- worker/server/chroma.js — current process.stderr.write calls to replace: -->
// Line 100: process.stderr.write("[chroma] init failed: " + err.message + "\n");
// Line 154: process.stderr.write("[chroma] syncFindings error: " + err.message + "\n");
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline log() in worker/index.js with imported createLogger</name>
  <files>worker/index.js</files>
  <action>
    1. Add import at top of file (after existing imports):
       import { createLogger } from './lib/logger.js';

    2. Remove the entire "5. Structured logger" section (the LEVELS const and log() function definition, lines 52-67).

    3. After the logLevel and dataDir variables are set (after the settings.json reading block, before the logs dir creation), create the logger:
       const logger = createLogger({ dataDir, port, logLevel, component: 'worker' });

    4. Replace all calls to log(...) in the file with logger.log(...):
       - log("INFO", ok ? "ChromaDB connected" : ...) → logger.log("INFO", ...)
       - log("INFO", "worker started", { port }) → logger.log("INFO", "worker started", { port })
       - log("INFO", `received ${signal}, shutting down`) → logger.log("INFO", `received ${signal}, shutting down`)
       - log("INFO", "worker stopped") → logger.log("INFO", "worker stopped")

    Note: The logger instance is created AFTER dataDir and logLevel are resolved but BEFORE the logs dir creation line (fs.mkdirSync) — this ordering is fine since createLogger does not immediately write to the log file. However, place the createLogger call AFTER fs.mkdirSync to ensure the logs/ directory exists before any log writes. The exact placement should be:
       - After: fs.mkdirSync(path.join(dataDir, "logs"), { recursive: true });
       - Before: const PID_FILE = ...

    The inline LEVELS const and log() function must be fully deleted — not commented out.
  </action>
  <verify>
    <automated>node --input-type=module &lt;&lt;'EOF'
import { readFileSync } from 'fs';
const src = readFileSync('./worker/index.js', 'utf8');
if (src.includes('function log(')) throw new Error('Inline log() still present');
if (!src.includes('createLogger')) throw new Error('createLogger not imported');
if (!src.includes("component: 'worker'")) throw new Error('component worker not set');
if (src.includes("const LEVELS = {")) throw new Error('LEVELS still present');
console.log('PASS');
EOF</automated>
  </verify>
  <done>worker/index.js imports createLogger, has no inline log() or LEVELS, all log() calls replaced with logger.log() calls</done>
</task>

<task type="auto">
  <name>Task 2: Add structured logging to worker/server/http.js with component=http</name>
  <files>worker/server/http.js</files>
  <action>
    The HTTP server currently has no logging at all. Add structured logging for meaningful events.

    1. Update createHttpServer signature to accept a logger option:
       async function createHttpServer(queryEngine, options = {})
       - Add: const log = options.logger || null;

    2. Add a helper at the top of the function body:
       function httpLog(level, msg, extra = {}) {
         if (log) log.log(level, msg, extra);
       }

    3. Add log calls at these specific points (do NOT log every request — only errors and lifecycle):
       - After fastify starts listening (after the if/else block for port 0):
         httpLog('INFO', 'http server listening', { port: options.port });
       - In each route's catch block (for 500 errors), before the reply.code(500):
         httpLog('ERROR', err.message, { route: '/graph' });  // use the actual route name
         (Add this to: /graph, /impact, /service/:name, /scan, /versions routes only — the ones with try/catch)

    4. Update the export — no change needed, createHttpServer signature is backward compatible since options.logger is optional.

    5. Update the call in worker/index.js (already done in Task 1) to pass the logger:
       IMPORTANT: worker/index.js is modified in Task 1 of this plan. The call to createHttpServer should become:
       const app = await createHttpServer(null, {
         port,
         resolveQueryEngine: getQueryEngine,
         logger,
       });
       Add this change to worker/index.js in this task (since Task 1 may not have added the logger option to the createHttpServer call).

    Implementation note: The logger option is optional — when omitted (tests), httpLog is a no-op. This keeps existing tests passing without changes.
  </action>
  <verify>
    <automated>node --input-type=module &lt;&lt;'EOF'
import { readFileSync } from 'fs';
const src = readFileSync('./worker/server/http.js', 'utf8');
if (!src.includes('options.logger')) throw new Error('logger option not used');
if (!src.includes('httpLog')) throw new Error('httpLog helper not defined');
if (!src.includes("'http server listening'")) throw new Error('startup log missing');
console.log('PASS');
EOF</automated>
  </verify>
  <done>worker/server/http.js accepts logger option, logs server startup and route errors via httpLog helper, backward compatible when logger is omitted</done>
</task>

<task type="auto">
  <name>Task 3: Replace process.stderr.write calls in worker/server/chroma.js with shared logger</name>
  <files>worker/server/chroma.js</files>
  <action>
    worker/server/chroma.js has two process.stderr.write calls that need to become structured log calls.

    Current calls:
    - Line 100: process.stderr.write("[chroma] init failed: " + err.message + "\n");
    - Line 154: process.stderr.write("[chroma] syncFindings error: " + err.message + "\n");

    The chroma module does not own a logger instance — it is a library module called from worker/index.js. The cleanest approach is to pass a logger at init time.

    Changes to make:

    1. Add a module-level variable: let _logger = null;

    2. Update initChromaSync signature:
       export async function initChromaSync(settings = {}, mockClient = null, logger = null)
       At the top of initChromaSync, set: _logger = logger;

    3. Replace process.stderr.write calls:
       - "[chroma] init failed" → if (_logger) _logger.error('chroma init failed', { error: err.message }); else process.stderr.write(...)
         Actually: replace entirely with: if (_logger) { _logger.error('chroma init failed', { error: err.message }); } else { process.stderr.write('[chroma] init failed: ' + err.message + '\n'); }
       - "[chroma] syncFindings error" → same pattern with message 'chroma syncFindings error'

    4. Update the call in worker/index.js to pass the logger:
       initChromaSync(allSettings, undefined, logger)
       (Note: mockClient arg becomes undefined since no mock is needed in production)
       Wait — the current signature is initChromaSync(settings, mockClient). Add logger as the third argument. Update worker/index.js call from:
         initChromaSync(allSettings)
       to:
         initChromaSync(allSettings, null, logger)
       Add this change to worker/index.js as part of this task.

    Do NOT import createLogger directly in chroma.js — it receives the logger instance injected from outside. This keeps chroma.js decoupled from dataDir/logLevel concerns.
  </action>
  <verify>
    <automated>node --input-type=module &lt;&lt;'EOF'
import { readFileSync } from 'fs';
const src = readFileSync('./worker/server/chroma.js', 'utf8');
if (src.match(/process\.stderr\.write\(\[chroma\]/)) throw new Error('Old stderr.write still present');
if (!src.includes('_logger')) throw new Error('_logger variable not present');
if (!src.includes('logger = null')) throw new Error('logger param not added to initChromaSync');
console.log('PASS');
EOF</automated>
  </verify>
  <done>worker/server/chroma.js uses injected logger for error reporting, falls back to process.stderr.write when no logger provided (for test compat), worker/index.js passes logger to initChromaSync</done>
</task>

</tasks>

<verification>
After all tasks:
1. grep -r "function log(" worker/index.js — should return nothing
2. grep -r "const LEVELS" worker/index.js — should return nothing
3. grep -n "createLogger" worker/index.js worker/server/http.js — should show imports
4. node --input-type=module verifications in each task pass
</verification>

<success_criteria>
worker/index.js, worker/server/http.js, and worker/server/chroma.js all use the shared createLogger from worker/lib/logger.js. The inline log() function is gone from worker/index.js. ChromaDB error paths use the injected logger. HTTP server startup and errors are logged with component=http.
</success_criteria>

<output>
After completion, create `.planning/phases/23-logging-instrumentation/23-02-SUMMARY.md`
</output>

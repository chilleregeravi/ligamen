---
phase: 113-verification-gate
plan: 01
type: execute
wave: 4
depends_on: ["107-01", "108-01", "109-01", "110-01", "111-01", "112-01"]
files_modified:
  - plugins/arcanon/.claude-plugin/plugin.json
  - plugins/arcanon/.claude-plugin/marketplace.json
  - .claude-plugin/marketplace.json
  - plugins/arcanon/package.json
  - plugins/arcanon/package-lock.json
  - plugins/arcanon/CHANGELOG.md
  - .planning/phases/113-verification-gate/113-VERIFICATION.md
autonomous: true
requirements: [VER-01, VER-02, VER-03, VER-04, VER-05, VER-06, VER-07]
must_haves:
  truths:
    - "Repo-wide grep returns zero matches for runtime-deps.json (regression guard from INST-01)"
    - "Repo-wide grep returns zero matches for commands/upload.md (regression guard from DEP-01)"
    - "Repo-wide grep returns zero --help strings in plugins/arcanon/commands/*.md (regression guard against v0.1.4 scope creep)"
    - "Repo-wide grep returns zero /arcanon:upload references in README.md and plugins/arcanon/skills/ (regression guard from DEP-04, DEP-05)"
    - "bats suite runs green with ≥310 tests passing (1 macOS HOK-06 caveat at IMPACT_HOOK_LATENCY_THRESHOLD=200 acceptable)"
    - "node test suite runs green for affected modules (migrations, query-engine, scan, hub-sync, server, mcp); only the 2 documented pre-existing failures from v0.1.2 are unrelated to v0.1.3"
    - "All 4 manifest files contain version 0.1.3 (6 version strings total)"
    - "package-lock.json is regenerated post-bump so npm ci stays in sync"
    - "CHANGELOG [0.1.3] - <today's UTC date> section is pinned with BREAKING, Added, Changed, Fixed, Removed subsections; fresh empty [Unreleased] heading at top"
    - "113-VERIFICATION.md exists with status: passed, per-REQ table for VER-01..07, test counts, documented non-regressions, and v0.1.3 phase summary (101..113)"
  artifacts:
    - path: ".planning/phases/113-verification-gate/113-VERIFICATION.md"
      provides: "Verification gate report — status, per-REQ pass/fail, test counts, phase summary, verdict"
      min_lines: 80
      contains: "Status: ✅ PASSED"
    - path: "plugins/arcanon/.claude-plugin/plugin.json"
      provides: "Plugin manifest at version 0.1.3"
      contains: '"version": "0.1.3"'
    - path: "plugins/arcanon/.claude-plugin/marketplace.json"
      provides: "Plugin-scoped marketplace at version 0.1.3 (×2 occurrences: plugin entry + top-level)"
      contains: '"version": "0.1.3"'
    - path: ".claude-plugin/marketplace.json"
      provides: "Root marketplace at version 0.1.3 (×2 occurrences: plugin entry + top-level)"
      contains: '"version": "0.1.3"'
    - path: "plugins/arcanon/package.json"
      provides: "npm package at version 0.1.3"
      contains: '"version": "0.1.3"'
    - path: "plugins/arcanon/package-lock.json"
      provides: "Regenerated lockfile so the plugin's own version (0.1.3) appears in both root .version and packages.\"\".version (CI npm ci alignment)"
      contains: '"version": "0.1.3"'
    - path: "plugins/arcanon/CHANGELOG.md"
      provides: "Pinned [0.1.3] - YYYY-MM-DD release section with all 5 required subsections + fresh [Unreleased] heading"
      contains: "## [0.1.3]"
  key_links:
    - from: "plugins/arcanon/CHANGELOG.md [0.1.3] BREAKING subsection"
      to: "INST-01 (runtime-deps.json removal) and DEP-01 (/arcanon:upload removal)"
      via: "narrative bullet entries describing each breaking change"
      pattern: "runtime-deps\\.json|/arcanon:upload"
    - from: "plugins/arcanon/CHANGELOG.md [0.1.3] Added subsection"
      to: "TRUST-01 (/arcanon:verify), TRUST-04 (services.base_path), TRUST-05 (scan_versions.quality_score), TRUST-06 (enrichment_log + impact_audit_log)"
      via: "narrative bullet entries"
      pattern: "/arcanon:verify|base_path|quality_score|enrichment_log|impact_audit_log"
    - from: "plugins/arcanon/CHANGELOG.md [0.1.3] Changed subsection"
      to: "INST-02..06 (install-deps.sh + mcp-wrapper.sh rewrite), TRUST-05 (status quality score surface)"
      via: "narrative bullet entries"
      pattern: "install-deps\\.sh|mcp-wrapper\\.sh|/arcanon:status"
    - from: "plugins/arcanon/CHANGELOG.md [0.1.3] Fixed subsection"
      to: "UPD-01..03 (THE-1027 update --check 5s timeout), TRUST-02 (evidence-at-ingest enforcement)"
      via: "narrative bullet entries"
      pattern: "THE-1027|evidence-at-ingest|--check"
    - from: "plugins/arcanon/CHANGELOG.md [0.1.3] Removed subsection"
      to: "INST-01 (runtime-deps.json) and DEP-01..05 (/arcanon:upload + tests + README + skill refs)"
      via: "narrative bullet entries"
      pattern: "runtime-deps\\.json|/arcanon:upload"
    - from: "113-VERIFICATION.md per-REQ table"
      to: "VER-01..07 in REQUIREMENTS.md"
      via: "row per REQ with pass/fail status and evidence"
      pattern: "VER-0[1-7]"
---

<objective>
Run the v0.1.3 release verification gate: repo-wide regression greps, full bats suite, node test suite for affected modules, manifest version bump (4 files / 6 strings) plus package-lock regeneration, CHANGELOG `[0.1.3] - YYYY-MM-DD` pinning with all 5 subsections, and a final `113-VERIFICATION.md` report. Mirrors v0.1.2's single-plan Phase 105 gate.

Purpose: Without this plan, v0.1.3 ships untested — the install-architecture rewrite (Phase 107), update-check fix (Phase 108), scan-trust hardening (Phases 109-111), and `/arcanon:verify` command (Phase 112) all need a single coordinated verification before tag. The greps catch leakage of removed artifacts (`runtime-deps.json`, `commands/upload.md`, `/arcanon:upload`); the test suites catch regression in code paths; the manifest bump is the version pin; the CHANGELOG is the user-facing record. The verification report is the audit trail proving the milestone is ready for `/gsd-complete-milestone v0.1.3`.

Output: 4 manifest files at `0.1.3` + regenerated `package-lock.json` + pinned CHANGELOG + `113-VERIFICATION.md` recording per-REQ pass/fail with test counts, documented non-regressions, and the v0.1.3 phase summary table (101..113).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-verification-gate/113-CONTEXT.md
@.planning/milestones/v0.1.2-phases/105-verification-gate/105-VERIFICATION.md
@plugins/arcanon/CHANGELOG.md
@plugins/arcanon/.claude-plugin/plugin.json
@plugins/arcanon/.claude-plugin/marketplace.json
@.claude-plugin/marketplace.json
@plugins/arcanon/package.json

<interfaces>
<!-- Manifest files and the version strings each holds. -->
<!-- After Phase 107, runtime-deps.json is GONE — do not re-add it. -->

plugins/arcanon/.claude-plugin/plugin.json (1 occurrence):
```json
{ "name": "arcanon", "version": "0.1.2", ... }
```

plugins/arcanon/.claude-plugin/marketplace.json (2 occurrences):
```json
{
  "name": "arcanon",
  "plugins": [{ "name": "arcanon", "version": "0.1.2", ... }],
  "version": "0.1.2"
}
```

.claude-plugin/marketplace.json (2 occurrences, same shape as above):
```json
{
  "name": "arcanon",
  "plugins": [{ "name": "arcanon", "version": "0.1.2", ... }],
  "version": "0.1.2"
}
```

plugins/arcanon/package.json (1 occurrence):
```json
{ "name": "@arcanon/plugin", "version": "0.1.2", ... }
```

plugins/arcanon/package-lock.json (2 occurrences — regenerated, NOT manually edited):
```json
{ "version": "0.1.2", "packages": { "": { "version": "0.1.2", ... } } }
```

<!-- 105-VERIFICATION.md report shape (mirror this in 113-VERIFICATION.md) -->
- Frontmatter: phase, status, verified_at
- "## Status: ✅ PASSED" heading
- Per-REQ section per VER-NN with command + expected + actual
- Phase summary table (Phase | Status | REQs | Notes)
- Breaking changes summary for the milestone
- Verdict line at end

<!-- CHANGELOG [Unreleased] → [0.1.3] structure (subsection order matches Keep a Changelog) -->
```
## [Unreleased]
                                ← keep empty after pin (fresh placeholder)

## [0.1.3] - YYYY-MM-DD

### BREAKING
- `runtime-deps.json` removed. ...
- `/arcanon:upload` deprecated stub removed. ...

### Added
- `/arcanon:verify` command. ...
- `services.base_path` column. ...
- `scan_versions.quality_score` column. ...
- `enrichment_log` table + `impact_audit_log` MCP tool. ...

### Changed
- `install-deps.sh` rewrite (sentinel + binding-load validation + rebuild fallback). ...
- `mcp-wrapper.sh` simplified to plain `exec node`. ...
- `/arcanon:status` surfaces scan quality score. ...

### Fixed
- `/arcanon:update --check` 5s timeout false-offline (THE-1027). ...
- Evidence-at-ingest enforcement in `persistFindings` (TRUST-02). ...

### Removed
- `runtime-deps.json` and `@arcanon/runtime-deps` package identity. ...
- `/arcanon:upload` deprecated stub + 5 bats tests + README + skill refs. ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run regression greps (VER-03, VER-04)</name>
  <files>(read-only — no files modified)</files>
  <action>
    Run the four regression greps that prove deleted artifacts stay deleted. Capture output for the verification report.

    1. **VER-03 — `runtime-deps.json` is gone:**
       ```bash
       test ! -f plugins/arcanon/runtime-deps.json && echo "PASS: runtime-deps.json absent" || echo "FAIL: runtime-deps.json still exists"
       ! grep -rn "runtime-deps\.json" plugins/ scripts/ tests/ .claude-plugin/ README.md 2>/dev/null | grep -v node_modules | grep -v package-lock.json | grep -v ".planning/" | grep -v CHANGELOG.md
       ```
       Expected: file absent + zero grep matches outside CHANGELOG (BREAKING entry for the removal is allowed) and outside .planning/ (historical references in archived plans/SUMMARYs are allowed).

    2. **VER-04a — `commands/upload.md` is gone:**
       ```bash
       test ! -f plugins/arcanon/commands/upload.md && echo "PASS: upload.md absent" || echo "FAIL: upload.md still exists"
       ```

    3. **VER-04b — no `--help` strings in `commands/*.md`:**
       ```bash
       ! grep -rn '\-\-help' plugins/arcanon/commands/ 2>/dev/null
       ```
       Expected: zero output. v0.1.4 will add `--help` system-wide; v0.1.3 explicitly excludes it (per D-04 in CONTEXT.md). Any match means scope creep.

    4. **VER-04c (regression guard) — no `/arcanon:upload` references in user-facing docs/skills:**
       ```bash
       ! grep -rn "/arcanon:upload" README.md plugins/arcanon/skills/ 2>/dev/null
       ```
       Expected: zero matches. CHANGELOG BREAKING is the only allowed mention (it lives in `plugins/arcanon/CHANGELOG.md`, NOT in skills/, so the grep doesn't see it).

    For each grep, record (a) exact command run, (b) expected, (c) actual. If any grep fails, STOP — do not proceed to bats; surface the failing grep to the user. The fix lives in the upstream phase (107 for runtime-deps.json, 108 for upload.md / /arcanon:upload), not here.

    No files modified by this task — output is captured for use in Task 6.
  </action>
  <verify>
    <automated>test ! -f plugins/arcanon/runtime-deps.json && test ! -f plugins/arcanon/commands/upload.md && ! grep -rn '\-\-help' plugins/arcanon/commands/ 2>/dev/null && ! grep -rn '/arcanon:upload' README.md plugins/arcanon/skills/ 2>/dev/null</automated>
  </verify>
  <done>All four regression greps return clean (file absences confirmed, zero matches in user-facing docs/commands/skills). Outputs captured for the verification report.</done>
</task>

<task type="auto">
  <name>Task 2: Run bats suite (VER-01)</name>
  <files>(read-only — bats only reads tests/)</files>
  <action>
    Run the full bats suite with the macOS-friendly latency threshold. Per D-06, this is local dev practice — CI already has `IMPACT_HOOK_LATENCY_THRESHOLD: "100"` set in `.github/workflows/ci.yml`.

    ```bash
    cd /Users/ravichillerega/sources/ligamen
    IMPACT_HOOK_LATENCY_THRESHOLD=200 make test 2>&1 | tee /tmp/113-bats-output.log
    ```

    Or the equivalent direct bats invocation if `make test` doesn't exist or fails to discover Phase 107-112's new test files:
    ```bash
    IMPACT_HOOK_LATENCY_THRESHOLD=200 bats tests/ 2>&1 | tee /tmp/113-bats-output.log
    ```

    Record:
    - Exact command run
    - Pass/fail count (e.g., "320 tests, 320 passed, 0 failed" or "320 tests, 319 passed, 1 failed")
    - Names + line numbers of any failures

    Acceptance bar (per D-06):
    - ✅ ≥310 tests pass — gate passes (the v0.1.2 baseline)
    - ✅ ≥320 tests pass — expected target after Phases 107-112 added install-deps.bats, verify.bats, migration tests, and bats regression guards from 108
    - ⚠️ Test 155 (`impact-hook - HOK-06: p99 latency`) failure on macOS at threshold=200 is acceptable IF AND ONLY IF the failure is the documented BSD fork overhead (visible in stderr/log), and `IMPACT_HOOK_LATENCY_THRESHOLD=300` (or higher) makes it pass. Document this caveat exactly as `105-VERIFICATION.md` did.

    If a different test fails: investigate. Likely the gate caught a real regression — surface it to the user before proceeding. Do NOT silently document it as "non-regression" without confirming via git blame that it predates v0.1.3.

    No files modified — output captured for Task 6.
  </action>
  <verify>
    <automated>IMPACT_HOOK_LATENCY_THRESHOLD=200 bats tests/ 2>&1 | tail -5 | grep -E "tests, [0-9]+ passed"</automated>
  </verify>
  <done>bats suite runs to completion. Pass count ≥310 (≥320 expected). Any failures are either (a) the documented HOK-06 macOS caveat OR (b) confirmed pre-v0.1.3 via git blame, otherwise gate fails and user is notified. Output saved at /tmp/113-bats-output.log for Task 6.</done>
</task>

<task type="auto">
  <name>Task 3: Run node test suite for affected modules (VER-02)</name>
  <files>(read-only — node tests only read worker/)</files>
  <action>
    Run the node test suite for v0.1.3-affected modules. Per CONTEXT.md, the affected modules are:
    - `migrations` — Phases 109/110/111 added migrations 012, 013, 014, 015
    - `query-engine` — Phases 109 (path_template), 110 (base_path), 111 (quality_score) write through it
    - `scan` — Phase 109 (findings.js evidence rejection)
    - `hub-sync` — included for completeness (no v0.1.3 changes here, but baseline)
    - `server` — Phase 112 (verify endpoint exposure if added; check)
    - `mcp` — Phase 111 (impact_audit_log tool)

    From the plugin root:
    ```bash
    cd /Users/ravichillerega/sources/ligamen/plugins/arcanon
    npm test 2>&1 | tee /tmp/113-node-output.log
    ```

    The `package.json` `test` script is:
    ```
    find worker -name '*.test.js' -not -path '*/node_modules/*' -print0 | xargs -0 node --test
    ```
    so this runs every node test under `worker/`.

    If a long-lived worker process holds a port and timeouts cascade (the same pattern that bit v0.1.2 — see `105-VERIFICATION.md` line 43), kill it first:
    ```bash
    pkill -f "node.*worker/index" 2>/dev/null
    pkill -f "node.*worker/mcp/server" 2>/dev/null
    sleep 1
    cd /Users/ravichillerega/sources/ligamen/plugins/arcanon && npm test 2>&1 | tee /tmp/113-node-output.log
    ```

    Acceptance bar:
    - All v0.1.3-touched tests pass (migrations 012-015, path_template canonicalization, base_path resolution, quality_score computation, enrichment_log writes, impact_audit_log MCP tool, evidence rejection in findings.js)
    - The 2 documented pre-existing failures from v0.1.2 are acceptable IF AND ONLY IF the same two failures + same line numbers reappear:
      - `worker/mcp/server-search.test.js` — `queryScan` behavior drift
      - `worker/scan/manager.test.js` — `incremental prompt` mock missing `_db`
    - Any NEW failure: investigate. Likely a real regression introduced by 107/108/109/110/111/112. Surface to user before proceeding.

    Record exact pass/fail counts and any failure names. Subsystem suites can be cross-checked individually:
    ```bash
    cd /Users/ravichillerega/sources/ligamen/plugins/arcanon
    npm run test:migrations 2>&1 | tail -10
    npm run test:storage 2>&1 | tail -10
    npm run test:hub-sync 2>&1 | tail -10
    ```
    Use these to localize a failure if `npm test` reports one, but `npm test` is the authoritative result for VER-02.

    No files modified — output captured for Task 6.
  </action>
  <verify>
    <automated>cd /Users/ravichillerega/sources/ligamen/plugins/arcanon && npm test 2>&1 | tail -3 | grep -E "(pass|ok)"</automated>
  </verify>
  <done>npm test completes. Pass count documented. The only failures are the 2 documented pre-existing v0.1.2 failures (server-search queryScan + manager incremental prompt mock); any new failure stops the gate. Output at /tmp/113-node-output.log saved for Task 6.</done>
</task>

<task type="auto">
  <name>Task 4: Bump 4 manifest files to 0.1.3 + regenerate package-lock (VER-06)</name>
  <files>plugins/arcanon/.claude-plugin/plugin.json, plugins/arcanon/.claude-plugin/marketplace.json, .claude-plugin/marketplace.json, plugins/arcanon/package.json, plugins/arcanon/package-lock.json</files>
  <action>
    Per D-01, bump exactly **4 manifest files** (6 version strings total). `runtime-deps.json` is NOT in the list — Phase 107 deleted it (Task 1's VER-03 grep proves this). Per D-02, regenerate `package-lock.json` after the bump so `npm ci` stays in sync.

    1. **`plugins/arcanon/.claude-plugin/plugin.json`** — 1 occurrence at line 3:
       Change `"version": "0.1.2",` → `"version": "0.1.3",`

    2. **`plugins/arcanon/.claude-plugin/marketplace.json`** — 2 occurrences:
       - Line 9 (inside `plugins[0]`): `"version": "0.1.2",` → `"version": "0.1.3",`
       - Line 14 (top-level): `"version": "0.1.2"` → `"version": "0.1.3"`

    3. **`.claude-plugin/marketplace.json`** (root) — 2 occurrences:
       - Line 9 (inside `plugins[0]`): `"version": "0.1.2",` → `"version": "0.1.3",`
       - Line 14 (top-level): `"version": "0.1.2"` → `"version": "0.1.3"`

    4. **`plugins/arcanon/package.json`** — 1 occurrence at line 3:
       Change `"version": "0.1.2",` → `"version": "0.1.3",`

    Use the Edit tool for each file (verify the surrounding context lines before edit so the change is unambiguous — the JSON `"version"` key recurs inside dependency entries, but at line 3 / 9 / 14 the surrounding `"name"` and `"plugins"` context disambiguates).

    5. **Regenerate `package-lock.json`** — D-02 mandate:
       ```bash
       cd /Users/ravichillerega/sources/ligamen/plugins/arcanon
       npm install --package-lock-only
       ```
       This rewrites `package-lock.json` so the root `.version` and `packages.""."version"` both read `0.1.3` (matching the bumped `package.json`). It does **not** install dependencies; it only re-derives the lockfile. This is what unblocks CI's `npm ci` (the same gotcha that hit v0.1.2 PR #19).

    Verify post-bump:
    ```bash
    cd /Users/ravichillerega/sources/ligamen
    grep -nH '"version"' .claude-plugin/marketplace.json plugins/arcanon/.claude-plugin/plugin.json plugins/arcanon/.claude-plugin/marketplace.json plugins/arcanon/package.json plugins/arcanon/package-lock.json | grep -v "node_modules"
    ```
    Expected output: every match shows `"0.1.3"`. The package-lock.json has 2 matches (root + packages.""), package.json has 1, plugin.json has 1, both marketplace.json files have 2 each = 8 total `"0.1.3"` strings across 5 files. (Manifest count from VER-06's perspective is 4 — package-lock is generated, not authored.)

    Confirm `runtime-deps.json` is still gone (sanity, in case anything regenerated it):
    ```bash
    test ! -f plugins/arcanon/runtime-deps.json && echo "OK"
    ```
  </action>
  <verify>
    <automated>cd /Users/ravichillerega/sources/ligamen && [ "$(grep -h '"version"' .claude-plugin/marketplace.json plugins/arcanon/.claude-plugin/plugin.json plugins/arcanon/.claude-plugin/marketplace.json plugins/arcanon/package.json | grep -c '"0.1.3"')" -eq 6 ] && grep -q '"version": "0.1.3"' plugins/arcanon/package-lock.json && test ! -f plugins/arcanon/runtime-deps.json</automated>
  </verify>
  <done>All 6 manifest version strings across 4 files read `0.1.3`. package-lock.json regenerated and contains `0.1.3` in both root and packages."" entries. runtime-deps.json absence reconfirmed. `npm ci` would now pass (verifiable post-commit on CI).</done>
</task>

<task type="auto">
  <name>Task 5: Pin CHANGELOG [0.1.3] section (VER-07)</name>
  <files>plugins/arcanon/CHANGELOG.md</files>
  <action>
    Per D-03, capture today's UTC date once and reuse it. First step:
    ```bash
    TODAY=$(date -u +%Y-%m-%d)
    echo "$TODAY"
    ```
    Use the value (e.g., `2026-04-25`) literally in the CHANGELOG section heading.

    Edit `plugins/arcanon/CHANGELOG.md`:

    1. Find the existing `## [Unreleased]` heading (currently at line 7) and the `## [0.1.2] - 2026-04-24` heading below it (line 9).

    2. Insert the new pinned `## [0.1.3] - $TODAY` section between them, with all 5 subsections populated. Keep `## [Unreleased]` as a fresh empty placeholder above the new section (this is the Keep-a-Changelog convention — a perpetual `[Unreleased]` heading sits at top awaiting next-cycle entries).

    Result after edit:
    ```
    ## [Unreleased]

    ## [0.1.3] - YYYY-MM-DD

    ### BREAKING

    - **`/arcanon:upload` removed.** The deprecated stub introduced in v0.1.1 is
      now gone (DEP-01). Use `/arcanon:sync` (canonical since v0.1.1). CI
      pipelines or scripts hardcoding `/arcanon:upload` will fail with
      "command not found".
    - **`runtime-deps.json` removed.** `package.json` is now the single source
      of truth for runtime npm dependencies (INST-01). The `@arcanon/runtime-deps`
      package identity is retired. `install-deps.sh` no longer reads any
      separate manifest; it computes its sentinel from
      `jq '.dependencies + .optionalDependencies' package.json` directly.

    ### Added

    - **`/arcanon:verify` command** (TRUST-01). Re-reads cited evidence in source
      files and returns one of `ok` / `moved` / `missing` / `method_mismatch`
      per connection, without re-running a full `/arcanon:map`. Read-only and
      idempotent — safe to wire into CI or pre-commit.
    - **`services.base_path` column** (TRUST-04). Migration 012 adds the column;
      `agent-prompt-service.md` instructs the scanner to emit per-service
      `base_path` (e.g., `/api`); connection resolution strips `base_path`
      before path matching, eliminating a class of false-mismatch findings.
    - **`scan_versions.quality_score` column** (TRUST-05). Migration 014 adds
      the column. `endScan()` computes
      `(high_confidence_count + 0.5 × low_confidence_count) / total_connections`
      and persists it. Surfaced in `/arcanon:status` and at the end of
      `/arcanon:map` as `Scan quality: 87% high-confidence, 3 prose-evidence warnings`.
    - **`enrichment_log` table + `impact_audit_log` MCP tool** (TRUST-06).
      Migration 015 adds the table; post-scan reconciliation (external →
      cross-service downgrades) writes a row per change. New MCP tool
      `impact_audit_log(scan_version_id)` exposes the log to any project context.

    ### Changed

    - **`scripts/install-deps.sh` rewritten** (INST-02..05). Sentinel is sha256
      of `jq '.dependencies + .optionalDependencies' package.json`. Validation
      uses `node -e "require('better-sqlite3'); ..."` (binding-load), not file
      existence. Broken binding triggers `npm rebuild better-sqlite3` once
      before giving up. Happy path exits in <100ms with no `npm` process spawn.
      Always exits 0 — genuine failures are surfaced via worker startup, not by
      failing the SessionStart hook.
    - **`scripts/mcp-wrapper.sh` simplified** (INST-06). Reduced to
      `CLAUDE_PLUGIN_ROOT` resolution + `exec node "${PLUGIN_ROOT}/worker/mcp/server.js"`.
      No self-heal block, no dep-install fallback, no file-existence checks.
    - **`/arcanon:status` surfaces scan quality score** (TRUST-05). When the
      worker has graph data, status output now includes the same
      "Scan quality: NN%" line as `/arcanon:map`.

    ### Fixed

    - **`/arcanon:update --check` 5-second false-offline** (THE-1027 / UPD-01..03).
      The mirror file (`~/.claude/plugins/marketplaces/arcanon/.../marketplace.json`)
      is now the source of truth for the offline-decision. A slow
      `claude plugin marketplace update arcanon` (>5s) no longer flips the
      verdict to `offline` — that status is reserved for genuinely missing
      mirror dirs (fresh install, no network).
    - **Evidence-at-ingest enforcement** (TRUST-02). `persistFindings` now
      rejects connections whose `evidence` field is prose with no literal
      substring match against the cited `source_file` at ±3 lines of
      `line_start`. Rejected connections log a structured warning and are
      skipped; the rest of the scan completes normally. Stops a class of
      hallucinated connections from polluting the graph.

    ### Removed

    - **`plugins/arcanon/runtime-deps.json`** (INST-01). File deleted; package
      identity `@arcanon/runtime-deps` retired. See `### Changed →
      install-deps.sh` for the replacement sentinel mechanism.
    - **`/arcanon:upload`** (DEP-01..05). Command file `commands/upload.md`
      deleted; the 5 deprecated-stub assertions in `tests/commands-surface.bats`
      removed; `README.md` mentions removed; `plugins/arcanon/skills/impact/SKILL.md`
      mentions removed. A new bats regression guard asserts the file does NOT
      exist (DEP-03). Use `/arcanon:sync` (canonical since v0.1.1).
    ```

    Substitute `YYYY-MM-DD` with the captured `$TODAY` value (e.g., `2026-04-25`).

    Verify the structure post-edit:
    ```bash
    cd /Users/ravichillerega/sources/ligamen
    grep -n "^## " plugins/arcanon/CHANGELOG.md | head -5
    ```
    Expected first 3 lines:
    ```
    7:## [Unreleased]
    9:## [0.1.3] - 2026-04-25       ← today's UTC date
    NN:## [0.1.2] - 2026-04-24      ← unchanged
    ```
    (Line numbers will shift; the order matters, not the absolute lines.)

    And confirm all 5 subsections appear under `## [0.1.3]`:
    ```bash
    awk '/^## \[0\.1\.3\]/,/^## \[0\.1\.2\]/' plugins/arcanon/CHANGELOG.md | grep -E "^### "
    ```
    Expected output (in this exact order):
    ```
    ### BREAKING
    ### Added
    ### Changed
    ### Fixed
    ### Removed
    ```

    All 5 subsections present, in Keep-a-Changelog order. The narrative content above is the seed — the executor MAY edit individual bullets to reflect what each phase actually shipped (referenced by REQ id) but MUST NOT delete a subsection or omit any of the 5 cross-references called out in `must_haves.key_links` above.
  </action>
  <verify>
    <automated>cd /Users/ravichillerega/sources/ligamen && grep -q "^## \[0.1.3\] - $(date -u +%Y-%m-%d)$" plugins/arcanon/CHANGELOG.md && [ "$(awk '/^## \[0\.1\.3\]/,/^## \[0\.1\.2\]/' plugins/arcanon/CHANGELOG.md | grep -cE '^### (BREAKING|Added|Changed|Fixed|Removed)$')" -eq 5 ] && grep -q "^## \[Unreleased\]$" plugins/arcanon/CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG `[0.1.3] - YYYY-MM-DD` section exists with today's UTC date, contains all 5 subsections (BREAKING, Added, Changed, Fixed, Removed) in Keep-a-Changelog order, and a fresh empty `[Unreleased]` heading sits above it.</done>
</task>

<task type="auto">
  <name>Task 6: Write 113-VERIFICATION.md report</name>
  <files>.planning/phases/113-verification-gate/113-VERIFICATION.md</files>
  <action>
    Mirror the structure of `.planning/milestones/v0.1.2-phases/105-verification-gate/105-VERIFICATION.md` exactly. Use the captured outputs from Tasks 1-3 plus the manifest/CHANGELOG state from Tasks 4-5.

    File path: `.planning/phases/113-verification-gate/113-VERIFICATION.md`

    Structure:
    ```markdown
    ---
    phase: 113-verification-gate
    status: passed
    verified_at: <today's UTC date, same as CHANGELOG>
    ---

    # Phase 113: Verification Gate

    ## Status: ✅ PASSED

    Milestone v0.1.3 release gate verified. Install architecture cleanup
    (Phase 107), update-check timeout fix + `/arcanon:upload` removal
    (Phase 108), scan trust hardening (Phases 109-111), and the
    `/arcanon:verify` command (Phase 112) all verify clean. Manifests pinned
    at 0.1.3 across 4 files; CHANGELOG `[0.1.3]` section pinned with all 5
    subsections.

    ## Per-REQ Status

    | REQ | Description | Status | Evidence |
    |---|---|---|---|
    | VER-01 | bats green ≥310 | ✅ | <count>/<total> passing; Task 2 output at /tmp/113-bats-output.log |
    | VER-02 | node green for affected modules | ✅ | <count>/<total> passing; 2 documented pre-existing failures only |
    | VER-03 | runtime-deps.json absent | ✅ | `test ! -f plugins/arcanon/runtime-deps.json` returns 0 |
    | VER-04 | upload.md absent + no --help in commands/ + no /arcanon:upload in README/skills | ✅ | 4 greps clean (Task 1) |
    | VER-05 | Fresh-install Node 25 smoke | ✅ or ⚠️ deferred | <see VER-05 section> |
    | VER-06 | 4 manifests at 0.1.3 (6 strings) + lockfile regen | ✅ | 6 `"0.1.3"` matches across 4 files; package-lock regenerated |
    | VER-07 | CHANGELOG [0.1.3] pinned with 5 subsections | ✅ | All 5 subsections present in order |

    ## VER-01 — bats Suite

    Command:
    ```bash
    IMPACT_HOOK_LATENCY_THRESHOLD=200 make test
    ```

    Result: <count>/<total> passing.

    <If HOK-06 macOS caveat hit, document it exactly like 105-VERIFICATION.md
    line 30 — explain BSD fork overhead is the cause, CI threshold=100 not
    affected, no regression introduced.>

    <List names + line numbers of any new tests added in 107-112 that ran here:
    install-deps.bats, verify.bats, plus migration tests from 109/110/111.>

    ## VER-02 — node Suite

    Command:
    ```bash
    cd plugins/arcanon && npm test
    ```

    Result: <count>/<total> passing.

    Pre-existing non-regression failures (carried from v0.1.2):
    - `worker/mcp/server-search.test.js` — `queryScan` behavior drift
    - `worker/scan/manager.test.js` — `incremental prompt` mock missing `_db`

    These predate v0.1.3 and are unrelated to install-architecture or
    scan-trust work. Filed for future milestone.

    Phases 109/110/111 added new tests for migrations 012-015, path_template
    canonicalization, base_path resolution, quality_score, and enrichment_log —
    all green.

    ## VER-03 — runtime-deps.json removal regression guard

    | Check | Result |
    |---|---|
    | `test ! -f plugins/arcanon/runtime-deps.json` | ✅ file absent |
    | Repo-wide grep for `runtime-deps.json` outside CHANGELOG/.planning/package-lock | ✅ zero matches |

    Per Phase 107's INST-01: `runtime-deps.json` was deleted; `install-deps.sh`
    now derives its sentinel from `package.json` directly.

    ## VER-04 — `/arcanon:upload` + `--help` regression guards

    | Check | Result |
    |---|---|
    | `test ! -f plugins/arcanon/commands/upload.md` | ✅ file absent |
    | `! grep -rn '\-\-help' plugins/arcanon/commands/` | ✅ zero matches |
    | `! grep -rn '/arcanon:upload' README.md plugins/arcanon/skills/` | ✅ zero matches |

    Per Phase 108's DEP-01..05: `/arcanon:upload` stub deleted along with
    its 5 bats assertions, README mentions, and skill references. v0.1.4's
    `--help` system is correctly NOT present (out of scope for v0.1.3 per
    REQUIREMENTS.md "Out of Scope" table).

    ## VER-05 — Fresh-install Smoke (Node 25)

    <Pick one of the two patterns based on what was actually run:>

    **Pattern A — performed in-session:**
    ```bash
    # Clone v0.1.3 tag in a fresh workspace
    git clone --branch v0.1.3 https://github.com/Arcanon-hub/arcanon /tmp/arcanon-fresh
    # ... claude plugin marketplace add + install + first session ... /arcanon:status
    ```
    Result: <PASS / FAIL with notes>

    **Pattern B — deferred to pre-tag manual run** (mirrors 105-VERIFICATION
    line 63):
    The fresh-install machinery is unchanged from v0.1.1, and Phase 107's
    `install-deps.sh` rewrite is unit-tested by INST-07..11 (5 bats fixtures
    landed in Phase 107). Manual smoke deferred to pre-tag run; recorded here
    once executed.

    ## VER-06 — Manifest Bump

    | File | Occurrences | Status |
    |---|---|---|
    | `plugins/arcanon/.claude-plugin/plugin.json` | 1 | ✅ "0.1.3" |
    | `plugins/arcanon/.claude-plugin/marketplace.json` | 2 | ✅ both "0.1.3" |
    | `.claude-plugin/marketplace.json` | 2 | ✅ both "0.1.3" |
    | `plugins/arcanon/package.json` | 1 | ✅ "0.1.3" |
    | **Total** | **6 strings / 4 files** | ✅ |
    | `plugins/arcanon/package-lock.json` | 2 (regenerated via `npm install --package-lock-only`) | ✅ "0.1.3" |

    `runtime-deps.json` is intentionally **not** in this list — INST-01 deleted
    it. Manifest count for v0.1.3 is 4, down from 5 in v0.1.2 (per D-01).

    ## VER-07 — CHANGELOG Pin

    `## [0.1.3] - <today's UTC date>` heading present. All 5 subsections in
    Keep-a-Changelog order:

    | Subsection | Required Coverage | Status |
    |---|---|---|
    | ### BREAKING | runtime-deps.json removal, /arcanon:upload removal | ✅ |
    | ### Added | /arcanon:verify, services.base_path, scan_versions.quality_score, enrichment_log + impact_audit_log | ✅ |
    | ### Changed | install-deps.sh rewrite, mcp-wrapper.sh simplification, /arcanon:status quality-score surface | ✅ |
    | ### Fixed | THE-1027 update --check 5s timeout, evidence-at-ingest enforcement | ✅ |
    | ### Removed | runtime-deps.json + @arcanon/runtime-deps, /arcanon:upload + tests + README + skill refs | ✅ |

    Fresh empty `## [Unreleased]` heading retained at top.

    ## Summary of Phases (v0.1.3)

    | Phase | Status | REQs | Notes |
    |---|---|---|---|
    | 107 Install Architecture Cleanup | ✅ | 12/12 | runtime-deps.json deleted; sentinel + binding-load validation; mcp-wrapper.sh simplified |
    | 108 Update-check Timeout + /arcanon:upload Removal | ✅ | 12/12 | THE-1027 fix; DEP-01..06 stub purge |
    | 109 Path Canonicalization + Evidence at Ingest | ✅ | 4/4 | Migration 013; persistFindings rejects prose-only evidence |
    | 110 services.base_path End-to-End | ✅ | 2/2 | Migration 012; agent prompt + connection resolution |
    | 111 Quality Score + Reconciliation Audit Trail | ✅ | 4/4 | Migrations 014 + 015; impact_audit_log MCP tool |
    | 112 /arcanon:verify Command | ✅ | 4/4 | New read-only command with 4 verdicts |
    | 113 Verification Gate | ✅ | 7/7 | This report |
    | **Total** | **✅** | **45/45** | |

    ## Breaking Changes Summary (for release notes)

    1. `runtime-deps.json` removed. Single source of truth = `package.json`.
       `@arcanon/runtime-deps` package identity retired.
    2. `/arcanon:upload` deprecated stub removed. Use `/arcanon:sync` (canonical
       since v0.1.1). Hardcoded CI references will fail with "command not found".

    ## Verdict

    **v0.1.3 Trust & Foundations — READY TO SHIP.**

    All 45 requirements complete. Test suites green (with 1 documented macOS
    HOK-06 latency caveat at threshold=200, and 2 pre-existing node failures
    unrelated to v0.1.3, all carried from v0.1.2). Manifests pinned at 0.1.3.
    CHANGELOG complete. Next step: `/gsd-complete-milestone v0.1.3`.
    ```

    Substitute the captured numbers from Tasks 1-3 into the `<count>/<total>`
    placeholders. Substitute today's UTC date into `verified_at` and the
    CHANGELOG section heading reference.

    Verify the report:
    ```bash
    test -f .planning/phases/113-verification-gate/113-VERIFICATION.md
    grep -q "Status: ✅ PASSED" .planning/phases/113-verification-gate/113-VERIFICATION.md
    grep -q "VER-01" .planning/phases/113-verification-gate/113-VERIFICATION.md
    grep -q "VER-07" .planning/phases/113-verification-gate/113-VERIFICATION.md
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/113-verification-gate/113-VERIFICATION.md && [ "$(grep -cE 'VER-0[1-7]' .planning/phases/113-verification-gate/113-VERIFICATION.md)" -ge 7 ] && grep -q "Status: ✅ PASSED" .planning/phases/113-verification-gate/113-VERIFICATION.md && grep -q "READY TO SHIP" .planning/phases/113-verification-gate/113-VERIFICATION.md</automated>
  </verify>
  <done>113-VERIFICATION.md exists at the canonical path, has frontmatter with `status: passed`, contains a per-REQ table covering VER-01 through VER-07 (each marked ✅), includes the v0.1.3 phase summary table (101..113 with REQ counts), the breaking-changes summary, and a final "READY TO SHIP" verdict line. The report is the audit trail for `/gsd-complete-milestone v0.1.3`.</done>
</task>

</tasks>

<threat_model>

`security_enforcement` is not enabled in this project's planning config. This is a documentation/verification phase that runs greps, executes existing test suites, edits version strings in JSON manifests, edits a CHANGELOG markdown file, and writes a verification report — no new trust boundaries, no new untrusted-input ingestion, no new code-execution surfaces. STRIDE register is therefore not applicable to this phase. Threat surface delta vs. the upstream phases (107-112) it verifies is zero.

</threat_model>

<verification>
Phase 113 passes when all 6 tasks complete and:

1. **Greps clean (Task 1):** No `runtime-deps.json` outside CHANGELOG/.planning. No `commands/upload.md`. No `--help` in `commands/*.md`. No `/arcanon:upload` in `README.md` or `plugins/arcanon/skills/`.

2. **bats green (Task 2):** ≥310 tests passing (≥320 expected after Phase 107-112 additions). Only acceptable failure: HOK-06 p99 latency on macOS at `IMPACT_HOOK_LATENCY_THRESHOLD=200`, exactly mirroring `105-VERIFICATION.md` line 30.

3. **node green (Task 3):** Affected suites pass. Only acceptable failures: the 2 documented pre-existing v0.1.2 failures (`server-search.test.js queryScan`, `manager.test.js incremental prompt mock`).

4. **Manifests bumped (Task 4):** 4 files / 6 strings at `0.1.3`. `package-lock.json` regenerated via `npm install --package-lock-only`.

5. **CHANGELOG pinned (Task 5):** `## [0.1.3] - YYYY-MM-DD` section with all 5 subsections (BREAKING, Added, Changed, Fixed, Removed) in Keep-a-Changelog order; fresh empty `## [Unreleased]` heading at top.

6. **Verification report exists (Task 6):** `113-VERIFICATION.md` mirrors `105-VERIFICATION.md` shape with per-REQ table, test counts, phase summary, breaking-changes summary, "READY TO SHIP" verdict.

Single end-to-end check command (run from repo root):
```bash
cd /Users/ravichillerega/sources/ligamen
test ! -f plugins/arcanon/runtime-deps.json \
  && test ! -f plugins/arcanon/commands/upload.md \
  && ! grep -rn '\-\-help' plugins/arcanon/commands/ 2>/dev/null \
  && ! grep -rn '/arcanon:upload' README.md plugins/arcanon/skills/ 2>/dev/null \
  && [ "$(grep -h '"version"' .claude-plugin/marketplace.json plugins/arcanon/.claude-plugin/plugin.json plugins/arcanon/.claude-plugin/marketplace.json plugins/arcanon/package.json | grep -c '"0.1.3"')" -eq 6 ] \
  && grep -q "^## \[0.1.3\] - $(date -u +%Y-%m-%d)$" plugins/arcanon/CHANGELOG.md \
  && test -f .planning/phases/113-verification-gate/113-VERIFICATION.md \
  && grep -q "READY TO SHIP" .planning/phases/113-verification-gate/113-VERIFICATION.md \
  && echo "Phase 113 gate: PASS"
```
</verification>

<success_criteria>
- [ ] VER-01: bats ≥310 passing (≥320 expected); HOK-06 macOS caveat documented if hit
- [ ] VER-02: node test suite green; only the 2 documented pre-existing v0.1.2 failures persist
- [ ] VER-03: `runtime-deps.json` is gone (file absent + grep clean outside historical/changelog refs)
- [ ] VER-04: `commands/upload.md` is gone, no `--help` in `commands/*.md`, no `/arcanon:upload` in README or skills
- [ ] VER-05: fresh-install smoke recorded in report (in-session execution OR explicit deferred-to-pre-tag note matching 105-VERIFICATION.md pattern)
- [ ] VER-06: 4 manifests / 6 version strings at `0.1.3`; package-lock.json regenerated
- [ ] VER-07: CHANGELOG `[0.1.3] - YYYY-MM-DD` section pinned with all 5 subsections; fresh `[Unreleased]` heading at top
- [ ] `113-VERIFICATION.md` written with per-REQ table, test counts, phase summary, breaking-changes summary, "READY TO SHIP" verdict
- [ ] All edits committed via git with `chore(113-01): ...` (verification work) and `chore(release): ...` (manifest bump + CHANGELOG pin) commit prefixes
</success_criteria>

<output>
After completion, create `.planning/phases/113-verification-gate/113-01-SUMMARY.md` covering:
- bats result (count, any caveats)
- node result (count, any caveats)
- 4 grep outcomes
- Manifest bump diff (which files changed, which strings)
- CHANGELOG pinned date + subsection list
- Path to `113-VERIFICATION.md`
- Verdict: ready for `/gsd-complete-milestone v0.1.3`
</output>

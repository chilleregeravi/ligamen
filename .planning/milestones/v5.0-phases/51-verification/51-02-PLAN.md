---
phase: 51-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 51-01
files_modified: []
autonomous: false
requirements:
  - VER-01
  - VER-02

must_haves:
  truths:
    - "`make test` runs all bats files and exits with zero failures"
    - "`claude plugin marketplace add <repo-path>` registers the plugin source directory without error"
    - "`claude plugin install ligamen@ligamen --scope user` installs all commands and hooks"
    - "After install, commands quality-gate, cross-impact, drift are available in Claude Code"
    - "After install, hooks (PostToolUse, PreToolUse, SessionStart) are active"
  artifacts:
    - path: "plugins/ligamen/.claude-plugin/plugin.json"
      provides: "Plugin manifest read by marketplace"
      contains: "\"name\""
    - path: "plugins/ligamen/.claude-plugin/marketplace.json"
      provides: "Marketplace metadata"
    - path: "plugins/ligamen/hooks/hooks.json"
      provides: "Hook registrations"
      contains: "CLAUDE_PLUGIN_ROOT"
  key_links:
    - from: "claude plugin marketplace add"
      to: "plugins/ligamen/.claude-plugin/plugin.json"
      via: "marketplace reads plugin.json from the registered directory"
      pattern: "\"name\".*ligamen"
    - from: "claude plugin install ligamen@ligamen"
      to: "plugins/ligamen/commands/"
      via: "install reads commands/ from plugin root"
    - from: "plugins/ligamen/hooks/hooks.json"
      to: "plugins/ligamen/scripts/"
      via: "CLAUDE_PLUGIN_ROOT env var set at hook invocation time"
      pattern: "CLAUDE_PLUGIN_ROOT"
---

<objective>
Run the full bats test suite and execute the end-to-end marketplace install flow to confirm the Phase 49-50 restructure is complete and correct.

Purpose: This is the final gate for v5.0. Automated tests confirm internal correctness. The marketplace flow confirms the distribution model works for end users.

Output: Zero test failures from `make test`. A working plugin install via `claude plugin marketplace add` + `claude plugin install`.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-verification/51-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full bats test suite and fix any remaining failures</name>
  <files></files>
  <read_first>
    Read the SUMMARY from plan 51-01 to understand what path changes were applied.
    Read tests/structure.bats to confirm PLUGIN_ROOT is set to plugins/ligamen/.
  </read_first>
  <action>
Run the full test suite:
```bash
make test
```

Expected: all tests pass, zero failures.

If tests fail, diagnose each failure. Common failure patterns after a path restructure:

1. **"No such file or directory" for a script path** — a test still references the old path. Fix the specific test file by applying the `plugins/ligamen/` prefix to the failing path.

2. **structure.bats "commands/ directory exists at plugin root" fails** — PLUGIN_ROOT in structure.bats does not resolve to plugins/ligamen/. Verify the PLUGIN_ROOT line was updated correctly.

3. **worker-lifecycle.bats bare script calls fail** — e.g. `bash scripts/worker-start.sh` fails because CWD is not plugins/ligamen/. Fix by changing to `bash "$PLUGIN_ROOT/scripts/worker-start.sh"` and similarly for `source lib/worker-client.sh` → `source "$PLUGIN_ROOT/lib/worker-client.sh"`.

4. **mcp-server.bats "worker/mcp/server.js" not found** — the cd in setup() must put CWD at plugins/ligamen/. Confirm mcp-server.bats setup() has `cd "$BATS_TEST_DIRNAME/../plugins/ligamen"`.

5. **hooks.json path tests fail** — hooks.json is now at plugins/ligamen/hooks/hooks.json. If structure.bats tests for `hooks/hooks.json` relative to PLUGIN_ROOT (plugins/ligamen/), those paths are correct as long as hooks/hooks.json lives inside plugins/ligamen/.

After fixing any failures, re-run `make test` until it exits 0.

Document each fix made in the SUMMARY — these become gap closure tasks if more are needed.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits with code 0
    - bats output contains "0 failures" or equivalent (no "X failed" line)
    - All test files in tests/*.bats are included in the run (count visible in bats output)
  </acceptance_criteria>
  <done>`make test` exits 0. Zero test failures reported across all bats files.</done>
</task>

<task type="auto">
  <name>Task 2: Verify marketplace install flow end-to-end</name>
  <files></files>
  <read_first>
    Read plugins/ligamen/.claude-plugin/plugin.json to confirm name field is "ligamen".
    Read plugins/ligamen/hooks/hooks.json to confirm hooks are present.
    Read the Makefile install target to confirm the commands used.
  </read_first>
  <action>
Run the marketplace install flow using the Makefile target:
```bash
make install
```

This runs two commands:
1. `claude plugin marketplace add $(PLUGIN_DIR)` — registers the repo directory as a marketplace source
2. `claude plugin install ligamen@ligamen --scope user` — installs the plugin

If `make install` fails, run each step individually with verbose output and diagnose:

**Step 1: Marketplace add**
```bash
claude plugin marketplace add "$(pwd)"
```
Expected: exits 0, no error output.

Common failures:
- "plugin.json not found" — the marketplace looks for `plugins/ligamen/.claude-plugin/plugin.json` (relative to the registered directory). Confirm the file exists at that path.
- "invalid plugin.json" — run `jq . plugins/ligamen/.claude-plugin/plugin.json` to check.

**Step 2: Plugin install**
```bash
claude plugin install ligamen@ligamen --scope user
```
Expected: exits 0, confirms commands and hooks installed.

Common failures:
- "marketplace not found" — step 1 failed; retry step 1 first.
- "commands not found" — plugins/ligamen/commands/ is missing. This would indicate Phase 49 is incomplete.

**Verification after install:**
```bash
claude plugin list
```
Should show `ligamen@ligamen` with source matching this repo.

If both steps succeed but the install command outputs warnings about hooks paths, investigate whether hooks.json CLAUDE_PLUGIN_ROOT references resolve at runtime. The hooks.json should use `$CLAUDE_PLUGIN_ROOT/scripts/format.sh` style paths (not hardcoded absolute paths) — Claude Code sets CLAUDE_PLUGIN_ROOT to the plugins/ligamen/ directory at hook invocation time.
  </action>
  <verify>
    <automated>claude plugin list 2>&1 | grep ligamen</automated>
  </verify>
  <acceptance_criteria>
    - `make install` exits 0
    - `claude plugin list` output contains "ligamen"
    - `claude plugin list` output contains this repo's path as the source
  </acceptance_criteria>
  <done>Plugin installed via marketplace flow. `claude plugin list` shows ligamen@ligamen as installed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm commands and hooks are active in Claude Code</name>
  <files></files>
  <action>Human verification required. Pause and ask the user to follow the how-to-verify steps below.</action>
  <verify>
    <automated>HUMAN_REQUIRED — see how-to-verify steps</automated>
  </verify>
  <done>User confirms all three slash commands respond and at least one PostToolUse hook fires.</done>
  <what-built>
    Full test suite passes (make test = 0 failures) and plugin installed via marketplace flow (make install succeeds, claude plugin list shows ligamen@ligamen).
  </what-built>
  <how-to-verify>
    1. Open a new Claude Code session (or reload the existing one)
    2. Verify `/quality-gate` command is available — type `/quality-gate` and confirm it is recognized
    3. Verify `/cross-impact` command is available
    4. Verify `/drift` command is available
    5. Make a small edit to any file in a test repo and confirm the PostToolUse hook fires (format.sh or lint.sh runs automatically)
    6. Confirm no "plugin not found" or "hook script not found" errors appear
    7. Run `make test` once more from the repo root to confirm zero failures
  </how-to-verify>
  <resume-signal>Type "approved" if all commands work and hooks fire. Describe any issues if not.</resume-signal>
</task>

</tasks>

<verification>
Phase 51 complete when:
1. `make test` exits 0 with zero bats failures
2. `make install` exits 0
3. `claude plugin list` shows ligamen@ligamen
4. Human confirms commands and hooks active in Claude Code session
</verification>

<success_criteria>
`make test` exits 0. Marketplace install flow works from a fresh state. Plugin commands and hooks are active in Claude Code. v5.0 Marketplace Restructure milestone is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/51-verification/51-02-SUMMARY.md`
</output>

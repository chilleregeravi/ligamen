---
phase: 90-discovery-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/ligamen/worker/scan/agent-prompt-discovery.md
autonomous: true
requirements:
  - DISC-01
  - DISC-02

must_haves:
  truths:
    - "Scanning a mono-repo with subdirectory manifests produces multiple service_hints entries"
    - "Scanning a repo with client-like files produces a non-empty client_files array"
    - "Scanning a repo with no client-like files produces an empty client_files array"
  artifacts:
    - path: "plugins/ligamen/worker/scan/agent-prompt-discovery.md"
      provides: "Updated discovery agent prompt with mono-repo heuristic and client_files schema"
      contains: "client_files"
  key_links:
    - from: "plugins/ligamen/worker/scan/agent-prompt-discovery.md"
      to: "Phase 2 deep scan"
      via: "client_files array in output JSON"
      pattern: "client_files"
---

<objective>
Update the discovery agent prompt with two improvements from THE-951:
1. Mono-repo detection heuristic — check subdirectory manifests and emit multiple service_hints entries (DISC-01)
2. client_files schema field — identify outbound HTTP call sites for Phase 2 deep scan efficiency (DISC-02)

Purpose: Phase 2 deep scan can find HTTP client files faster when they are pre-identified; mono-repo awareness avoids treating a workspace as a single service.
Output: Updated `plugins/ligamen/worker/scan/agent-prompt-discovery.md` with both additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mono-repo detection heuristic to discovery prompt (DISC-01)</name>
  <read_first>
    plugins/ligamen/worker/scan/agent-prompt-discovery.md
  </read_first>
  <files>plugins/ligamen/worker/scan/agent-prompt-discovery.md</files>
  <action>
In `plugins/ligamen/worker/scan/agent-prompt-discovery.md`, make two targeted edits:

**Edit 1 — "What to Check" section, item 1 (Manifest files):**
After the existing line listing root-level manifests, add a new instruction:

```
   After checking root-level manifests, also check for manifest files one level deep:
   `*/package.json`, `*/pyproject.toml`, `*/Cargo.toml`, `*/go.mod`.
   If two or more subdirectories each contain their own manifest, treat the repo as a mono-repo
   and list each such subdirectory as a separate `service_hints` entry with its `root_path` set
   to that subdirectory (per DISC-01).
```

**Edit 2 — "What to Check" section, add a new item 8:**
```
8. **Client/HTTP files** — files whose names match `*client*`, `*api*`, `*http*` (case-insensitive),
   OR any file that imports `fetch`, `requests`, `reqwest`, or `httpx`. List these in `client_files`
   (per DISC-02). Do NOT read source files line-by-line; only check filenames for the name patterns,
   and limit import scanning to files already opened for entry-point detection.
```

**Edit 3 — "Rules" bullet for mono-repo:**
Replace the existing rule:
```
- If a repo has multiple services (mono-repo), list each as a separate `service_hints` entry
```
with:
```
- If a repo has multiple services (mono-repo detected via subdirectory manifests), list each subdirectory service as a separate `service_hints` entry with the correct `root_path`
```

The "What to Check" section and Rules section are the only areas to modify in this task. Do not touch the Output Format / JSON schema yet — that is handled in Task 2.
  </action>
  <verify>
    <automated>grep -n "subdirector" /Users/ravichillerega/sources/ligamen/plugins/ligamen/worker/scan/agent-prompt-discovery.md</automated>
  </verify>
  <done>File contains instructions to check `*/package.json` (and peer manifests) and the mono-repo heuristic rule references subdirectory manifests.</done>
</task>

<task type="auto">
  <name>Task 2: Add client_files field to discovery output schema (DISC-02)</name>
  <read_first>
    plugins/ligamen/worker/scan/agent-prompt-discovery.md
  </read_first>
  <files>plugins/ligamen/worker/scan/agent-prompt-discovery.md</files>
  <action>
In the `## Output Format` JSON example of `agent-prompt-discovery.md`, insert `client_files` between `event_config_files` and `has_dockerfile`:

```json
  "client_files": ["string — files matching *client*, *api*, *http* patterns or importing fetch/requests/reqwest/httpx"],
```

The final JSON schema block must look like:

```json
{
  "repo_name": "string — directory name",
  "languages": ["python", "typescript", "rust", "go"],
  "frameworks": ["fastapi", "express", "actix-web", "gin"],
  "service_hints": [
    {
      "name": "string — likely service name",
      "type": "service | library | sdk",
      "root_path": "string — directory containing the service",
      "entry_file": "string — main entry point file",
      "framework": "string — detected framework"
    }
  ],
  "route_files": ["string — files likely containing endpoint definitions"],
  "proto_files": ["string — .proto files found"],
  "openapi_files": ["string — openapi/swagger files found"],
  "event_config_files": ["string — files with event/queue configuration"],
  "client_files": ["string — files matching *client*, *api*, *http* patterns or importing fetch/requests/reqwest/httpx"],
  "has_dockerfile": true,
  "has_docker_compose": true,
  "mono_repo": false,
  "notes": "string — anything unusual about the repo structure"
}
```

No other sections should change in this task.
  </action>
  <verify>
    <automated>grep -n "client_files" /Users/ravichillerega/sources/ligamen/plugins/ligamen/worker/scan/agent-prompt-discovery.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "client_files" returns at least 2 matches (schema field + instruction reference)
    - grep "subdirector" returns at least 1 match (mono-repo heuristic)
    - grep "\*/package.json" returns at least 1 match (subdirectory manifest glob)
    - grep "reqwest\|httpx\|requests" returns at least 1 match (import patterns)
    - File still has exactly one fenced JSON code block with all original fields preserved
  </acceptance_criteria>
  <done>
    - `client_files` field appears in the JSON schema block and in the "What to Check" instructions
    - All original fields (route_files, proto_files, openapi_files, event_config_files, has_dockerfile, has_docker_compose, mono_repo, notes) remain present
    - The three acceptance criteria for DISC-01 pass: subdirectory manifest instruction exists, mono-repo heuristic references root_path per subdirectory
  </done>
</task>

</tasks>

<verification>
Run all grep checks from task acceptance_criteria against the final file state:

```bash
grep -c "client_files" plugins/ligamen/worker/scan/agent-prompt-discovery.md   # expect >= 2
grep -c "subdirector" plugins/ligamen/worker/scan/agent-prompt-discovery.md    # expect >= 1
grep -c '\*/package.json' plugins/ligamen/worker/scan/agent-prompt-discovery.md # expect >= 1
grep -c 'reqwest\|httpx' plugins/ligamen/worker/scan/agent-prompt-discovery.md  # expect >= 1
```

Visual review: the JSON schema block must be valid JSON (no trailing commas, all fields present).
</verification>

<success_criteria>
1. `agent-prompt-discovery.md` instructs agents to scan `*/package.json`, `*/pyproject.toml`, `*/Cargo.toml`, `*/go.mod` and emit multiple `service_hints` entries for mono-repos (DISC-01)
2. `agent-prompt-discovery.md` output schema includes `client_files` array with filename-pattern and import-pattern detection rules (DISC-02)
3. All original schema fields are preserved
4. No other files are modified
</success_criteria>

<output>
After completion, create `.planning/phases/90-discovery-improvements/90-01-SUMMARY.md`
</output>

---
phase: 58-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/commands.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "README contains a keyboard shortcut reference table listing F, Esc, /, I, 2, 3 with their actions"
    - "README graph UI section describes PNG export, subgraph isolation, what-changed overlay, and edge bundling"
    - "docs/commands.md contains a graph UI section covering all v5.1 interactive capabilities"
  artifacts:
    - path: "README.md"
      provides: "v5.1 feature descriptions and keyboard shortcut table"
      contains: "| Key | Action |"
    - path: "docs/commands.md"
      provides: "Graph UI interactive capabilities reference"
      contains: "Graph UI"
  key_links:
    - from: "README.md"
      to: "docs/commands.md"
      via: "Documentation table link"
      pattern: "commands\\.md"
---

<objective>
Update README.md and docs/commands.md to document all v5.1 Graph Interactivity features introduced in phases 52-57.

Purpose: Users discovering Ligamen need accurate documentation covering keyboard shortcuts, PNG export, clickable panel navigation, subgraph isolation, what-changed overlay, and edge bundling.
Output: Updated README.md with keyboard shortcut table and expanded Graph UI section; updated docs/commands.md with a new Graph UI section covering all interactive controls.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with v5.1 graph features and keyboard shortcut table</name>
  <files>README.md</files>
  <read_first>
    - README.md — read the full file; the Graph UI section starts at line 46 and currently lists 6 bullet points describing static visualization capabilities. The new content goes here.
  </read_first>
  <action>
    In README.md, replace the existing "Graph UI (http://localhost:37888):" bullet list with an expanded section that covers all v5.1 capabilities. The updated section must include:

    1. All existing bullets (deterministic layered layout, boundary grouping, external actor detection, protocol-differentiated edges, collapsible filter panel, click-to-inspect detail panels)

    2. New v5.1 capability bullets added to that list:
       - Keyboard-driven navigation (fit, deselect, search focus, subgraph isolation)
       - Clickable service names in detail panel connections list navigate directly to that node
       - Subgraph isolation (I key, expandable to 2/3 hops)
       - What-changed overlay (new/modified nodes and edges from the latest scan are visually highlighted)
       - Edge bundling (parallel edges between the same node pair collapse into a single edge with count badge)
       - One-click PNG export button to screenshot the current canvas view

    3. Immediately after the Graph UI bullet list, add a new subsection "### Keyboard Shortcuts" containing a markdown table with these exact rows (in this order):

    | Key | Action |
    |-----|--------|
    | `F` | Fit all nodes to the visible canvas area |
    | `Esc` | Deselect the current node and close the detail panel |
    | `/` | Move keyboard focus to the search input |
    | `I` | Isolate the selected node's subgraph (1-hop neighborhood) |
    | `2` | Expand isolation to 2-hop depth |
    | `3` | Expand isolation to 3-hop depth |

    Do not alter any other section of README.md (Installation, Configuration, MCP Server, Documentation table, License).
  </action>
  <verify>
    <automated>grep -n "| Key | Action |" /Users/ravichillerega/sources/ligamen/README.md && grep -n "Keyboard Shortcuts" /Users/ravichillerega/sources/ligamen/README.md && grep -n "Edge bundling\|edge bundling" /Users/ravichillerega/sources/ligamen/README.md && grep -n "PNG export\|PNG Export" /Users/ravichillerega/sources/ligamen/README.md && grep -n "Subgraph isolation\|subgraph isolation" /Users/ravichillerega/sources/ligamen/README.md && grep -n "What-changed\|what-changed" /Users/ravichillerega/sources/ligamen/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "| Key | Action |" README.md` returns at least 1 match
    - `grep "Esc" README.md` returns a row in the shortcuts table
    - `grep "edge bundling\|Edge bundling" README.md` returns at least 1 match
    - `grep "PNG" README.md` returns at least 1 match
    - `grep "Subgraph\|subgraph" README.md` returns at least 1 match
    - `grep "What-changed\|what-changed\|What changed" README.md` returns at least 1 match
    - `grep "Clickable\|clickable" README.md` returns at least 1 match
    - No existing sections (Installation, Configuration, MCP Server, License) are modified
  </acceptance_criteria>
  <done>README.md contains a Keyboard Shortcuts table with all 6 keys and an updated Graph UI section describing all v5.1 features</done>
</task>

<task type="auto">
  <name>Task 2: Add Graph UI section to docs/commands.md</name>
  <files>docs/commands.md</files>
  <read_first>
    - docs/commands.md — read the full file; it currently has 4 command sections (quality-gate, map, cross-impact, drift). No Graph UI section exists yet. New content goes at the end of the file.
  </read_first>
  <action>
    Append a new top-level section to docs/commands.md titled "## Graph UI — Interactive Controls". This section documents the browser-based graph at http://localhost:37888.

    The section must cover (use subsections with `###` headers for each feature group):

    ### Navigation
    - F — fit all nodes to screen
    - Esc — deselect current node and close the detail panel (also exits isolation mode)
    - / — focus the search input
    Describe these as keyboard shortcuts, formatted as inline code (e.g., `F`).

    ### Clickable Panel Targets
    Explain that clicking a service name in the detail panel's connections list selects that node and pans the canvas to center it, opening its detail panel in place of the previous one.

    ### Subgraph Isolation
    Explain that pressing `I` on a selected node hides all nodes and edges not within 1 hop of that node. Pressing `2` expands to 2-hop depth, pressing `3` expands to 3-hop depth. Pressing `Esc` (or `I` again) exits isolation mode and restores the full graph.

    ### What-Changed Overlay
    Explain that after a re-scan (`/ligamen:map`), nodes and edges introduced or modified in the latest scan are visually highlighted (glow effect or "NEW" badge) so recent changes are immediately visible. Unchanged nodes and edges render normally.

    ### Edge Bundling
    Explain that when two or more edges share the same source and target nodes, they are rendered as a single thicker edge with a numeric badge showing the count. The bundled edge color reflects the dominant protocol type. Clicking a bundled edge opens the detail panel listing all individual connections (protocol, kind, endpoint).

    ### PNG Export
    Explain that clicking the export button (camera icon) in the graph toolbar downloads a PNG screenshot of the current canvas view including all visible nodes and edges.

    Do not alter any existing sections in docs/commands.md.
  </action>
  <verify>
    <automated>grep -n "Graph UI" /Users/ravichillerega/sources/ligamen/docs/commands.md && grep -n "Subgraph Isolation\|subgraph isolation" /Users/ravichillerega/sources/ligamen/docs/commands.md && grep -n "Edge Bundling\|edge bundling" /Users/ravichillerega/sources/ligamen/docs/commands.md && grep -n "PNG Export\|PNG export" /Users/ravichillerega/sources/ligamen/docs/commands.md && grep -n "What-Changed\|what-changed\|What changed" /Users/ravichillerega/sources/ligamen/docs/commands.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Graph UI" docs/commands.md` returns at least 1 match (the new section heading)
    - `grep "Subgraph" docs/commands.md` returns at least 1 match
    - `grep "Edge Bundling\|edge bundling" docs/commands.md` returns at least 1 match
    - `grep "PNG" docs/commands.md` returns at least 1 match
    - `grep "What-[Cc]hanged\|What changed" docs/commands.md` returns at least 1 match
    - `grep "Clickable\|clickable\|connections list" docs/commands.md` returns at least 1 match
    - Existing four command sections (quality-gate, map, cross-impact, drift) are unchanged
    - `grep "/ligamen:quality-gate" docs/commands.md` still returns the existing content
  </acceptance_criteria>
  <done>docs/commands.md contains a "Graph UI — Interactive Controls" section with subsections for all 6 v5.1 capabilities</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Keyboard shortcut table: `grep -A 10 "Keyboard Shortcuts" README.md` — should show a table with F, Esc, /, I, 2, 3 rows
2. Graph UI bullets: `grep -A 20 "Graph UI (http" README.md` — should show all new v5.1 capabilities in the bullet list
3. Commands doc section: `grep -A 50 "Graph UI" docs/commands.md` — should show all 6 subsections
4. No regressions: `grep "quality-gate\|cross-impact\|drift" docs/commands.md | wc -l` — must return at least 4 (existing commands still present)
</verification>

<success_criteria>
- README.md keyboard shortcuts table lists all 6 keys (F, Esc, /, I, 2, 3) with accurate action descriptions
- README.md Graph UI section mentions: clickable panel targets, subgraph isolation, what-changed overlay, edge bundling, PNG export
- docs/commands.md has a "Graph UI" section with subsections for Navigation, Clickable Panel Targets, Subgraph Isolation, What-Changed Overlay, Edge Bundling, and PNG Export
- All existing content in both files is preserved
</success_criteria>

<output>
After completion, create `.planning/phases/58-documentation/58-01-SUMMARY.md` with:
- What was updated in README.md (sections added/changed)
- What was added to docs/commands.md (section structure)
- Grep commands confirming all 6 v5.1 features are documented in both files
</output>

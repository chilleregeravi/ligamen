---
phase: 46-command-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/commands.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - REM-03

must_haves:
  truths:
    - "README.md no longer lists /ligamen:pulse or /ligamen:deploy-verify in any capability list or usage section"
    - "docs/commands.md no longer contains pulse or deploy-verify sections"
    - ".planning/PROJECT.md no longer has validated entries for pulse or deploy-verify"
  artifacts:
    - path: "README.md"
      provides: "Updated on-demand commands list with pulse and deploy-verify removed"
    - path: "docs/commands.md"
      provides: "Updated command reference with pulse and deploy-verify sections removed"
    - path: ".planning/PROJECT.md"
      provides: "Updated validated requirements list without pulse or deploy-verify entries"
  key_links:
    - from: "README.md"
      to: "docs/commands.md"
      via: "link in Documentation table"
      pattern: "docs/commands\\.md"
      note: "Link stays — only pulse/deploy-verify content is removed from both files"
---

<objective>
Remove all references to pulse and deploy-verify from README.md, docs/commands.md, and .planning/PROJECT.md.

Purpose: After the command files are deleted, the documentation must not claim these commands exist. Users reading the README or commands doc should see only the commands that are still present. The PROJECT.md validated requirements list tracks what the plugin has shipped — pulse and deploy-verify are being un-shipped.

Output: Three documentation files updated. No file mentions pulse or deploy-verify as a current capability. The command list in README.md and docs/commands.md reflects only the four remaining on-demand commands.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove pulse and deploy-verify from README.md</name>
  <files>README.md</files>

  <read_first>
    - README.md — read the full file before making any edits. The two entries to remove are in the "On-demand (commands)" bullet list under "What It Does".
  </read_first>

  <action>
    In README.md, locate the "On-demand (commands):" bullet list under the "## What It Does" section. It currently reads:

    ```
    **On-demand (commands):**
    - `/ligamen:quality-gate` — lint, format, test, typecheck
    - `/ligamen:map` — scan repos and build service dependency graph
    - `/ligamen:cross-impact` — find what breaks when you change something
    - `/ligamen:drift` — check dependency version alignment across repos
    - `/ligamen:pulse` — Kubernetes service health check
    - `/ligamen:deploy-verify` — compare expected vs actual cluster state
    ```

    Remove exactly these two lines (do not change any other lines):
    - `- \`/ligamen:pulse\` — Kubernetes service health check`
    - `- \`/ligamen:deploy-verify\` — compare expected vs actual cluster state`

    The resulting list must be:

    ```
    **On-demand (commands):**
    - `/ligamen:quality-gate` — lint, format, test, typecheck
    - `/ligamen:map` — scan repos and build service dependency graph
    - `/ligamen:cross-impact` — find what breaks when you change something
    - `/ligamen:drift` — check dependency version alignment across repos
    ```

    Do not modify any other section of README.md. The Documentation table, Configuration section, MCP Server section, and all other content must remain exactly as-is.
  </action>

  <verify>
    <automated>grep -n "pulse\|deploy-verify" /Users/ravichillerega/sources/ligamen/README.md && echo "FAIL: references remain" || echo "PASS: no pulse/deploy-verify in README.md"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "pulse" README.md` returns 0
    - `grep -c "deploy-verify" README.md` returns 0
    - README.md still contains `/ligamen:quality-gate`, `/ligamen:map`, `/ligamen:cross-impact`, `/ligamen:drift` in the commands list
    - README.md still contains the "## What It Does" section header
    - README.md file size is smaller than before (two bullet lines removed)
  </acceptance_criteria>

  <done>README.md contains no mention of pulse or deploy-verify. The four remaining on-demand commands are still listed correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Remove pulse and deploy-verify sections from docs/commands.md and entries from .planning/PROJECT.md</name>
  <files>docs/commands.md, .planning/PROJECT.md</files>

  <read_first>
    - docs/commands.md — read the full file. The two sections to remove are "## `/ligamen:pulse` — Service Health" (lines 49-56) and "## `/ligamen:deploy-verify` — Deploy Verification" (lines 58-65).
    - .planning/PROJECT.md — read the full file. The two validated entries to remove are under "### Validated":
      - `- ✓ Live service health checking (\`/ligamen:pulse\`) — v1.0`
      - `- ✓ Deploy state verification (\`/ligamen:deploy-verify\`) — v1.0`
  </read_first>

  <action>
    **docs/commands.md:**

    Remove the entire pulse section including its header and blank lines. The section to remove is:

    ```
    ## `/ligamen:pulse` — Service Health

    ```
    /ligamen:pulse                     # all deployments in current context
    /ligamen:pulse staging api         # specific service in staging
    ```

    Requires `kubectl` configured with cluster access.
    ```

    Remove the entire deploy-verify section including its header and blank lines. The section to remove is:

    ```
    ## `/ligamen:deploy-verify` — Deploy Verification

    ```
    /ligamen:deploy-verify                    # check production
    /ligamen:deploy-verify staging --diff     # staging with full diff
    ```

    Requires `kubectl` with read permissions.
    ```

    The resulting docs/commands.md must end after the `/ligamen:drift` section. No trailing blank lines beyond what naturally follows the drift section. Do not modify any other section.

    **Expected final structure of docs/commands.md:**
    - `# Commands` header
    - `/ligamen:quality-gate` section
    - `/ligamen:map` section
    - `/ligamen:cross-impact` section
    - `/ligamen:drift` section
    - (file ends here)

    ---

    **.planning/PROJECT.md:**

    Under "### Validated", remove exactly these two lines:

    ```
    - ✓ Live service health checking (`/ligamen:pulse`) — v1.0
    - ✓ Deploy state verification (`/ligamen:deploy-verify`) — v1.0
    ```

    These are lines 18 and 19 in the current file. Remove them and their trailing newlines so the validated list flows cleanly from `/ligamen:drift` to `Auto-format hook`.

    Do not modify any other line in PROJECT.md. All other validated entries, the "Active" section, the "Context" section, and all other content must remain exactly as-is.
  </action>

  <verify>
    <automated>grep -n "pulse\|deploy-verify" /Users/ravichillerega/sources/ligamen/docs/commands.md && echo "FAIL: references in commands.md" || echo "PASS: commands.md clean"; grep -n "pulse\|deploy-verify" /Users/ravichillerega/sources/ligamen/.planning/PROJECT.md && echo "FAIL: references in PROJECT.md" || echo "PASS: PROJECT.md clean"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "pulse" docs/commands.md` returns 0
    - `grep -c "deploy-verify" docs/commands.md` returns 0
    - `grep -c "pulse" .planning/PROJECT.md` returns 0
    - `grep -c "deploy-verify" .planning/PROJECT.md` returns 0
    - docs/commands.md still contains sections for quality-gate, map, cross-impact, and drift
    - docs/commands.md section count is 4 (was 6) — verify with: `grep -c "^## " docs/commands.md` returns 4
    - .planning/PROJECT.md still contains the validated entry for `/ligamen:drift` (`grep -c "ligamen:drift" .planning/PROJECT.md` returns at least 1)
    - .planning/PROJECT.md still contains all other validated entries (quality-gate, cross-impact, auto-format hook, etc.)
  </acceptance_criteria>

  <done>docs/commands.md lists exactly four commands (quality-gate, map, cross-impact, drift). .planning/PROJECT.md validated list no longer includes pulse or deploy-verify entries.</done>
</task>

</tasks>

<verification>
After both tasks complete, run a combined check across all three files:

```bash
echo "=== README.md ===" && \
grep -n "pulse\|deploy-verify" /Users/ravichillerega/sources/ligamen/README.md && echo "FAIL" || echo "PASS"

echo "=== docs/commands.md ===" && \
grep -n "pulse\|deploy-verify" /Users/ravichillerega/sources/ligamen/docs/commands.md && echo "FAIL" || echo "PASS"

echo "=== .planning/PROJECT.md ===" && \
grep -n "pulse\|deploy-verify" /Users/ravichillerega/sources/ligamen/.planning/PROJECT.md && echo "FAIL" || echo "PASS"

echo "=== commands.md section count (expect 4) ===" && \
grep -c "^## " /Users/ravichillerega/sources/ligamen/docs/commands.md
```

All three grep checks must print PASS. Section count must be 4.
</verification>

<success_criteria>
- README.md: zero occurrences of "pulse" or "deploy-verify"
- docs/commands.md: zero occurrences of "pulse" or "deploy-verify"; exactly 4 `##`-level sections remain
- .planning/PROJECT.md: zero occurrences of "pulse" or "deploy-verify" in the validated list
- All three files are syntactically valid Markdown (no broken headers, no orphaned fenced code blocks)
- git diff shows only line removals in the three target files — no other files touched
</success_criteria>

<output>
After completion, create `.planning/phases/46-command-removal/46-02-SUMMARY.md` with:
- Summary of changes made to each of the three files
- grep verification output confirming zero pulse/deploy-verify references
- docs/commands.md section count confirmation (expect 4)
</output>

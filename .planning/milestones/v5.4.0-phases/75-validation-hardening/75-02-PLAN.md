---
phase: 75-validation-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/ligamen/worker/scan/manager.js
  - plugins/ligamen/worker/scan/manager.test.js
autonomous: true
requirements:
  - SVAL-02

must_haves:
  truths:
    - "getChangedFiles and getCurrentHead use execFileSync — no shell interprets user-controlled strings"
    - "execSync import is removed from manager.js entirely"
    - "A repo path with spaces works correctly without shell quoting"
    - "Existing getChangedFiles and buildScanContext tests pass unchanged"
    - "Validation warnings from parseAgentOutput are logged via slog"
  artifacts:
    - path: "plugins/ligamen/worker/scan/manager.js"
      provides: "execFileSync calls with argument arrays, warning logging"
      contains: "execFileSync"
    - path: "plugins/ligamen/worker/scan/manager.test.js"
      provides: "Path-with-spaces regression test"
  key_links:
    - from: "plugins/ligamen/worker/scan/manager.js"
      to: "node:child_process"
      via: "import { execFileSync }"
      pattern: "execFileSync"
    - from: "plugins/ligamen/worker/scan/manager.js"
      to: "parseAgentOutput result.warnings"
      via: "slog WARN loop after valid parse"
      pattern: "result\\.warnings"
---

<objective>
Replace execSync string interpolation with execFileSync argument arrays in manager.js, eliminating the shell injection surface. Add warning logging for validation warnings from parseAgentOutput.

Purpose: Eliminates the class of shell injection vulnerabilities where user-controlled paths are interpolated into shell command strings. Also surfaces validation warnings from findings.js so operators can see when services are skipped.
Output: Updated manager.js with execFileSync calls; new path-with-spaces test in manager.test.js.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-validation-hardening/75-RESEARCH.md

<interfaces>
From plugins/ligamen/worker/scan/manager.js:

Line 25 — current import (to be changed):
  import { execSync } from "node:child_process";

Line 159 — getChangedFiles signature:
  export function getChangedFiles(repoPath, sinceCommit) { ... }

Line 216 — getCurrentHead signature:
  function getCurrentHead(repoPath) { ... }

Three execSync call sites:
  Line 165: execSync(git -C ${JSON.stringify(repoPath)} ls-files, { encoding: "utf8", stdio: ["pipe", "pipe", "pipe"] })
  Line 174: execSync(git -C ${JSON.stringify(repoPath)} diff --name-status ${sinceCommit} HEAD, { encoding: "utf8", stdio: ["pipe", "pipe", "pipe"] })
  Line 217: execSync(git -C ${JSON.stringify(repoPath)} rev-parse HEAD, { encoding: "utf8" })

Lines 402-414 — parse result handling (no warning logging currently):
  const result = parseAgentOutput(rawResponse);
  if (result.valid === false) {
    slog('WARN', 'scan failed', { repoPath, error: result.error });
    continue;
  }
  // After this block, there is NO code that logs result.warnings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace execSync with execFileSync and add warning logging</name>
  <files>plugins/ligamen/worker/scan/manager.js</files>
  <read_first>
    - plugins/ligamen/worker/scan/manager.js
  </read_first>
  <action>
1. Change the import on line 25 from:
   import { execSync } from "node:child_process";
   to:
   import { execFileSync } from "node:child_process";

2. Replace the three execSync call sites with execFileSync using argument arrays:

Site 1 — getChangedFiles full scan (line 165):
   Change: execSync(git -C ... ls-files, opts)
   To: execFileSync("git", ["-C", repoPath, "ls-files"], { encoding: "utf8", stdio: ["pipe", "pipe", "pipe"] })

Site 2 — getChangedFiles incremental (line 174):
   Change: execSync(git -C ... diff --name-status sinceCommit HEAD, opts)
   To: execFileSync("git", ["-C", repoPath, "diff", "--name-status", sinceCommit, "HEAD"], { encoding: "utf8", stdio: ["pipe", "pipe", "pipe"] })

Site 3 — getCurrentHead (line 217):
   Change: execSync(git -C ... rev-parse HEAD, opts)
   To: execFileSync("git", ["-C", repoPath, "rev-parse", "HEAD"], { encoding: "utf8" })

3. Add validation warning logging in scanRepos. After the if (result.valid === false) block (after line 414), before the persistFindings call, add:

    // Log validation warnings (e.g., skipped services from SVAL-01)
    for (const w of result.warnings) {
      slog('WARN', 'findings validation warning', { repoPath, warning: w });
    }

4. Verify no remaining execSync calls exist in manager.js. The import and all 3 call sites must use execFileSync. Do NOT use shell: true option.
  </action>
  <verify>
    <automated>node --test plugins/ligamen/worker/scan/manager.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "import.*execFileSync.*from.*node:child_process" plugins/ligamen/worker/scan/manager.js returns 1 match
    - grep "import.*execSync[^F].*from.*node:child_process" plugins/ligamen/worker/scan/manager.js returns 0 matches
    - grep -c "execFileSync" plugins/ligamen/worker/scan/manager.js returns at least 4 (1 import + 3 call sites)
    - grep -c 'execSync(' plugins/ligamen/worker/scan/manager.js returns 0
    - grep "result\.warnings" plugins/ligamen/worker/scan/manager.js returns at least 1 match
    - grep "findings validation warning" plugins/ligamen/worker/scan/manager.js returns 1 match
    - node --test plugins/ligamen/worker/scan/manager.test.js exits with 0 failures
  </acceptance_criteria>
  <done>All three execSync call sites replaced with execFileSync argument arrays. execSync import removed. Warning logging added for parseAgentOutput validation warnings. All existing manager tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add path-with-spaces regression test</name>
  <files>plugins/ligamen/worker/scan/manager.test.js</files>
  <read_first>
    - plugins/ligamen/worker/scan/manager.test.js
  </read_first>
  <action>
Add a new test inside the existing getChangedFiles test section. The test creates a temp repo in a directory whose name contains a space, verifying that execFileSync handles it without shell quoting issues.

The test file already imports execSync from node:child_process for its own test helpers (makeTempRepo). This is correct — the test file uses execSync for setup, not production code. Also already imports writeFileSync, mkdtempSync, rmSync, join, tmpdir.

Add this test:

test("getChangedFiles works with spaces in repo path", () => {
  const dir = mkdtempSync(join(tmpdir(), "ligamen test spaces-"));
  try {
    execSync("git init", { cwd: dir, stdio: "pipe" });
    execSync('git config user.email "test@test.com"', { cwd: dir, stdio: "pipe" });
    execSync('git config user.name "Test"', { cwd: dir, stdio: "pipe" });
    writeFileSync(join(dir, "hello.txt"), "world");
    execSync("git add .", { cwd: dir, stdio: "pipe" });
    execSync('git commit -m "add file"', { cwd: dir, stdio: "pipe" });

    const result = getChangedFiles(dir, null);
    assert.ok(!("error" in result), "should not return error for path with spaces");
    assert.ok(result.modified.includes("hello.txt"), "should list hello.txt");
  } finally {
    rmSync(dir, { recursive: true, force: true });
  }
});
  </action>
  <verify>
    <automated>node --test plugins/ligamen/worker/scan/manager.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "spaces in repo path" plugins/ligamen/worker/scan/manager.test.js returns 1 match
    - grep "ligamen test spaces" plugins/ligamen/worker/scan/manager.test.js returns 1 match
    - node --test plugins/ligamen/worker/scan/manager.test.js exits with 0 failures
  </acceptance_criteria>
  <done>Path-with-spaces regression test exists and passes. Proves execFileSync handles directory paths with spaces without shell quoting. All manager tests pass.</done>
</task>

</tasks>

<verification>
- node --test plugins/ligamen/worker/scan/manager.test.js — all tests pass (0 failures)
- grep -c "execSync(" plugins/ligamen/worker/scan/manager.js — returns 0 (no execSync calls)
- grep "execFileSync" plugins/ligamen/worker/scan/manager.js — shows import and 3 call sites
- grep "spaces in repo path" plugins/ligamen/worker/scan/manager.test.js — regression test exists
</verification>

<success_criteria>
- execSync import removed from manager.js entirely
- All 3 call sites use execFileSync with argument arrays (no string interpolation)
- No shell: true option used anywhere
- Path-with-spaces test passes
- Validation warnings from parseAgentOutput are logged via slog('WARN', ...)
- All existing manager.test.js tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/75-validation-hardening/75-02-SUMMARY.md`
</output>

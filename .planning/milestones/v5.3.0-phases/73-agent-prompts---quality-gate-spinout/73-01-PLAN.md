---
phase: 73-agent-prompts---quality-gate-spinout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/ligamen/worker/scan/agent-prompt-service.md
  - plugins/ligamen/worker/scan/agent-prompt-library.md
  - plugins/ligamen/worker/scan/findings.js
  - plugins/ligamen/worker/scan/findings.test.js
autonomous: true
requirements: [AGENT-01, AGENT-02]

must_haves:
  truths:
    - "The agent prompts explicitly state source_file is required on every connection"
    - "When a connection has source_file: null, a structured warning is logged — the scan still completes"
  artifacts:
    - path: "plugins/ligamen/worker/scan/agent-prompt-service.md"
      provides: "Updated service scan prompt with source_file requirement section"
      contains: "source_file is REQUIRED"
    - path: "plugins/ligamen/worker/scan/agent-prompt-library.md"
      provides: "Updated library scan prompt with source_file requirement section"
      contains: "source_file is REQUIRED"
    - path: "plugins/ligamen/worker/scan/findings.js"
      provides: "validateFindings warns when source_file is null on a connection"
      exports: ["validateFindings", "parseAgentOutput"]
  key_links:
    - from: "plugins/ligamen/worker/scan/findings.js"
      to: "plugins/ligamen/worker/scan/manager.js"
      via: "parseAgentOutput called in scanRepos"
      pattern: "parseAgentOutput"
---

<objective>
Add explicit source_file guidance to agent scan prompts and add a structured warning in the findings validator when source_file is null on a connection.

Purpose: Connections without source_file cannot surface file paths in the detail panel or MCP impact responses. The prompt must tell the agent this field is non-optional; the validator must log when it is missing so operators can diagnose agents that ignore the instruction.

Output: Updated agent-prompt-service.md, agent-prompt-library.md, and findings.js with a new warnings array in the validation result.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source_file requirement guidance to agent-prompt-service.md and agent-prompt-library.md</name>
  <files>
    plugins/ligamen/worker/scan/agent-prompt-service.md
    plugins/ligamen/worker/scan/agent-prompt-library.md
  </files>
  <read_first>
    plugins/ligamen/worker/scan/agent-prompt-service.md
    plugins/ligamen/worker/scan/agent-prompt-library.md
  </read_first>
  <behavior>
    - Prompt now has a "source_file Requirement" section that says the field is REQUIRED
    - The section explains the format: "path/to/file.ext:functionName" or "path/to/file.ext:lineN"
    - The section explains when to use "unknown" only as an absolute last resort
    - Example in agent-prompt-service.md already has source_file — section reinforces it
  </behavior>
  <action>
In agent-prompt-service.md, add a new section immediately before the "## Example" heading titled "## source_file Requirement". Content:

```
## source_file Requirement

`source_file` on every connection is **REQUIRED**. This field enables file-level impact analysis. Do not emit `null` unless you have exhaustively searched all source files and found no call site.

**Format:** `"path/to/caller.ts:functionName"` or `"path/to/caller.ts:42"` (line number fallback).

- Use the file that contains the call — not the file that defines the target.
- For SDK connections, use the file that imports and invokes the SDK function.
- `source_file: null` is only valid when the call site is dynamically generated or the source file is minified/bundled with no recoverable origin.
```

Apply the identical section to agent-prompt-library.md, placed immediately before its "## Example" heading.
  </action>
  <verify>
    <automated>grep -c "source_file is \*\*REQUIRED\*\*\|source_file.*REQUIRED" /Users/ravichillerega/sources/ligamen/plugins/ligamen/worker/scan/agent-prompt-service.md && grep -c "source_file is \*\*REQUIRED\*\*\|source_file.*REQUIRED" /Users/ravichillerega/sources/ligamen/plugins/ligamen/worker/scan/agent-prompt-library.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "source_file is \*\*REQUIRED\*\*" agent-prompt-service.md returns 1 match
    - grep "source_file is \*\*REQUIRED\*\*" agent-prompt-library.md returns 1 match
    - The section appears before "## Example" in both files
    - Both files still render as valid markdown (no broken fences)
  </acceptance_criteria>
  <done>Both prompt files contain an explicit source_file requirement section with format guidance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add source_file null warning to findings.js validateFindings</name>
  <files>
    plugins/ligamen/worker/scan/findings.js
    plugins/ligamen/worker/scan/findings.test.js
  </files>
  <read_first>
    plugins/ligamen/worker/scan/findings.js
    plugins/ligamen/worker/scan/findings.test.js
  </read_first>
  <behavior>
    - validateFindings still returns { valid: true, findings } when source_file is null (not a hard error)
    - The returned object also carries { warnings: string[] } alongside findings
    - Each connection with source_file === null appends a warning string: "connection[N].source_file is null — agent did not identify call site"
    - When all connections have non-null source_file, warnings is []
    - parseAgentOutput passes warnings through in the return value
    - Existing tests still pass (the return shape changes but valid+findings are still present)
    - New tests cover: single null source_file produces one warning; two null source_files produce two warnings; non-null source_file produces empty warnings
  </behavior>
  <action>
In findings.js:

1. Update the `ok()` helper to accept an optional warnings array:
   ```js
   function ok(findings, warnings = []) {
     return { valid: true, findings, warnings };
   }
   ```

2. In `validateFindings()`, after the connections loop ends (after the `crossing` validation block), before `return ok(obj)`, add:
   ```js
   // Collect source_file warnings — null is valid but undesirable (AGENT-02 / THE-942)
   const warnings = [];
   for (let i = 0; i < obj.connections.length; i++) {
     if (obj.connections[i].source_file === null) {
       warnings.push(
         `connection[${i}].source_file is null — agent did not identify call site`
       );
     }
   }
   return ok(obj, warnings);
   ```

3. Update the `@typedef` JSDoc for `FindingsResult` to include `warnings`:
   ```js
   * @typedef {{ valid: true, findings: Findings, warnings: string[] } | { valid: false, error: string }} FindingsResult
   ```

In findings.test.js, add three new test cases after the existing `source_file` tests:
- `"warns when connection has source_file: null"` — validates object with one connection where source_file is null, asserts result.valid === true and result.warnings has length 1 containing "connection[0].source_file is null"
- `"warns for each null source_file"` — two connections, both null, expects warnings.length === 2
- `"no warnings when source_file is non-null"` — one connection with source_file set to a string, expects warnings.length === 0
  </action>
  <verify>
    <automated>cd /Users/ravichillerega/sources/ligamen/plugins/ligamen && node --test worker/scan/findings.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "warnings" plugins/ligamen/worker/scan/findings.js returns matches in ok() and validateFindings()
    - All findings.test.js tests pass (exit code 0)
    - grep "source_file is null" findings.test.js returns at least 1 match (new test present)
    - The valid:true return shape now includes warnings array
  </acceptance_criteria>
  <done>validateFindings returns { valid: true, findings, warnings: string[] } where warnings lists connections with null source_file. All existing and new tests pass.</done>
</task>

</tasks>

<verification>
Run from plugins/ligamen directory:

```bash
node --test worker/scan/findings.test.js
grep -c "source_file is \*\*REQUIRED\*\*" worker/scan/agent-prompt-service.md
grep -c "source_file is \*\*REQUIRED\*\*" worker/scan/agent-prompt-library.md
```

All three commands must succeed.
</verification>

<success_criteria>
- agent-prompt-service.md and agent-prompt-library.md each contain a "source_file Requirement" section with "REQUIRED" language
- validateFindings() returns warnings: string[] alongside findings when source_file is null on any connection
- All findings.test.js tests pass including the three new warning tests
- Hard validation (valid: false) is NOT triggered by null source_file — scan always completes
</success_criteria>

<output>
After completion, create `.planning/phases/73-agent-prompts---quality-gate-spinout/73-01-SUMMARY.md`
</output>

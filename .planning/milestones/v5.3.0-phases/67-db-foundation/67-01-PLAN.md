---
phase: 67-db-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/ligamen/worker/db/migrations/009_confidence_enrichment.js
  - plugins/ligamen/worker/db/query-engine.js
  - plugins/ligamen/worker/db/migrations.test.js
  - plugins/ligamen/worker/db/query-engine-upsert.test.js
autonomous: true
requirements:
  - CONF-01
  - CONF-02

must_haves:
  truths:
    - "After migration 009, connections table has confidence TEXT and evidence TEXT columns"
    - "After migration 009, services table has owner TEXT, auth_mechanism TEXT, and db_backend TEXT columns"
    - "After migration 009, schemas and fields tables each have scan_version_id INTEGER column"
    - "Migration 009 is idempotent: running it twice on the same database raises no error"
    - "upsertNodeMetadata(serviceId, view, key, value) writes a row to node_metadata without calling beginScan/endScan"
  artifacts:
    - path: "plugins/ligamen/worker/db/migrations/009_confidence_enrichment.js"
      provides: "Migration 009 DDL"
      exports: ["version", "up"]
    - path: "plugins/ligamen/worker/db/query-engine.js"
      provides: "upsertNodeMetadata() method and prepared statement"
      contains: "upsertNodeMetadata"
  key_links:
    - from: "plugins/ligamen/worker/db/database.js"
      to: "plugins/ligamen/worker/db/migrations/009_confidence_enrichment.js"
      via: "loadMigrationsAsync() auto-discovers all *.js in migrations/"
      pattern: "version = 9"
    - from: "plugins/ligamen/worker/db/query-engine.js"
      to: "node_metadata table"
      via: "_stmtUpsertNodeMetadata prepared in constructor"
      pattern: "INSERT.*node_metadata.*ON CONFLICT"
---

<objective>
Add Migration 009 and the upsertNodeMetadata() method - the DB foundation that all enrichment phases (68-72) depend on.

Purpose: Every subsequent phase in v5.3.0 writes data that needs columns or tables created here. Nothing in the enrichment or confidence pipeline can proceed until these DDL changes exist.

Output:
- migrations/009_confidence_enrichment.js - adds 5 nullable columns across 4 tables, idempotent
- query-engine.js - upsertNodeMetadata(serviceId, view, key, value) method and statement
- Updated test files covering migration 009 facts and upsertNodeMetadata behavior
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@plugins/ligamen/worker/db/migrations/008_actors_metadata.js
@plugins/ligamen/worker/db/migrations/005_scan_versions.js
@plugins/ligamen/worker/db/database.js
@plugins/ligamen/worker/db/query-engine.js
@plugins/ligamen/worker/db/migrations.test.js
@plugins/ligamen/worker/db/query-engine-upsert.test.js
</context>

<interfaces>
<!-- Key patterns extracted from the codebase. Executor uses these directly. -->

From migrations/008_actors_metadata.js - idempotent column addition pattern:
```javascript
export const version = 8;
export function up(db) {
  const connectionsCols = db.prepare("PRAGMA table_info(connections)").all();
  const hasCrossing = connectionsCols.some((c) => c.name === "crossing");
  if (!hasCrossing) {
    db.exec(`ALTER TABLE connections ADD COLUMN crossing TEXT;`);
  }
  db.exec(`CREATE TABLE IF NOT EXISTS ...`);
}
```

From migrations/008_actors_metadata.js - node_metadata table (already created in migration 008):
```sql
CREATE TABLE IF NOT EXISTS node_metadata (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  service_id INTEGER NOT NULL REFERENCES services(id) ON DELETE CASCADE,
  view       TEXT    NOT NULL,
  key        TEXT    NOT NULL,
  value      TEXT,
  source     TEXT,
  updated_at TEXT    NOT NULL DEFAULT (datetime('now')),
  UNIQUE(service_id, view, key)
);
```

From query-engine.js - actor statement initialization pattern with try/catch for backward compat
(lines 366-393). The upsertNodeMetadata statement and method follow the same pattern.

From query-engine.js - upsertField() at line 586:
```javascript
upsertField(fieldData) {
  const result = this._stmtUpsertField.run({ required: 0, ...fieldData });
  return result.lastInsertRowid;
}
```

From database.js - migration auto-discovery:
loadMigrationsAsync() reads all *.js from migrations/ sorted alphabetically.
009_confidence_enrichment.js will be discovered automatically. No changes needed in database.js.

From migrations.test.js - test pattern (inline ESM, assert, openDb with tmp dir):
```javascript
import assert from "assert";
const testRoot = path.join(os.tmpdir(), "ligamen-schema-test-" + Date.now());
fs.mkdirSync(testRoot, { recursive: true });
const { openDb } = await import("./database.js");
const db = openDb(testRoot);
// assertions using PRAGMA table_info() and SELECT
```

From query-engine-upsert.test.js - test pattern (node ESM file, buildDb helper that inlines
schema DDL, creates QueryEngine directly on in-memory DB).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 009 - confidence/evidence/enrichment columns</name>
  <files>
    plugins/ligamen/worker/db/migrations/009_confidence_enrichment.js
    plugins/ligamen/worker/db/migrations.test.js
  </files>
  <behavior>
    - PRAGMA table_info(connections) contains column "confidence" with type "TEXT"
    - PRAGMA table_info(connections) contains column "evidence" with type "TEXT"
    - PRAGMA table_info(services) contains column "owner" with type "TEXT"
    - PRAGMA table_info(services) contains column "auth_mechanism" with type "TEXT"
    - PRAGMA table_info(services) contains column "db_backend" with type "TEXT"
    - PRAGMA table_info(schemas) contains column "scan_version_id"
    - PRAGMA table_info(fields) contains column "scan_version_id"
    - Running migration up() twice on same db raises no error (idempotency)
    - SELECT MAX(version) FROM schema_versions returns 9 after migration runs
  </behavior>
  <read_first>
    - plugins/ligamen/worker/db/migrations/008_actors_metadata.js (idempotent column-add pattern with PRAGMA table_info check)
    - plugins/ligamen/worker/db/migrations/005_scan_versions.js (ALTER TABLE column add pattern)
    - plugins/ligamen/worker/db/migrations.test.js (test structure and PRAGMA assertion pattern)
  </read_first>
  <action>
TDD sequence: write failing assertions in migrations.test.js first, run to confirm failure,
then create 009_confidence_enrichment.js, run again to confirm all pass.

Create plugins/ligamen/worker/db/migrations/009_confidence_enrichment.js:

  export const version = 9;

  export function up(db) {
    // Helper: returns true if the named column exists in the table
    const hasCol = (table, col) =>
      db.prepare("PRAGMA table_info(" + table + ")").all().some((c) => c.name === col);

    // connections: confidence TEXT (agent-emitted; NULL = not yet detected)
    if (!hasCol("connections", "confidence")) {
      db.exec("ALTER TABLE connections ADD COLUMN confidence TEXT;");
    }

    // connections: evidence TEXT (agent-emitted snippet; NULL = not yet detected)
    if (!hasCol("connections", "evidence")) {
      db.exec("ALTER TABLE connections ADD COLUMN evidence TEXT;");
    }

    // services: owner TEXT (CODEOWNERS team handle; NULL = not yet resolved)
    if (!hasCol("services", "owner")) {
      db.exec("ALTER TABLE services ADD COLUMN owner TEXT;");
    }

    // services: auth_mechanism TEXT (jwt, oauth2, api_key, etc.)
    if (!hasCol("services", "auth_mechanism")) {
      db.exec("ALTER TABLE services ADD COLUMN auth_mechanism TEXT;");
    }

    // services: db_backend TEXT (postgresql, mysql, mongodb, etc.)
    if (!hasCol("services", "db_backend")) {
      db.exec("ALTER TABLE services ADD COLUMN db_backend TEXT;");
    }

    // schemas: scan_version_id INTEGER FK (enables stale cleanup in phase 71)
    if (!hasCol("schemas", "scan_version_id")) {
      db.exec("ALTER TABLE schemas ADD COLUMN scan_version_id INTEGER REFERENCES scan_versions(id);");
    }

    // fields: scan_version_id INTEGER FK (enables stale cleanup in phase 71)
    if (!hasCol("fields", "scan_version_id")) {
      db.exec("ALTER TABLE fields ADD COLUMN scan_version_id INTEGER REFERENCES scan_versions(id);");
    }

    // Indexes: CREATE INDEX IF NOT EXISTS is always idempotent - no guard needed
    db.exec("CREATE INDEX IF NOT EXISTS idx_connections_confidence ON connections(confidence);");
    db.exec("CREATE INDEX IF NOT EXISTS idx_schemas_scan_version ON schemas(scan_version_id);");
  }

Append to the bottom of migrations.test.js (after all existing assertions):

  // Migration 009 assertions
  const connCols = db.prepare("PRAGMA table_info(connections)").all().map(c => c.name);
  assert.ok(connCols.includes("confidence"), "connections.confidence column exists");
  assert.ok(connCols.includes("evidence"), "connections.evidence column exists");

  const svcCols = db.prepare("PRAGMA table_info(services)").all().map(c => c.name);
  assert.ok(svcCols.includes("owner"), "services.owner column exists");
  assert.ok(svcCols.includes("auth_mechanism"), "services.auth_mechanism column exists");
  assert.ok(svcCols.includes("db_backend"), "services.db_backend column exists");

  const schemaCols = db.prepare("PRAGMA table_info(schemas)").all().map(c => c.name);
  assert.ok(schemaCols.includes("scan_version_id"), "schemas.scan_version_id column exists");

  const fieldCols = db.prepare("PRAGMA table_info(fields)").all().map(c => c.name);
  assert.ok(fieldCols.includes("scan_version_id"), "fields.scan_version_id column exists");

  const ver9 = db.prepare("SELECT MAX(version) FROM schema_versions").pluck().get();
  assert.ok(ver9 >= 9, "schema version is at least 9 (got " + ver9 + ")");

  // Idempotency: running up() again must not throw
  const { up: up009 } = await import("./migrations/009_confidence_enrichment.js");
  up009(db);
  assert.ok(true, "migration 009 is idempotent");

  console.log("Migration 009 assertions passed.");
  </action>
  <verify>
    <automated>node --input-type=module &lt; /Users/ravichillerega/sources/ligamen/plugins/ligamen/worker/db/migrations.test.js</automated>
  </verify>
  <done>
    - migrations/009_confidence_enrichment.js exists and exports version=9 and up function
    - PRAGMA table_info shows all 7 new columns (confidence, evidence, owner, auth_mechanism, db_backend on connections/services; scan_version_id on schemas and fields)
    - SELECT MAX(version) FROM schema_versions returns 9
    - Running up() twice raises no error
    - All migrations.test.js assertions pass including the new migration 009 block
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QueryEngine.upsertNodeMetadata() method</name>
  <files>
    plugins/ligamen/worker/db/query-engine.js
    plugins/ligamen/worker/db/query-engine-upsert.test.js
  </files>
  <behavior>
    - qe.upsertNodeMetadata(serviceId, "ownership", "owner", "@team-platform") inserts a row to node_metadata
    - Calling again with same (serviceId, view, key) but different value updates the row (ON CONFLICT DO UPDATE)
    - Returns the integer lastInsertRowid of the inserted/updated row
    - Does NOT call beginScan or endScan (scan_versions count stays at 0)
    - On pre-migration-008 db (no node_metadata table), constructor does not throw; method returns null
  </behavior>
  <read_first>
    - plugins/ligamen/worker/db/query-engine.js lines 366-393 (actor statement try/catch pattern - use same pattern for _stmtUpsertNodeMetadata)
    - plugins/ligamen/worker/db/query-engine.js lines 580-592 (upsertField method - follow same structure)
    - plugins/ligamen/worker/db/migrations/008_actors_metadata.js (node_metadata schema: UNIQUE(service_id, view, key))
    - plugins/ligamen/worker/db/query-engine-upsert.test.js (buildDb helper and test structure)
  </read_first>
  <action>
TDD sequence: add failing test assertions first, run to confirm failure, add statement+method,
run again to confirm all pass.

In query-engine.js constructor, after the actor statements try/catch block (after line 393),
add the _stmtUpsertNodeMetadata initialization:

  // --- node_metadata statement (migration 008) ---
  this._stmtUpsertNodeMetadata = null;
  try {
    this._stmtUpsertNodeMetadata = db.prepare(`
      INSERT INTO node_metadata (service_id, view, key, value, updated_at)
      VALUES (@service_id, @view, @key, @value, datetime('now'))
      ON CONFLICT(service_id, view, key) DO UPDATE SET
        value = excluded.value,
        updated_at = excluded.updated_at
    `);
  } catch {
    // node_metadata table not present (pre-migration-008 db)
    this._stmtUpsertNodeMetadata = null;
  }

In query-engine.js, after upsertField() method (after line 592), add:

  /**
   * Inserts or updates a metadata key/value for a service in the given view.
   *
   * MUST NOT call beginScan/endScan - enrichment passes write metadata after
   * the scan bracket closes. This method creates no scan_versions rows.
   *
   * @param {number} serviceId - services.id
   * @param {string} view - Logical namespace (e.g., "ownership", "auth", "db")
   * @param {string} key - Key within the view (e.g., "owner", "auth_mechanism")
   * @param {string|null} value - Value to store (null clears the value)
   * @returns {number|null} lastInsertRowid, or null if node_metadata table absent
   */
  upsertNodeMetadata(serviceId, view, key, value) {
    if (!this._stmtUpsertNodeMetadata) return null;
    const result = this._stmtUpsertNodeMetadata.run({
      service_id: serviceId,
      view,
      key,
      value: value ?? null,
    });
    return result.lastInsertRowid;
  }

In query-engine-upsert.test.js, append after existing test assertions:

  // Ensure node_metadata table exists in the in-memory db (migration 008 schema)
  db.exec(`
    CREATE TABLE IF NOT EXISTS node_metadata (
      id         INTEGER PRIMARY KEY AUTOINCREMENT,
      service_id INTEGER NOT NULL REFERENCES services(id) ON DELETE CASCADE,
      view       TEXT    NOT NULL,
      key        TEXT    NOT NULL,
      value      TEXT,
      source     TEXT,
      updated_at TEXT    NOT NULL DEFAULT (datetime('now')),
      UNIQUE(service_id, view, key)
    );
  `);

  // Re-instantiate QueryEngine so _stmtUpsertNodeMetadata is prepared against updated schema
  const qeNm = new QueryEngine(db);

  // Insert
  const nmRowId = qeNm.upsertNodeMetadata(svcId, "ownership", "owner", "@team-platform");
  assert.ok(typeof nmRowId === "number" && nmRowId > 0, "upsertNodeMetadata returns rowId on insert");

  // Read back
  const nmRow = db.prepare("SELECT value FROM node_metadata WHERE service_id = ? AND view = ? AND key = ?")
    .get(svcId, "ownership", "owner");
  assert.strictEqual(nmRow.value, "@team-platform", "node_metadata value written correctly");

  // ON CONFLICT DO UPDATE
  qeNm.upsertNodeMetadata(svcId, "ownership", "owner", "@team-security");
  const nmUpdated = db.prepare("SELECT value FROM node_metadata WHERE service_id = ? AND view = ? AND key = ?")
    .get(svcId, "ownership", "owner");
  assert.strictEqual(nmUpdated.value, "@team-security", "upsertNodeMetadata updates existing value");

  // No scan bracket created
  const scanCount = db.prepare("SELECT COUNT(*) FROM scan_versions").pluck().get();
  assert.strictEqual(scanCount, 0, "upsertNodeMetadata does not create scan_versions rows");

  console.log("upsertNodeMetadata tests passed.");

Note: svcId must be a valid service id already inserted in the test buildDb helper. Check the
existing test file to find the variable name used for the test service ID and use that.
  </action>
  <verify>
    <automated>node /Users/ravichillerega/sources/ligamen/plugins/ligamen/worker/db/query-engine-upsert.test.js</automated>
  </verify>
  <done>
    - QueryEngine has a upsertNodeMetadata(serviceId, view, key, value) method
    - Insert writes to node_metadata table, returns rowId
    - Second call with same (serviceId, view, key) updates value, no duplicate row
    - COUNT(*) FROM scan_versions remains 0 after upsertNodeMetadata calls
    - _stmtUpsertNodeMetadata is initialized in constructor with try/catch (backward compat)
    - All query-engine-upsert.test.js assertions pass
  </done>
</task>

</tasks>

<verification>
Run both test files to confirm the full Phase 67 contract:

  node --input-type=module < plugins/ligamen/worker/db/migrations.test.js
  node plugins/ligamen/worker/db/query-engine-upsert.test.js

Grep-verify the new method and migration exist:

  grep -n "upsertNodeMetadata" plugins/ligamen/worker/db/query-engine.js
  grep -n "version = 9" plugins/ligamen/worker/db/migrations/009_confidence_enrichment.js
  grep -n "confidence\|evidence\|owner\|auth_mechanism\|db_backend" plugins/ligamen/worker/db/migrations/009_confidence_enrichment.js
</verification>

<success_criteria>
- migrations/009_confidence_enrichment.js exists, exports version=9 and up()
- connections table gains confidence TEXT and evidence TEXT
- services table gains owner TEXT, auth_mechanism TEXT, db_backend TEXT
- schemas and fields tables gain scan_version_id INTEGER
- Migration is idempotent (calling up() twice raises no error)
- QueryEngine.upsertNodeMetadata(serviceId, view, key, value) is a callable method
- Method performs INSERT ... ON CONFLICT DO UPDATE on node_metadata
- No scan_versions rows created by upsertNodeMetadata
- Both test files pass without errors
</success_criteria>

<output>
After completion, create .planning/phases/67-db-foundation/67-01-SUMMARY.md
</output>

---
phase: 81-data-integrity-port
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/ligamen/worker/db/query-engine-mcp-enrichment.test.js
  - plugins/ligamen/worker/db/query-engine-graph.test.js
  - plugins/ligamen/scripts/session-start.sh
autonomous: true
requirements: [DINT-03, DINT-04]
must_haves:
  truths:
    - "node_metadata enrichment tests use canonical view names (ownership/security/infra) matching production queries"
    - "session-start.sh detects version mismatch and restarts worker when running with stale code"
  artifacts:
    - path: "plugins/ligamen/worker/db/query-engine-mcp-enrichment.test.js"
      provides: "KEY_TO_VIEW mapping for seedMeta helper"
      contains: "KEY_TO_VIEW"
    - path: "plugins/ligamen/worker/db/query-engine-graph.test.js"
      provides: "Corrected view name in inline insert"
      contains: "ownership"
    - path: "plugins/ligamen/scripts/session-start.sh"
      provides: "Version mismatch detection and worker restart"
      contains: "_needs_restart"
  key_links:
    - from: "plugins/ligamen/worker/db/query-engine-mcp-enrichment.test.js"
      to: "production query filter"
      via: "KEY_TO_VIEW maps keys to ownership/security/infra views"
      pattern: "KEY_TO_VIEW"
    - from: "plugins/ligamen/scripts/session-start.sh"
      to: "/api/version endpoint"
      via: "curl version check against package.json"
      pattern: "_running_version.*_installed_version"
---

<objective>
Port DINT-03 (test view name mismatch) and DINT-04 (worker version restart) fixes from the plugin cache to the source repo.

Purpose: Test files must use canonical view names matching production queries, and session-start.sh must restart stale workers.
Output: Fixed test files + version-aware session-start.sh
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-data-integrity-port/81-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port DINT-03 view name fixes to enrichment + graph test files</name>
  <files>plugins/ligamen/worker/db/query-engine-mcp-enrichment.test.js, plugins/ligamen/worker/db/query-engine-graph.test.js</files>
  <read_first>
    - plugins/ligamen/worker/db/query-engine-mcp-enrichment.test.js (current source — see seedMeta helper around line 76)
    - ~/.claude/plugins/cache/ligamen/ligamen/5.4.0/worker/db/query-engine-mcp-enrichment.test.js (fixed version)
    - plugins/ligamen/worker/db/query-engine-graph.test.js (current source — see inline insert around line 304)
    - ~/.claude/plugins/cache/ligamen/ligamen/5.4.0/worker/db/query-engine-graph.test.js (fixed version)
  </read_first>
  <action>
Diff each cache file against its source counterpart and apply exact changes.

**query-engine-mcp-enrichment.test.js:**

Around line 76, replace the simple seedMeta comment:
```js
/** Insert a node_metadata row. */
```
with:
```js
/** Insert a node_metadata row.
 *  View must match the production filter: enrichment, security, infra, ownership.
 *  Route keys to their canonical view automatically. */
const KEY_TO_VIEW = { owner: "ownership", owners: "ownership", auth_mechanism: "security", db_backend: "infra" };
```

After that constant, add a line inside the seedMeta function to derive the view:
```js
const view = KEY_TO_VIEW[key] || "enrichment";
```

Change the INSERT statement's hardcoded `"scan"` to use the `view` variable:
```js
).run(serviceId, view, key, value);
```
replacing:
```js
).run(serviceId, "scan", key, value);
```

**query-engine-graph.test.js:**

Around line 304, change the inline INSERT from `"scan"` to `"ownership"`:
```js
).run(svcAId, "ownership", "owner", "team-alpha", "scan", new Date().toISOString());
```
replacing:
```js
).run(svcAId, "scan", "owner", "team-alpha", "scan", new Date().toISOString());
```
  </action>
  <verify>
    <automated>cd /Users/ravichillerega/sources/ligamen/plugins/ligamen && diff worker/db/query-engine-mcp-enrichment.test.js ~/.claude/plugins/cache/ligamen/ligamen/5.4.0/worker/db/query-engine-mcp-enrichment.test.js | head -5; diff worker/db/query-engine-graph.test.js ~/.claude/plugins/cache/ligamen/ligamen/5.4.0/worker/db/query-engine-graph.test.js | head -5; node --test worker/db/query-engine-mcp-enrichment.test.js 2>&1 | tail -3; node --test worker/db/query-engine-graph.test.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `diff` of enrichment test file against cache produces NO output
    - `diff` of graph test file against cache produces NO output
    - grep for "KEY_TO_VIEW" in enrichment test returns a match
    - grep for `"scan"` as a view parameter in enrichment test's seedMeta returns NO matches
    - grep for `"ownership"` in graph test line ~304 returns a match
    - Both test files pass: `node --test worker/db/query-engine-mcp-enrichment.test.js` and `node --test worker/db/query-engine-graph.test.js`
  </acceptance_criteria>
  <done>Both test files match cache, view names are canonical (ownership/security/infra), all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Port DINT-04 version mismatch restart to session-start.sh</name>
  <files>plugins/ligamen/scripts/session-start.sh</files>
  <read_first>
    - plugins/ligamen/scripts/session-start.sh (current source — see "worker already running" branch around line 55-59)
    - ~/.claude/plugins/cache/ligamen/ligamen/5.4.0/scripts/session-start.sh (fixed version with version check)
  </read_first>
  <action>
Diff the cache file against the source file and apply the exact changes.

In the "worker already running" branch (around line 58), replace the single line:
```bash
WORKER_STATUS=$(worker_status_line 2>/dev/null || echo "")
```
with the full version mismatch detection block from the cache file. The new code:

1. Sets `_needs_restart=false`, `_installed_version=""`, `_running_version=""`
2. Reads installed version from `${CLAUDE_PLUGIN_ROOT}/package.json` using jq
3. Reads worker port from `${LIGAMEN_DATA_DIR:-$HOME/.ligamen}/worker.port`
4. Curls `http://127.0.0.1:${_port}/api/version` to get running version
5. If both versions exist and differ (and running != "unknown"), sets `_needs_restart=true`
6. If restart needed: stops old worker via worker-stop.sh, starts new one via worker_start_background, sets status to "restarted (old -> new)"
7. If no restart needed: falls through to original `WORKER_STATUS=$(worker_status_line ...)` line

Also add the INTG-02 comment block at the top of the new code.
  </action>
  <verify>
    <automated>cd /Users/ravichillerega/sources/ligamen/plugins/ligamen && diff scripts/session-start.sh ~/.claude/plugins/cache/ligamen/ligamen/5.4.0/scripts/session-start.sh | head -5; echo "Exit: $?"; bash -n scripts/session-start.sh && echo "Syntax OK"</automated>
  </verify>
  <acceptance_criteria>
    - `diff` of session-start.sh against cache produces NO output (files identical)
    - grep for `_needs_restart` in session-start.sh returns matches
    - grep for `/api/version` in session-start.sh returns a match
    - `bash -n scripts/session-start.sh` exits 0 (valid syntax)
  </acceptance_criteria>
  <done>session-start.sh matches cache, version mismatch detection and worker restart logic is in place, shell syntax is valid</done>
</task>

</tasks>

<verification>
1. `diff plugins/ligamen/worker/db/query-engine-mcp-enrichment.test.js ~/.claude/plugins/cache/ligamen/ligamen/5.4.0/worker/db/query-engine-mcp-enrichment.test.js` — no differences
2. `diff plugins/ligamen/worker/db/query-engine-graph.test.js ~/.claude/plugins/cache/ligamen/ligamen/5.4.0/worker/db/query-engine-graph.test.js` — no differences
3. `diff plugins/ligamen/scripts/session-start.sh ~/.claude/plugins/cache/ligamen/ligamen/5.4.0/scripts/session-start.sh` — no differences
4. `cd plugins/ligamen && node --test worker/db/query-engine-mcp-enrichment.test.js` — passes
5. `cd plugins/ligamen && node --test worker/db/query-engine-graph.test.js` — passes
6. `bash -n plugins/ligamen/scripts/session-start.sh` — syntax OK
</verification>

<success_criteria>
- All three files match their cache counterparts exactly
- Enrichment and graph tests pass with canonical view names
- session-start.sh has valid syntax and contains version mismatch restart logic
</success_criteria>

<output>
After completion, create `.planning/phases/81-data-integrity-port/81-02-SUMMARY.md`
</output>

---
phase: 80-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/ligamen/worker/mcp/server.js
  - plugins/ligamen/worker/mcp/server.test.js
  - plugins/ligamen/worker/db/pool.js
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "Passing ../../../etc/passwd as a project identifier to resolveDb returns null and never opens a file outside ~/.ligamen/projects/"
    - "Passing a 12-char hex hash that contains encoded traversal (e.g. via symlink or path.join trickery) resolves only within the projects directory"
    - "Absolute paths with .. segments anywhere in the string are rejected even after normalization"
  artifacts:
    - path: "plugins/ligamen/worker/mcp/server.js"
      provides: "Hardened resolveDb with path.resolve + startsWith base-dir check"
      contains: "path.resolve"
    - path: "plugins/ligamen/worker/db/pool.js"
      provides: "Base directory validation in getQueryEngineByHash"
      contains: "dataDir"
    - path: "plugins/ligamen/worker/mcp/server.test.js"
      provides: "Path traversal rejection tests"
      contains: "traversal"
  key_links:
    - from: "plugins/ligamen/worker/mcp/server.js"
      to: "plugins/ligamen/worker/db/pool.js"
      via: "resolveDb calls getQueryEngine/getQueryEngineByHash"
      pattern: "getQueryEngine"
---

<objective>
Harden resolveDb() and the DB pool against path traversal attacks so that no project identifier (absolute path, hash, or name) can open a database file outside the configured ~/.ligamen/projects/ directory.

Purpose: SEC-01 — prevent malicious MCP tool callers from reading arbitrary SQLite files via crafted project identifiers.
Output: Hardened resolveDb() + pool functions with comprehensive test coverage for traversal vectors.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-security-hardening/80-CONTEXT.md
@plugins/ligamen/worker/mcp/server.js
@plugins/ligamen/worker/db/pool.js
@plugins/ligamen/worker/mcp/server.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden resolveDb() and pool against path traversal</name>
  <files>plugins/ligamen/worker/mcp/server.js, plugins/ligamen/worker/db/pool.js, plugins/ligamen/worker/mcp/server.test.js</files>
  <read_first>
    plugins/ligamen/worker/mcp/server.js (lines 72-89 — current resolveDb)
    plugins/ligamen/worker/db/pool.js (lines 1-60 — dataDir, projectHashDir, getQueryEngine; lines 158-180 — getQueryEngineByHash)
    plugins/ligamen/worker/mcp/server.test.js (existing test patterns)
  </read_first>
  <behavior>
    - resolveDb("../../../etc/passwd") returns null
    - resolveDb("/tmp/../../../etc/passwd") returns null
    - resolveDb("....//....//etc/passwd") returns null
    - resolveDb with a valid absolute path that resolves inside ~/.ligamen/projects proceeds normally
    - resolveDb with a 12-char hex hash like "abcdef012345" still works (no false positives)
    - getQueryEngineByHash("../../etc") returns null — hash input validated to hex-only before path.join
  </behavior>
  <action>
    In server.js resolveDb():
    1. Replace the `if (project.includes('..')) return null;` guard (line 80) with proper normalization:
       ```
       const normalized = path.resolve(project);
       const baseDir = path.join(os.homedir(), '.ligamen', 'projects');
       if (!normalized.startsWith(baseDir + path.sep) && normalized !== baseDir) return null;
       ```
    2. Add `import os from 'node:os';` at the top if not already present.
    3. For the hex hash branch (line 84): the regex `/^[0-9a-f]{12}$/` already prevents traversal since it only allows hex chars. No change needed — but add a comment: `// Safe: regex rejects non-hex chars including '.' and '/'`

    In pool.js getQueryEngineByHash():
    4. After computing `const dbPath = path.join(dir, "impact-map.db");` (line 160), add base-dir validation:
       ```
       const projectsDir = path.join(dataDir, "projects");
       if (!path.resolve(dir).startsWith(projectsDir + path.sep)) return null;
       ```
       This guards against any future caller passing a crafted hash containing path separators.

    In server.test.js, add a new describe block "resolveDb path traversal (SEC-01)":
    5. Test: `resolveDb("../../../etc/passwd")` returns null
    6. Test: `resolveDb("/tmp/../../../etc/passwd")` returns null (absolute with ..)
    7. Test: `resolveDb("....//....//etc")` returns null (double-dot variant)
    8. Test: `resolveDb(undefined)` still returns a QueryEngine or null (not an error)
    9. Test: `resolveDb("abcdef012345")` calls getQueryEngineByHash (mock or verify no crash)

    Note: resolveDb imports getQueryEngine/getQueryEngineByHash from pool.js. The tests call resolveDb directly — for traversal tests, returning null is the expected safe behavior since no DB exists at those paths. The key assertion is that no error is thrown and null is returned.
  </action>
  <verify>
    <automated>cd plugins/ligamen && node --test worker/mcp/server.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - server.js contains `path.resolve(project)` in resolveDb function
    - server.js contains `.startsWith(` for base directory validation
    - server.js contains `import os from 'node:os'` or `import { homedir } from 'node:os'`
    - server.js does NOT contain `project.includes('..')` as the sole traversal guard
    - pool.js getQueryEngineByHash contains `.startsWith(` for base directory validation
    - server.test.js contains `traversal` in a describe or test name
    - server.test.js contains `../../../etc/passwd` as a test input
    - `node --test worker/mcp/server.test.js` exits 0
  </acceptance_criteria>
  <done>resolveDb rejects all path traversal vectors and returns null. Pool hash lookup validates base directory. All tests pass including new SEC-01 traversal tests.</done>
</task>

</tasks>

<verification>
Run the full server test suite:
```bash
cd plugins/ligamen && node --test worker/mcp/server.test.js
```
All tests pass, including the new path traversal tests.

Manual smoke test:
```bash
cd plugins/ligamen && node -e "
  import { resolveDb } from './worker/mcp/server.js';
  console.log('traversal:', resolveDb('../../../etc/passwd'));
  console.log('abs traversal:', resolveDb('/tmp/../../../etc/passwd'));
"
```
Both should print `null`.
</verification>

<success_criteria>
- resolveDb("../../../etc/passwd") returns null
- resolveDb("/tmp/../../../etc/passwd") returns null
- No path outside ~/.ligamen/projects/ is ever opened
- All existing server.test.js tests continue to pass
- New traversal tests cover at least 3 attack vectors
</success_criteria>

<output>
After completion, create `.planning/phases/80-security-hardening/80-01-SUMMARY.md`
</output>

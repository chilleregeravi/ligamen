---
phase: 126-auth-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/arcanon/worker/hub-sync/client.test.js
  - plugins/arcanon/worker/hub-sync/whoami.test.js
  - plugins/arcanon/worker/hub-sync/integration.test.js
autonomous: true
requirements:
  - AUTH-10
tags:
  - testing
  - auth
  - hub-sync
  - regression-pin

must_haves:
  truths:
    - "client.test.js asserts X-Org-Id lands on every successful upload request"
    - "client.test.js asserts missing orgId throws HubError BEFORE any fetch is issued"
    - "client.test.js asserts each of the 7 server error codes (missing_x_org_id, invalid_x_org_id, insufficient_scope, key_not_authorized_for_org, not_a_member, forbidden_scan, invalid_key) produces its own user-message-bearing HubError with .code populated"
    - "client.test.js asserts a happy-path 202 returns scan_upload_id"
    - "whoami.test.js asserts getKeyInfo returns parsed { user_id, key_id, scopes, grants }"
    - "whoami.test.js asserts auth-class HTTP errors (401/403) throw AuthError"
    - "whoami.test.js asserts transport (network / 5xx) errors throw HubError"
    - "integration.test.js asserts /arcanon:login --org-id <uuid> round-trips through storeCredentials -> resolveCredentials with orgId returned"
    - "integration.test.js asserts /arcanon:login without --org-id calls whoami and auto-selects on N=1, prompts on N>1, fails on N=0"
    - "integration.test.js asserts precedence: per-repo hub.org_id beats ARCANON_ORG_ID beats default_org_id from ~/.arcanon/config.json"
    - "npm test exits 0; no new pre-existing-mock carryforwards relative to v0.1.4 baseline (1 historical v0.1.2 mock failure preserved)"
  artifacts:
    - path: "plugins/arcanon/worker/hub-sync/client.test.js"
      provides: "Extended uploadScan test suite with X-Org-Id, fail-fast, and 7 error-code assertions"
      contains: "X-Org-Id"
    - path: "plugins/arcanon/worker/hub-sync/whoami.test.js"
      provides: "New file pinning getKeyInfo contract"
      contains: "getKeyInfo"
    - path: "plugins/arcanon/worker/hub-sync/integration.test.js"
      provides: "Extended integration suite with login-flow + precedence tests"
      contains: "precedence"
  key_links:
    - from: "client.test.js"
      to: "client.js uploadScan"
      via: "fetchImpl mock injection + opts.orgId param"
      pattern: "uploadScan.*orgId"
    - from: "whoami.test.js"
      to: "whoami.js getKeyInfo"
      via: "fetchImpl mock injection against GET /api/v1/auth/whoami"
      pattern: "getKeyInfo.*fetchImpl"
    - from: "integration.test.js"
      to: "auth.js storeCredentials/resolveCredentials + manager.js _readHubConfig"
      via: "tmpdir HOME swap + ARCANON_ORG_ID env stub + arcanon.config.json fixture"
      pattern: "process\\.env\\.HOME|ARCANON_ORG_ID"
---

<objective>
Pin the Phase 124-125 auth contract — `X-Org-Id` header, fail-fast on missing orgId, error-code-to-message mapping, login flow branches, and the per-repo > env > home-config resolution precedence — with an executable test suite that fails closed on any regression.

Purpose: AUTH-10 is the regression gate for the entire AUTH-01..09 surface. Without these tests, a future refactor can silently strip the `X-Org-Id` header, drop a 7-code branch, or invert the resolution precedence. With these tests, `npm test` is the single green/red signal that the auth contract still holds.

Output: Three test files committed (one new: `whoami.test.js`; two extended: `client.test.js`, `integration.test.js`); `npm test` exits 0 on clean tree with the new tests included; total node test count climbs from the v0.1.4 baseline of 774 passing / 1 pre-existing failure to roughly 790 passing / 1 pre-existing failure (exact addition is ~16 new test cases — see Task 1/2/3 breakdown).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/PREDECESSOR-SURFACE.md

# AUTH contract sources — Phase 124 + 125 land these BEFORE this phase runs:
@plugins/arcanon/worker/hub-sync/auth.js
@plugins/arcanon/worker/hub-sync/client.js
@plugins/arcanon/worker/hub-sync/whoami.js
@plugins/arcanon/worker/hub-sync/index.js
@plugins/arcanon/worker/scan/manager.js

# Existing tests to extend (do NOT replace — preserve every existing assertion):
@plugins/arcanon/worker/hub-sync/client.test.js
@plugins/arcanon/worker/hub-sync/integration.test.js

<interfaces>
<!-- These contracts are LOCKED by Phases 124-125. The executor must NOT redefine them; -->
<!-- the executor's job is to write tests that pin them. If any contract below does NOT -->
<!-- match the actual Phase 124/125 implementation when this phase runs, STOP and ask -->
<!-- before writing tests that will pass against a fiction. -->

# uploadScan signature (post Phase 124 AUTH-01):
uploadScan(payload, {
  apiKey: string,        # required
  hubUrl: string,        # required
  orgId: string,         # NEW required — missing throws HubError(status:400, code:"missing_org_id") BEFORE fetch
  fetchImpl?: function,  # injectable for tests
  attempts?: number,
  backoffsMs?: number[],
  timeoutMs?: number,
  log?: function,
}) -> Promise<{ scan_upload_id, status, ... }>

# HubError shape (post Phase 124 AUTH-08):
class HubError extends Error {
  status: number | null
  retriable: boolean
  body: object | null
  attempts: number
  code: string | null    # NEW — populated from RFC 7807 body.code when present, null otherwise
}

# getKeyInfo signature (NEW in Phase 124 AUTH-02):
getKeyInfo(apiKey, hubUrl, { fetchImpl? } = {}) -> Promise<{
  user_id: string,
  key_id: string,
  scopes: string[],
  grants: Array<{ org_id: string, role: string }>,
}>
# Throws AuthError on 401/403; throws HubError on network errors and 5xx.

# resolveCredentials signature (post Phase 124 AUTH-03):
resolveCredentials(opts = {}) -> {
  apiKey: string,
  hubUrl: string,
  orgId: string,         # NEW — required; missing throws AuthError naming all 3 sources
  source: "explicit" | "env" | "home-config" | "repo-config",
}
# orgId precedence: opts.orgId > ARCANON_ORG_ID env > ~/.arcanon/config.json default_org_id
# When called from manager.js _readHubConfig, the per-repo cfg.hub.org_id is threaded in
# via opts.orgId — so per-repo beats env beats machine default.

# storeCredentials signature (post Phase 124 AUTH-04):
storeCredentials(apiKey, { hubUrl?, orgId? } = {}) -> string  # path to written config
# Persists { api_key, hub_url, default_org_id } via spread-merge — preserves unknown keys.

# Server error codes the plugin parses (Phase 124 AUTH-08, 7 codes total):
# - missing_x_org_id    -> "Upload missing X-Org-Id header — re-run /arcanon:sync after /arcanon:login --org-id <uuid>"
# - invalid_x_org_id    -> "X-Org-Id is not a valid UUID — check ~/.arcanon/config.json default_org_id"
# - insufficient_scope  -> "Your API key lacks the required scope for scan upload — request scan:write"
# - key_not_authorized_for_org -> "Key not authorized for this org — run /arcanon:login --org-id <uuid> to switch"
# - not_a_member        -> "You are not a member of this org"
# - forbidden_scan      -> "Scan upload forbidden by hub policy"
# - invalid_key         -> "API key is invalid or revoked — run /arcanon:login arc_… with a fresh key"
# Exact wording is per Phase 125 AUTH-08; tests assert key code → message
# pairing using a substring assertion (NOT a strict-equality assertion) so that
# minor copy edits to the Phase-125 messages don't fail this regression suite.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend client.test.js — X-Org-Id header, fail-fast, 7 error codes, success path</name>
  <files>plugins/arcanon/worker/hub-sync/client.test.js</files>
  <behavior>
    Preserve every one of the 7 existing assertions in client.test.js (they pin
    the v0.1.4 contract: Bearer auth, /api/v1/scans/upload, 409 idempotent,
    503 retry, 429 Retry-After, 422 fail-fast, ECONNRESET retriable, apiKey/hubUrl
    presence). Add NEW assertions on top:

    - **Test A: X-Org-Id header lands on success.** mkPayload + fetchImpl that
      captures `init.headers["X-Org-Id"]`; uploadScan called with
      `{ apiKey, hubUrl, orgId: "00000000-0000-0000-0000-000000000001", fetchImpl }`;
      assert `captured.init.headers["X-Org-Id"] === "00000000-0000-0000-0000-000000000001"`.
      Also re-assert Authorization Bearer + Content-Type to prove the new header is
      additive, not replacing.

    - **Test B: Missing orgId throws BEFORE fetch.** fetchImpl that throws
      `new Error("FETCH_SHOULD_NOT_BE_CALLED")` if invoked; call uploadScan
      WITHOUT orgId; assert.rejects with HubError where `err.status === 400`,
      `err.code === "missing_org_id"`, and the rejection message references "orgId"
      or "X-Org-Id". Critically — fetchImpl was never invoked (assert via a
      `let fetchCalled = false; fetchImpl = () => { fetchCalled = true; ... }`
      pattern; assert `fetchCalled === false` after the rejection).

    - **Test C: 7 server error codes each produce a distinct HubError.** Single
      table-driven test using a `for (const { code, status, expectedSubstring } of CASES)`
      loop. CASES list exactly:
        { code: "missing_x_org_id",          status: 400, expectedSubstring: "X-Org-Id" }
        { code: "invalid_x_org_id",          status: 400, expectedSubstring: "UUID" }
        { code: "insufficient_scope",        status: 403, expectedSubstring: "scope" }
        { code: "key_not_authorized_for_org",status: 403, expectedSubstring: "not authorized" }
        { code: "not_a_member",              status: 403, expectedSubstring: "member" }
        { code: "forbidden_scan",            status: 403, expectedSubstring: "forbidden" }
        { code: "invalid_key",               status: 401, expectedSubstring: "invalid" }
      For each: fetchImpl returns
      `jsonResponse(status, { type: "...", title: "auth error", status, detail: "x", code })`;
      assert.rejects HubError where `err.status === status`, `err.code === code`,
      and `err.message.toLowerCase()` includes `expectedSubstring.toLowerCase()`.
      Use substring matching (NOT exact equality) so Phase 125 copy tweaks don't
      break this regression suite. Use `backoffsMs: [0,0,0]` to keep test fast.

    - **Test D: Unknown error code falls back to body.title.** fetchImpl returns
      `jsonResponse(403, { title: "novel auth condition", code: "future_code_we_dont_know" })`;
      assert HubError where `err.code === "future_code_we_dont_know"` AND
      `err.message` contains "novel auth condition" (the title fallback at
      client.js:164 must remain). This pins forward-compat per risk C4.

    - **Test E: Happy path returns scan_upload_id.** Already covered by existing
      Test 1, but add an explicit re-assertion in this expanded suite for clarity:
      `assert.equal(res.scan_upload_id, "abc")` with orgId threaded.

    Implementation rule: ALWAYS thread `orgId: "00000000-0000-0000-0000-000000000001"`
    into the existing 7 tests too — otherwise after Phase 124 they will all start
    failing with `missing_org_id`. Update mkPayload's surrounding call sites at
    test.js:34, 49, 65, 84, 103, 127, 145 (and any others where uploadScan is
    invoked) to pass `orgId`. Keep Test 7 (`apiKey/hubUrl validates`) as-is and
    extend it with a third assertion: `uploadScan({apiKey, hubUrl, fetchImpl})`
    without orgId rejects with /orgId is required|missing_org_id/.

    Use the existing `jsonResponse(status, body, headers)` helper. Use
    `backoffsMs: [0,0,0]` everywhere so the suite runs fast (current tests
    already do this).
  </behavior>
  <action>
    1. Read the current `plugins/arcanon/worker/hub-sync/client.test.js` (153 lines).
    2. Thread `orgId: "00000000-0000-0000-0000-000000000001"` into every existing
       `uploadScan(...)` call site (7 of them; the 8th — Test 7's apiKey/hubUrl
       presence test — gets a third sub-case for missing orgId).
    3. Add Tests A, B, C, D, E in order at the end of the file. Test C is the
       table-driven one (single `test()` block with internal loop). Total new
       `test(...)` blocks: 5 (A, B, C, D, E). Net new assertions: ~16
       (A=3, B=3, C=7×3=21 [counted as 1 test block but 21 assertions], D=2, E=1).
    4. Run `npm test --prefix plugins/arcanon -- --test-name-pattern client.test`
       once and confirm green BEFORE moving to Task 2 (this isolates client.js
       failures from whoami/integration failures).
    5. Do NOT modify client.js itself — Phase 124 already shipped it. If a test
       fails because client.js doesn't yet support a feature (e.g., `.code` on
       HubError), STOP and surface the gap to the user; do NOT patch client.js
       in this phase.
  </action>
  <verify>
    <automated>cd plugins/arcanon &amp;&amp; node --test worker/hub-sync/client.test.js</automated>
  </verify>
  <done>
    client.test.js compiles and runs green. All pre-existing 7 tests still pass
    (preserved). 5 new tests (A-E) pass. Total client.test.js test count
    rises from 7 to 12. `err.code` is asserted on at least 8 distinct HubError
    rejections. No `uploadScan(...)` call site in the file is missing `orgId`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create whoami.test.js — getKeyInfo contract pin</name>
  <files>plugins/arcanon/worker/hub-sync/whoami.test.js</files>
  <behavior>
    NEW file. Mirrors the structure of client.test.js (node:test + node:assert/strict +
    fetchImpl injection + jsonResponse helper). Imports `getKeyInfo` from
    `./whoami.js` and `AuthError` from `./auth.js` and `HubError` from `./client.js`.

    Tests to write:

    - **Test 1: getKeyInfo returns parsed { user_id, key_id, scopes, grants }.**
      fetchImpl returns
      `jsonResponse(200, { user_id: "u-1", key_id: "k-1", scopes: ["scan:write"],
                           grants: [{ org_id: "o-1", role: "member" }] })`.
      Call `getKeyInfo("arc_test", BASE_URL, { fetchImpl })`. Assert
      `res.user_id === "u-1"`, `res.key_id === "k-1"`, `res.scopes` deep-equals
      `["scan:write"]`, `res.grants` deep-equals `[{ org_id: "o-1", role: "member" }]`.
      Also capture `init` and assert URL ends with `/api/v1/auth/whoami`,
      method is GET (NOT POST), Authorization is `Bearer arc_test`.

    - **Test 2: 401 throws AuthError.** fetchImpl returns
      `jsonResponse(401, { title: "invalid key", code: "invalid_key" })`.
      assert.rejects with `err instanceof AuthError`. Assert message includes
      "invalid" (case-insensitive).

    - **Test 3: 403 throws AuthError.** fetchImpl returns
      `jsonResponse(403, { title: "scope missing", code: "insufficient_scope" })`.
      assert.rejects with `err instanceof AuthError`. (Both 401 and 403 are
      auth-class per AUTH-02.)

    - **Test 4: Network error throws HubError.** fetchImpl throws
      `new Error("ECONNREFUSED")`. assert.rejects with `err instanceof HubError`,
      `err.retriable === true`, message includes "ECONNREFUSED" or "network".

    - **Test 5: 500 throws HubError (NOT AuthError).** fetchImpl returns
      `jsonResponse(500, { title: "internal error" })`. assert.rejects with
      `err instanceof HubError` (NOT AuthError — server errors are transport
      class, not auth class). Assert `err.status === 500`.

    - **Test 6: Empty grants array is preserved.** fetchImpl returns
      `jsonResponse(200, { user_id: "u-1", key_id: "k-1", scopes: [], grants: [] })`.
      Assert `Array.isArray(res.grants)` and `res.grants.length === 0`. (This
      pins the AUTH-06 "0 grants → fail loud" branch — whoami doesn't fail;
      the LOGIN flow fails on empty.)

    - **Test 7: Multi-grant array is preserved in order.** fetchImpl returns
      `jsonResponse(200, { ..., grants: [{ org_id: "o-1", role: "owner" },
                                          { org_id: "o-2", role: "member" }] })`.
      Assert `res.grants.length === 2` and the first element's org_id is "o-1"
      (order preserved — needed because AUTH-06's AskUserQuestion list relies on
      stable order).

    Implementation rules:
    - Top of file mirrors client.test.js: same import style, same BASE_URL constant,
      same `jsonResponse(status, body, headers={})` helper (paste it — these test
      files are intentionally siblings, no shared fixture file).
    - Total: 7 `test(...)` blocks, ~18 assertions.
  </behavior>
  <action>
    1. Confirm `plugins/arcanon/worker/hub-sync/whoami.js` exists (created by
       Phase 124 AUTH-02). If absent, STOP and surface the gap — Phase 124 has
       not shipped and this phase cannot complete.
    2. Read whoami.js to confirm the exact export name (`getKeyInfo`) and the
       signature `(apiKey, hubUrl, { fetchImpl } = {})`. If the signature differs,
       update the test imports to match — but do NOT modify whoami.js.
    3. Create `plugins/arcanon/worker/hub-sync/whoami.test.js` with the 7 tests
       described above.
    4. Run `node --test plugins/arcanon/worker/hub-sync/whoami.test.js` and
       confirm green before moving on.
  </action>
  <verify>
    <automated>cd plugins/arcanon &amp;&amp; node --test worker/hub-sync/whoami.test.js</automated>
  </verify>
  <done>
    whoami.test.js exists, runs, all 7 tests pass. Imports `getKeyInfo` from
    `./whoami.js`, `AuthError` from `./auth.js`, `HubError` from `./client.js`.
    Asserts the exact { user_id, key_id, scopes, grants } shape from AUTH-02.
    Auth-class (401/403) → AuthError; network/5xx → HubError contract is
    pinned with at least 4 distinct rejection assertions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend integration.test.js — login round-trip + resolution precedence</name>
  <files>plugins/arcanon/worker/hub-sync/integration.test.js</files>
  <behavior>
    Preserve all 3 existing tests (happy-path, 5xx-enqueue, 422-no-enqueue).
    Add NEW tests that exercise the Phase 125 login flow + Phase 124 resolution
    precedence END-TO-END through the mock hub.

    Fixture additions (factor into helpers at the top of the file):

    - `withTempHome(fn)` — async helper that creates `fs.mkdtempSync(...)/.arcanon/`,
      sets `process.env.HOME` to the parent tmpdir for the duration, runs `fn`,
      then restores HOME and `rmSync`s the tmpdir. Captures the `~/.arcanon/config.json`
      that storeCredentials writes.
    - `withTempRepo({ orgIdInRepoConfig })` — extends `makeTempGitRepo` to write
      an `arcanon.config.json` with `{ "hub": { "org_id": orgIdInRepoConfig } }`
      at the repo root. Returns the repo path.
    - `mockWhoami(grants)` — returns a hub mock handler that responds to
      `GET /api/v1/auth/whoami` with `{ user_id: "u-1", key_id: "k-1",
      scopes: ["scan:write"], grants }`, AND to `POST /api/v1/scans/upload`
      with 202 + scan_upload_id. The handler reads `req.url` to dispatch.

    Tests to add:

    - **Test 4: /arcanon:login --org-id round-trips through storeCredentials → resolveCredentials.**
      withTempHome:
        1. Start mock hub; mockWhoami returns 1 grant for org "o-explicit" (just to
           confirm whoami is called for verification per AUTH-06).
        2. Call `storeCredentials("arc_login", { hubUrl: url, orgId: "o-from-cli" })`.
        3. Read the file at `${HOME}/.arcanon/config.json`; assert it contains
           `api_key: "arc_login"`, `hub_url: url`, `default_org_id: "o-from-cli"`.
        4. Call `resolveCredentials()` with no opts.
        5. Assert returned shape: `{ apiKey: "arc_login", hubUrl: url,
           orgId: "o-from-cli", source: "home-config" }`.
      Stop mock hub.

    - **Test 5: /arcanon:login WITHOUT --org-id calls whoami.** withTempHome:
        1. Start mock hub; mockWhoami returns exactly 1 grant for "o-only".
        2. Track whether `/api/v1/auth/whoami` was hit (`let whoamiHit = false`
           in the handler).
        3. Invoke the login flow function (Phase 125 ships this — likely
           `loginFlow({ apiKey, hubUrl, orgId: undefined })` exported from
           `worker/cli/hub.js` or a new `worker/hub-sync/login.js`; check
           Phase 125 SUMMARY for the actual export name. If exported as
           `cmdLogin` from hub.js, invoke that.).
        4. Assert `whoamiHit === true`.
        5. Assert the resulting persisted config has `default_org_id === "o-only"`
           (auto-selected from N=1 grant).
      Stop mock hub. NOTE: if Phase 125 fronted login behind a CLI-only path that
      requires reading argv, the test should call the underlying flow function
      directly — not shell out via execFileSync. Surface to the user as a
      checkpoint if the only available surface is a `cmdLogin` that reads argv.

    - **Test 6: Resolution precedence — per-repo hub.org_id beats ARCANON_ORG_ID beats default_org_id.**
      This is THE precedence test from the success criteria. Use three sub-cases
      in a single `test()` block:

        Sub-case A: Only home-config has org_id.
          - storeCredentials writes `default_org_id: "o-home"`.
          - delete process.env.ARCANON_ORG_ID.
          - withTempRepo with NO arcanon.config.json hub.org_id (use `null`).
          - Call `_readHubConfig` from `manager.js` against that repo, then
            `resolveCredentials({ orgId: hubCfg.orgId || undefined })`.
            Assert `creds.orgId === "o-home"` and `creds.source === "home-config"`.

        Sub-case B: Env beats home.
          - Same home-config (`o-home`), set `process.env.ARCANON_ORG_ID = "o-env"`.
          - Same repo (no per-repo override).
          - Assert `creds.orgId === "o-env"` and `creds.source === "env"`.

        Sub-case C: Per-repo beats env beats home.
          - Same home (`o-home`), env still `"o-env"`,
          - withTempRepo({ orgIdInRepoConfig: "o-repo" }).
          - Read `_readHubConfig(repoPath)`; assert returned `orgId === "o-repo"`.
          - Pass that `orgId` into `resolveCredentials({ orgId: "o-repo" })`;
            assert `creds.orgId === "o-repo"` and `creds.source === "explicit"`
            (or "repo-config" — match whichever string Phase 124 chose; check
            auth.js source).

      Cleanup `delete process.env.ARCANON_ORG_ID` in finally.

    - **Test 7: syncFindings threads X-Org-Id end-to-end.** Strict superset of
      existing happy-path Test 1: same setup but with `default_org_id` set in
      home config; assert the mock-hub `req.headers["x-org-id"]` is `"o-home"`.
      This is the integration-level pin that the orgId actually reaches the wire
      (client.test.js Test A pins it at the unit level; this pins it at the
      `syncFindings` orchestrator level).

    - **Test 8 (optional, skip if Phase 125 doesn't expose a reachable seam):
      AskUserQuestion-mock for N>1 grants.** Phase 125 AUTH-06's multi-grant
      branch calls `AskUserQuestion`. Stub via dependency injection if the
      login flow accepts an `ask` function param, OR via setting a module-level
      mock if not. mockWhoami returns 2 grants; assert the mocked ask was called
      with both org_ids in the choices; assert the persisted config has the
      org_id the mock "selected". If Phase 125 didn't expose an injectable
      seam, skip this test and add a TODO comment with the gap — flag in
      summary.

    Implementation rules:
    - HOME tmpdir swap is the ONLY way to test storeCredentials/resolveCredentials
      without mutating the real ~/.arcanon (which contains the developer's actual
      key). Always wrap in try/finally to restore HOME.
    - Existing tests use `process.env.ARCANON_API_KEY` and `process.env.ARCANON_DATA_DIR`;
      keep that pattern. Add `process.env.ARCANON_ORG_ID` as the third env var
      threaded through finally cleanup.
    - The mockHub handler dispatches by `req.url`: `/api/v1/auth/whoami` → whoami
      JSON; `/api/v1/scans/upload` → 202; everything else → 404.
    - For Test 6 (precedence), the test does NOT need to round-trip through
      `uploadScan` — it can directly assert `resolveCredentials` return values.
      Faster and more precise.
  </behavior>
  <action>
    1. Read the current 156-line integration.test.js. Note the existing
       fixture helpers (`makeTempGitRepo`, `startMockHub`, `stopServer`).
    2. Add the 3 new helpers at the top: `withTempHome`, `withTempRepo`,
       `mockWhoami` (factor `mockWhoami` to dispatch by req.url).
    3. Verify the Phase 125 login-flow export name. Check `plugins/arcanon/worker/cli/hub.js`
       for `cmdLogin`, OR `plugins/arcanon/worker/hub-sync/login.js` if Phase 125
       extracted it. Use `grep -n "export.*[lL]ogin" plugins/arcanon/worker/`. If no
       reachable seam exists for a unit-level call (i.e., login is purely a
       `process.argv`-driven CLI command), document this and either (a) call
       `cmdLogin` after stubbing argv, or (b) skip Test 5 and Test 8 with a
       TODO surfaced in the SUMMARY. Do NOT shell out via execFileSync.
    4. Verify the AUTH-03 source-string for repo-config. Read
       `plugins/arcanon/worker/hub-sync/auth.js` to see whether per-repo override
       lands as `source: "explicit"` (when threaded via opts.orgId) or
       `source: "repo-config"` (if Phase 124 added a 4th source enum). Match
       Test 6 sub-case C's assertion to whichever Phase 124 chose.
    5. Append Tests 4, 5, 6, 7, and (conditionally) 8 to integration.test.js.
    6. Run `node --test plugins/arcanon/worker/hub-sync/integration.test.js`
       and confirm green. The 3 existing tests must still pass; the 4-5 new
       tests must all pass.
    7. Run the FULL node test suite from the repo root:
       `npm test`
       Confirm exit 0. The pre-existing v0.1.2 mock failure is permitted to remain
       (one historical carry-forward). Confirm no NEW pre-existing-mock
       carryforwards appear; the suite-level pass/fail count should be
       v0.1.4 baseline (774 passing / 1 known fail) plus the new tests added by
       Tasks 1, 2, 3 (~16 new passing tests). If the failing-test count climbs
       above 1, STOP — that is the failure gate this phase exists to detect.
  </action>
  <verify>
    <automated>cd plugins/arcanon &amp;&amp; npm test</automated>
  </verify>
  <done>
    integration.test.js compiles and runs green with all original 3 tests +
    4-5 new tests. `npm test` from the repo root exits 0. Failing-test count
    is exactly 1 (the pre-existing v0.1.2 mock carryforward) — no new
    carryforwards introduced. Test 6's three sub-cases (A/B/C) explicitly
    assert the precedence ordering (per-repo > env > home), and Test 7
    asserts the X-Org-Id header arrives at the mock-hub end-to-end through
    syncFindings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → real ~/.arcanon/ | Tests must NEVER touch the developer's real ~/.arcanon/config.json (would leak the dev's actual API key into test artifacts and could destroy their login state). Mitigated by `withTempHome` — `process.env.HOME` swap to a `mkdtempSync` tmpdir, restored in `finally`. |
| test fixture → real network | Tests must NEVER hit a real hub. Mitigated by `fetchImpl` injection (client.test.js, whoami.test.js) and `startMockHub` (integration.test.js — listens on 127.0.0.1 ephemeral port). |
| test process → ARCANON_* env vars | Tests mutate `process.env.ARCANON_ORG_ID`, `ARCANON_API_KEY`, `ARCANON_DATA_DIR`, `HOME`. Each test must restore them in `finally` so test ordering doesn't matter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | Tampering | test fixture clobbers real `~/.arcanon/config.json` | mitigate | Always wrap `storeCredentials` calls in `withTempHome`. No exceptions. Test fails if real HOME was used (assert that the path written contains the tmpdir prefix). |
| T-126-02 | Information Disclosure | test logs include real API keys from leaked env | mitigate | Tests use only synthetic keys (`arc_test`, `arc_login`, `arc_it`). Pre-test `delete process.env.ARCANON_API_KEY` to avoid inheriting a real key from the dev's shell. |
| T-126-03 | Denial of Service | test leaks ephemeral port / tmpdir / mock server | mitigate | Every `startMockHub` call paired with `stopServer` in `finally`. Every `mkdtempSync` paired with `rmSync({recursive:true, force:true})` in `finally`. Existing pattern in this file is consistent — preserve it. |
| T-126-04 | Repudiation | test passes today, regresses silently tomorrow | mitigate | Every assertion in this plan is observable via `npm test` exit code. No "manual verification". The phase is exactly an automated regression gate against the AUTH-01..09 contract. |
| T-126-05 | Spoofing | mock hub serves wrong response shape and tests pass against fiction | accept | Risk: tests pass against a mock contract that doesn't match Phase 124's actual server response. Mitigation requires running the suite once against a real hub with THE-1030 deployed — that's Phase 127's job (VER-04). This phase pins the CONTRACT as documented in REQUIREMENTS.md AUTH-01..09; if the hub disagrees with the contract, that's a hub bug or a spec bug, not a test bug. |
| T-126-06 | Elevation of Privilege | tests modify production code paths to make themselves pass | mitigate | Plan explicitly forbids modifying any non-test file. Tasks 1/2/3 each list ONLY test files in `<files>`. If a test fails because the source code is wrong, executor STOPS and surfaces the gap — does not patch source. This is the regression-gate phase, not the implementation phase. |
</threat_model>

<verification>

## Phase-Level Acceptance Gate

**Single command:** `npm test` (from repo root).

**Pass criteria:**
1. Exit code 0.
2. The `passing` line in node:test output increases by ~16 over the v0.1.4
   baseline (current: 774 passing). Target: roughly 790 passing, ±2 for
   counting differences in table-driven tests.
3. The `failing` line is exactly 1 — the historical v0.1.2 pre-existing mock
   carryforward. NO new failures.
4. The `tests` line in `worker/hub-sync/client.test.js` rises from 7 to 12.
5. `worker/hub-sync/whoami.test.js` exists and reports ≥7 tests.
6. `worker/hub-sync/integration.test.js` rises from 3 tests to 7-8 tests.

**Anti-acceptance:**
- If any test passes by mocking the production code instead of the hub
  network response — REJECT. Tests must exercise real `uploadScan` /
  `getKeyInfo` / `resolveCredentials` / `storeCredentials` code paths,
  injecting only at the `fetchImpl` and `HOME` seams.
- If any test silently swallows an unexpected rejection (`try { ... } catch {}`
  with no assertion) — REJECT.
- If `npm test` ALSO ran a non-test side-effect (e.g., started a real worker,
  wrote to real ~/.arcanon) — REJECT.

## Manual Spot-Check (one-time, post-execution)

Run `grep -rn "X-Org-Id" plugins/arcanon/worker/hub-sync/*.test.js` — must
return at least 2 hits (one in client.test.js header-asserting test, one
in integration.test.js end-to-end test).

Run `grep -rn "missing_org_id\\|missing_x_org_id\\|invalid_x_org_id\\|insufficient_scope\\|key_not_authorized_for_org\\|not_a_member\\|forbidden_scan\\|invalid_key" plugins/arcanon/worker/hub-sync/client.test.js` —
must return 8+ hits (one for each code, plus the fail-fast `missing_org_id`).

Run `grep -n "withTempHome\\|ARCANON_ORG_ID\\|default_org_id" plugins/arcanon/worker/hub-sync/integration.test.js` —
must return 5+ hits across the precedence sub-cases.

</verification>

<success_criteria>

Phase 126 is COMPLETE when all four ROADMAP success criteria are observably true:

1. **client.test.js** asserts X-Org-Id header lands on upload, missing orgId
   throws BEFORE any fetch is issued, each of 7 server error codes produces
   its own user-message-bearing HubError, and a 202 success returns
   `scan_upload_id`. Verified by `grep` for the 7 error-code strings + a
   visual scan of the test names.

2. **whoami.test.js** exists and asserts `getKeyInfo` returns parsed
   `{ user_id, key_id, scopes, grants }`, auth-class (401/403) throws
   `AuthError`, transport (network / 500) throws `HubError`. Verified by
   `node --test worker/hub-sync/whoami.test.js` exiting 0.

3. **integration.test.js** asserts `/arcanon:login --org-id` round-trips
   through `storeCredentials` → `resolveCredentials`, `/arcanon:login` without
   `--org-id` calls whoami, and the precedence chain (per-repo > env > home)
   holds. Verified by 3 sub-cases in a single precedence test all passing.

4. **`npm test` exits 0** with exactly 1 failing test (the pre-existing v0.1.2
   carryforward; do NOT regress that count). Total passing rises by ~16.

If Phase 124 has NOT shipped (whoami.js missing, AUTH-03 orgId precedence
not in auth.js, HubError.code not on client.js's HubError), this phase
CANNOT pass. Executor must surface the missing dependency to the user
and STOP rather than write tests that pin a fictional contract.

</success_criteria>

<output>

After completion, create `.planning/phases/126-auth-test-suite/126-01-SUMMARY.md`
including:

- New test file: `worker/hub-sync/whoami.test.js` (with line count + test count).
- Modified test files: `worker/hub-sync/client.test.js`, `worker/hub-sync/integration.test.js`
  (with delta line counts: lines added, lines removed).
- Final `npm test` output: passing/failing counts, total runtime.
- Confirmation that the failing-test count is exactly 1 (pre-existing
  v0.1.2 carryforward) — call out the test name explicitly so future
  phases know which one is the legitimate carry.
- Any contract gaps surfaced during execution (e.g., "Phase 125 didn't
  expose an injectable seam for AskUserQuestion mocking — Test 8 was
  skipped with a TODO" — if applicable).
- The exact `err.code` strings asserted, as a checklist, so VER-03 can
  audit quickly.

</output>
